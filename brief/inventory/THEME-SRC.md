# THEME-SRC — source inventory of the ElmsNest DEV theme (side pages + custom sections)

- Theme: `gid://shopify/OnlineStoreTheme/154726400174` — name "ElmsNest - Homepage Rebuild 2026-09-01", role `UNPUBLISHED`, Kalles 5.4.2.
- Pulled: 2026-09-02, read-only via Admin GraphQL `theme.files(filenames:[…]).body.content`. Nothing on Shopify was modified.
- Dump location: `/home/user/ElmsNest/brief/inventory/theme-src/<same path as in the theme>` — **121 files** (list in §0.3).
- Shop facts used below (GraphQL `shop`): name `ElmsNest`, primary domain `https://elmsnest.com`, `currencyCode: ILS`, `moneyFormat: "{{amount}} ₪"`, `moneyWithCurrencyFormat: "{{amount}} ₪"`.
- Caveat on byte sizes: the API returns theme-editor-saved JSON re-serialised (pretty-printed). Five JSON files therefore differ from the size the theme reports (`config/settings_data.json` 9265→13492 B, `sections/header-group.json` 2283→2646, `sections/footer-group.json` 10610→10973, `templates/product.elms-pdp.json` 16517→25661, `templates/product.elmsnest.json` 17000→25981). Content is what the API delivered, verbatim; all other 116 files match byte-for-byte.
- Facts only; no design judgement. Settings keys/values are quoted verbatim from the dumped files.

---

## 0. Inventory

### 0.1 Every template in the theme (STEP 1 listing, name → size in bytes)

templates/404.json 546 · article.json 11472 · article.post-with-instagram-shop.json 27923 · article.post-with-lookbook.json 22693 · article.post-with-shop.json 18668 · blog.json 5699 · blog.masonry.json 5718 · blog.sidebar-drawer.json 5700 · blog.sidebar-left.json 5729 · blog.sidebar-right.json 5730 · cart.json 3996 · collection.drawer-sidebar.json 6472 · collection.filter-sidebar.json 4133 · collection.full-width.json 3667 · collection.infinite.json 3625 · collection.json 3778 · collection.left-sidebar.json 6535 · collection.load-more.json 3627 · collection.progress-bar.json 3680 · collection.right-sidebar.json 6537 · collection.sub.json 11192 · customers/account.json 1619 · customers/activate_account.json 779 · customers/addresses.json 1621 · customers/login.json 1535 · customers/order.json 1438 · customers/register.json 1542 · customers/reset_password.json 1420 · gift_card.liquid 23398 · index.ecomposer.liquid 18 · index.json 14965 (NOT dumped — already in repo `theme/templates/index.json`) · list-collections.json 2687 · page.about-us.json 16978 · page.access-forbidden.json 1789 · page.brands.json 1978 · page.contact-us.json 6857 · page.elmsnest-content.json 502 · page.faq-1.json 1849 · page.faq-2.json 7255 · page.json 1754 · page.kids.json 35272 · page.men.json 28365 · page.privacy-policy.json 1845 · page.returns-exchanges.json 1818 · page.shipping-delivery.json 1845 · page.store-locator.json 4591 · page.terms-conditions.json 1813 · page.timeline.json 5948 · page.women.json 46021 · password.liquid 167 · product.elms-pdp.json 16517 · product.elmsnest.json 17000 · product.json 17935 · product.only_config.json 9299 · search.compare.json 2628 · search.json 2716 · search.other.liquid 30 · search.wishlist.json 3084.

Not dumped by instruction: `templates/index.json`, `article.post-with-*.json` (3), `blog.masonry/sidebar-*.json` (4).

### 0.2 `blocks/_product-*.liquid` (names + sizes only, not dumped)

_product-ask-question 1490 · _product-brands 1251 · _product-buy-button-qa 18066 · _product-buy-button 18465 · _product-carousel-article 52545 · _product-carousel 61650 · _product-countdown 12131 · _product-delivery-return 1589 · _product-description 10517 · _product-extra-link 600 · _product-fbt-2 15897 · _product-fbt 13407 · _product-grid 46659 · _product-inventory 9137 · _product-item-property 14487 · _product-link-custom 1007 · _product-link-popup 1598 · _product-linked 12902 · _product-list 20930 · _product-live-review 8518 · _product-medias 33469 · _product-meta-available 2168 · _product-meta-barcode 1118 · _product-meta-category 617 · _product-meta-options 699 · _product-meta-sku 1061 · _product-meta-tags 1089 · _product-meta-type 890 · _product-meta-vendor 920 · _product-meta 729 · _product-order 14301 · _product-payment-terms 1095 · _product-pickup 8595 · _product-price 2534 · _product-size-chart 4408 · _product-sold 8940 · _product-tab-add 1425 · _product-tab-des 923 · _product-tab-html 1298 · _product-tab-liquid 1099 · _product-tab-review 813 · _product-tax-ship 726 · _product-title-qv 6236 · _product-title 6263 · _product-trust-badge 4665 · _product-variant-picker 19269 · _product-video 7035 (47 files).

### 0.3 Files dumped to `brief/inventory/theme-src/` (121)

- `layout/theme.liquid`, `config/settings_data.json`
- `sections/header-group.json`, `sections/footer-group.json`, `sections/system-group.json`
- templates (49): `templates/404.json article.json blog.json cart.json collection.json collection.{drawer-sidebar,filter-sidebar,full-width,infinite,left-sidebar,load-more,progress-bar,right-sidebar,sub}.json customers/{account,activate_account,addresses,login,order,register,reset_password}.json gift_card.liquid index.ecomposer.liquid list-collections.json page.json page.{about-us,access-forbidden,brands,contact-us,elmsnest-content,faq-1,faq-2,kids,men,privacy-policy,returns-exchanges,shipping-delivery,store-locator,terms-conditions,timeline,women}.json password.liquid product.json product.elms-pdp.json product.elmsnest.json product.only_config.json search.json search.compare.json search.other.liquid search.wishlist.json`
- custom sections (18): `sections/elms-pdp-{comparison,description,faq,feature-grid,installation,night-gallery,problem-solution,reviews,trust-strip,warranty-shipping}.liquid`, `sections/elmsnest-{cart-guidance,content-page,product-guidance}.liquid`, `sections/elmsnest-home-{growth,hero,places,shop,trust}.liquid`
- custom snippets (16): `snippets/elms-pdp-{compare-cell,icon}.liquid`, `snippets/elmsnest-pdp-{direct-answer,facts,faq,not-fit,price,specs,trust,unit-price}.liquid`, `snippets/elmsnest-{product-schema,measurement-preview,home-product-card,whatsapp}.liquid`
- custom assets (2): `assets/elmsnest-pdp.css`, `assets/elms-pdp-faq.js`
- Kalles sections (21): `sections/{main-collection,main-product,main-product-tabs,main-cart,cart-drawer,main-search,main-404,main-page,main-heading,main-list-collections,contact-form,main-login,main-register,related-products,recently-products,toolbar-mobile,back_top,sidebar-product,brc-nav-product,hdt_predictive-search,ecom-predictive-search}.liquid`
- Kalles snippets (11): `snippets/{card-product1,card-price,card-product-media,item-cart,item-cart-page,product-variant-options,product-media,product-qty-selector,cart-shipping-bar}.liquid` + extras `snippets/css-variables.liquid` (defines the `--en-*` tokens) and `snippets/facets.liquid` (the filter UI `main-collection`/`main-search` render).
- `config/settings_schema.json` (79625 B) was fetched for default lookups only and kept in the scratchpad, not in the repo.

Other custom files that exist in the theme but were **not** dumped (outside the requested list): `sections/elmsnest-{collections,hero,places,products,terms}.liquid`, `sections/elmsnest-v2-{atmosphere,first-lit,goodnight,hero,night-wall,places,switch,terms}.liquid`, `snippets/elmsnest-v2-{base,buy,fonts,price}.liquid`, `assets/elmsnest-hero-{desktop,mobile}-performance.webp`.

---

## 1. Template → sections (`order`) → settings that matter

Notation: `[section id] type` then `key: value`. Colour-scheme ids are abbreviated: **S1** = `scheme-1` (cream, text #2b2118 / bg #f7f0e6), **S-night77** = `scheme-77e4ef58-56a9-4631-876a-12098ba7d57d` (dark, text #f7f0e6 / bg #2b2118), **S-c947** = `scheme-c9476b69-abc7-454f-a946-2d5a9f5f3a37` (cream), **S-e0b7** = `scheme-e0b73555-c767-4eda-aa6d-4e44e10aefce` (light #fffdf7), **S-env2** = `scheme-env2-night` (#020306/#f4eee3/accent #ffd394). Full values in §3.1. "DISABLED" = `"disabled": true` (Liquid excludes disabled blocks from `section.blocks`).

### 1.1 Live-path templates

| Template | `order` → sections and the settings that matter |
|---|---|
| `templates/404.json` | `[main] main-404` — `color_scheme: "scheme-1"`, `link: "/"`. Section renders `h1 templates.404.title` (`hdt-h0`), `h3 templates.404.subtext`, `p templates.404.link_html`, min-height 60vh, centered. |
| `templates/collection.json` | `[collections_list_simple_4yRUED] top-list-collections` — `show_type: "all"`, `menu: "footer"`, `sort: "alphabetical"`, `hide_mobile: false`, `navUI: true`, `color_scheme: "scheme-1"`, `section_layout: "container"`. → `[main_heading_PHNGje] main-heading` — `use_dynamic_source: false`, `image: "shopify://shop_images/bg_collections.jpg"`, `color_scheme: S-night77`, `bg_overlay: 54`, `pd: "55px,,55px,"`, `pd_mb: "34px,,34px,"`; blocks `_heading_liquid` `text: "{{ collection.title }}"`, `font_family: "hdt-font-1"`, `text_size: "2xl"`, `uppercase: false`; `_heading_brc` (breadcrumb). → `[main-collection] main-collection` — `show_description: true`, `description_position: "before"`, `use_read_more: false`, **`product_des: "1"`** (→ `snippets/card-product1.liquid`), **`image_ratio: "custom-pr"`**, `image_size: true`, `show_vendor: false`, `pr_border: "none"`, **`products_count: 12`**, **`col_dk: "4"`, `col_tb: "3"`, `col_mb: "2"`**, `space_items: "x"`, **`pagination_type: "links"`**, `enable_progressbar: true`, `paginate_pos: "center"`, `dis_pagination: 40`, `btn_style: "hdt-btn-outline"`, `size: "xl"`, **`enable_sorting: true`, `enable_filtering: true`, `enable_num_cols_selector: true`**, `widget_heading: "קטגוריות מוצרים"`, `cat_source: "1"`, `count: false`, `color_scheme: "scheme-1"`, `section_layout: "container"`, `pd: ",,30px,"`, `pd_mb: ",,22px,"`, `sidebar_drawer: false`, `sidebar_position: "left"`, `sidebar_size: "small"`, `colors_by_section_sb: true`; blocks: `[filter_KX6bJW] filter` **DISABLED** → `section.blocks.size == 0` → `has_sidebar = false` (main-collection.liquid l.27–39); the filter button + `render 'facets'` still render because `enable_filtering: true` (l.171–177), plus `layout-switch` (l.188) and `sort-by-popover` (l.193), `active-filters` (l.198). |
| `templates/search.json` | `[collections_list_simple_4yRUED] top-list-collections` (same as above, + `hdt_background_parallax: false`). → `[main_heading_xYyUyq] main-heading` — `use_dynamic_source: false`, no image, `color_scheme: S-night77`, `bg_overlay: 54`, `pd: "50px,,50px,"`, `pd_mb: "40px,,40px,"`; only block `_heading_search` **DISABLED** (band renders with no heading text). → `[main-search] main-search` — `product_des: "1"` (card-product1), **`image_ratio: "asos"`**, `image_size: true`, `show_vendor: false`, `pr_border: "none"`, `products_count: 16`, `col_dk: "4"`, `col_tb: "3"`, `col_mb: "2"`, `space_items: "x"`, `pagination_type: "links"`, `paginate_pos: "center"`, `dis_pagination: 60`, `enable_sorting: true`, `enable_filtering: true`, `price_behavior: "auto"`, `enable_num_cols_selector: true`, `full_width: false`, `color_scheme: "scheme-1"`, `pd: ",,20px,"`. Section renders `facets`, `layout-switch`, `sort-by-popover`, `active-filters`, products via `card-product1`, collections results via `collection-item`, articles via `pr_loop_item`. |
| `templates/cart.json` | `[main_heading_zHKQUU] main-heading` — `color_scheme: S-night77`, `bg_overlay: 54`, `space_item: 0`, `pd: "50px,,50px,"`; block `_heading_liquid` `text: "{{ page_title }}"`, `text_size: "2xl"`, `uppercase: true`, `line_height: "hdt-lh-1"`. → `[main-cart] main-cart` — `show_cart_note: false`, `show_free_shipping_bar: false`, `show_gift: false`, `show_estimate: false`, `show_cart_discount: false`, `discount_error_message: "יש להזין קוד הנחה תקין"`, `pd: "70px,,50px,"`; blocks: `subtotal`, `tax`, `agree` DISABLED (`content: "אני מסכים/ה לתנאי השימוש ולמדיניות הפרטיות"`), `btn_checkout` (`color_btn: "#f7f0e6"`, `background_btn: "#2b2118"`, `background_hover_btn: "#21180f"`, `border_btn: "#2b2118"`, `size: "m"`, `text_size: "hdt-text-base"`), `accelerated_btns`, `img_trust` DISABLED (`svg: "visa,paypal,master,american_express,amazon"`), `payment-terms`. Line items via `snippets/item-cart-page.liquid` + `line-item-price` (main-cart.liquid l.95–96). → `[elmsnest_cart_guidance] elmsnest-cart-guidance` (no settings; see §2). |
| `templates/product.json` (default product template) | `[breadcrumb] brc-nav-product` — `back_next_pr: true`, `colors_by_section: true`, `color_scheme: S-c947`, `background_opacity: 1`, `section_layout: "wrapper_container"`, `pd: "20px,,20px,"`, `pd_mb: "15px,,15px,"`. → `[main-product] main-product` — `sticky_atc: true`, `hdt_layout_atc_mob: "hdt_layout_atc_mob_default"`, `hdt_show_sticky_atc: "hdt_show_scrolls_outside_the_scope_of_the_form"`, `ani: "none"`, `fullwidth: false`, `pd: "30px,,60px,"`, `pd_mb: "22px,,45px,"`; block tree: `[group_product_9RYttB] _group-product` → `[product_title_ehRqkq] _product-title` (`tag: "h1"`, `heading_size: "h_custom"`, `font_size_heading_lg: 32`, `font_size_heading: 25`, `font_weight: "semibold"`), `[product_price_eTf3F8] _product-price` (`label_onsale_style: "none"`), `[liquid_CpnXHU] _liquid` **DISABLED** (`option_type: "liquid"`, `text` = a "free shipping" banner `<section class="fsb">` with kicker "חדשות טובות בדרך אליכם", title "משלוח חינם לנקודת איסוף", detail "…שליח עד הבית בתוספת 29.90 ₪", gradient/box-shadow/radius CSS), `[liquid_TJzckp] _liquid` **DISABLED** (`hdt-product__tax-ship` line "המחיר כולל מס. משלוח לנקודת איסוף חינם; שליח עד הבית בתוספת 29.90 ₪." + link `/policies/shipping-policy`), `[product_inventory_cQVk3B] _product-inventory` **DISABLED** (`show_inventory_quantity: true`, `show_progress_bar: true`, `progress_bar_max: 50`), `[product_variant_picker_XNdArY] _product-variant-picker` (`color_picker_type: "block"`, `color_size: "medium"`, `picker_type: "block"`, `size_chart_source: "page"`), `[product_buy_button_aPmAbD] _product-buy-button` (**`show_quantity_selector: true`**, `show_dynamic_checkout: true`, `show_gift_card_recipient: false`, `show_wishlist: false`, `show_compare: false`, `btn_fullwidth: true`, `btn_style: ""`), `[product_pickup_Jpchr6] _product-pickup`, `[product_description_AJaqwJ] _product-description` (`des: "1"`, `length: 130`, `readm_txt: "קראו עוד"`, `readl_txt: "הציגו פחות"`, `text_color: "hdt-text-secondary"`). → `[elmsnest_product_guidance] elmsnest-product-guidance` (no settings → all schema defaults, `ground` default `"cream"`). → `[elmsnest_related_products] related-products` — `product_des: "1"`, `image_ratio: "adapt_image"`, `image_size: true`, `show_vendor: false`, `display_type: "1"` (Shopify recommendations API, `routes.product_recommendations_url`), `pr_border: "none"`, `limit: 8`, `col_dk: "4"`, `col_tb: "3"`, `col_mb: "2"`, `space_items: "x"`, `navUI: true`, `nav_visible: "hover"`, `nav_style: "outline"`, `nav_shape: "rounded"`, `nav_size: "small"`, `dotUI: false`, `section_layout: "container"`, `pd: "60px,,45px,"`, `pd_mb: "42px,,30px,"`; block `[pr-heading] section-heading` (`design_heading: "1"`, `alignment: "center"`, `heading_gap: 18`, `margin_bottom: 30`). Cards come from `snippets/related_option.liquid` (not dumped). → `[sidebar] sidebar-product` **DISABLED** (`space: 50`). → `[custom_liquid_eAd3KJ] custom-liquid` **DISABLED** — `custom_liquid` = `<section class="elms-feeling-hero" dir="rtl">` with hard-coded CDN image `elmsnest-garden-evening.jpg` (alt "גינה מוארת באווירה חמימה בשעות הערב"), eyebrow "הבית, כמו שאתם אוהבים", h2 "הבית מתחיל בתחושה", text "לא רק מוצרים — אלא אווירה שתרצו לחזור אליה בכל ערב.", CSS `border-radius:26px; box-shadow:0 20px 55px …`; `section_layout: "full_width"`, `mg: ",,50px,"`. |
| `templates/product.elms-pdp.json` | `order`: `breadcrumb, main-product, elms_trust_strip, elms_problem_solution, elms_night_gallery, elms_feature_grid, elms_installation, elmsnest_product_guidance, elms_reviews, elms_comparison, elms_policy, elms_faq, elmsnest_related_products`. `[breadcrumb]` as product.json. `[main-product]` same settings but `pd: "30px,,40px,"`, `pd_mb: "22px,,30px,"`; group children: `_product-title` (`font_size_heading_lg: 38`, `font_size_heading: 26`, `semibold`), `[liquid_en_direct_answer] _liquid` `text: "{% render 'elmsnest-pdp-direct-answer', product: product %}"`, `[liquid_en_price] _liquid` → `elmsnest-pdp-price`, `[liquid_en_facts] _liquid` → `elmsnest-pdp-facts`, `_product-variant-picker` (block/medium/page), `[liquid_en_unit_price] _liquid` → `elmsnest-pdp-unit-price`, `_product-buy-button` (**`show_quantity_selector: false`**, dynamic checkout true, fullwidth true), `[liquid_en_specs] _liquid` → `elmsnest-pdp-specs`, `_product-description` (`des: "1"`, `length: 130`). `[elms_trust_strip] elms-pdp-trust-strip` — `enable: true`, `bg_color: "#FFFDF7"`, `text_color: "#2B2118"`, `padding_top: 28`, `padding_bottom: 28` (no `ground` key → schema default `"cream"`, so the two colours are not applied — custom colours apply only when `ground == "custom"`); 4 `item` blocks: `trust_shipping` (`icon: "truck"`, `label: "משלוח לנקודת איסוף — חינם"`, `note: "שליח עד הבית — 29.90 ₪"`), `trust_cancel` (`return`, "ביטול עסקה עד 14 יום", "בהתאם לחוק הגנת הצרכן ולמדיניות הביטולים"), `trust_outdoor` (`drop`, "מיועד לשימוש בחוץ", "דרגת העמידות המדויקת מופיעה במפרט הטכני"), `trust_ask` (`chat`, "אפשר לשאול לפני שמזמינים", "נבדוק יחד אם המוצר מתאים למקום שלכם"). `[elms_problem_solution] elms-pdp-problem-solution` — `heading: "החצר חשוכה — ואין שקע איפה שצריך אור"`, `problem_kicker: "המצב היום"`, `problem_title: "תשתית חשמל בחוץ היא פרויקט"`, `problem_text` (richtext), `solution_kicker: "מה שאנחנו מציעים"`, `solution_title: "תאורה שמותקנת בלי חוטים"`, `solution_text`, `image_position: "end"`, no `image`, `bg_color: "#F7F0E6"`, `text_color: "#2B2118"`, padding 56/56. `[elms_night_gallery] elms-pdp-night-gallery` — `heading: "ככה זה נראה בפועל"`, `subheading`, `note: "התמונות להמחשה…"`, `columns: "2"`, padding 96/96 (no `source`/`skip_first`/`max_images`/`ground`/`kicker` keys → defaults `auto`/1/6/`night`/"מקרוב"). `[elms_feature_grid] elms-pdp-feature-grid` — `heading: "הנתונים שחשוב לבדוק"`, `note: "מוצג רק נתון שהיצרן מפרסם. נתון שאינו מופיע כאן — אינו מובטח."`, `bg_color: "#FFFDF7"`, `text_color: "#2B2118"`, padding 56; 6 `fact` blocks: `feat_ip` (`source: "ip_rating"`, `label: "עמידות למים"`, `icon: "drop"`), `feat_charge` (`charge_time_hours`, "זמן טעינה", `sun`), `feat_hours` (`light_hours`, "שעות תאורה", `clock`), `feat_temp` (`color_temp`, "גוון אור", `bolt`), `feat_sensor` (`sensor_range_m`, "טווח חיישן", `shield`), `feat_material` (`material`, "חומר", `tool`). `[elms_installation] elms-pdp-installation` — `heading: "ההתקנה — ללא חוטים וללא חשמלאי"`, `subheading`, `note: "דגמים המחוברים לרשת החשמל דורשים חיבור על ידי חשמלאי מוסמך…"`, `bg_color: "#F7F0E6"`, `text_color: "#2B2118"`, padding 56 (no `ground`/`show_image`/`image_index`/`kicker` → defaults `cream`/`true`/3/"שלב אחר שלב"); 4 `step` blocks `step_sun`, `step_mount`, `step_aim`, `step_charge` (title+text). `[elmsnest_product_guidance] elmsnest-product-guidance` (no settings → defaults). `[elms_reviews] elms-pdp-reviews` — `enable: true`, `heading: "מה לקוחות כתבו"`, `bg_color: "#F7F0E6"`, `text_color: "#2B2118"`, padding 56, **no blocks → renders nothing** (section requires ≥1 `review`/`@app` block). `[elms_comparison] elms-pdp-comparison` — `heading: "מה ההבדל מול מנורה סולארית זולה"`, `subheading: "ההשוואה מתייחסת לאופן שבו אנחנו מוכרים — לא להבטחת ביצועים של מוצר מסוים."`, `note`, `criterion_label: "מה בודקים"`, `col_a_label: "ElmsNest"`, `col_b_label: "מנורה גנרית זולה"`, `bg_color: "#FFFDF7"`, `text_color: "#2B2118"`, padding 56 (no `ground`/`kicker`/`crest_icon` → defaults `night`/"השוואה"/`sun`; the bg/text colours are therefore unused); 5 `row` blocks: `cmp_specs` ("מפרט טכני מבוסס על נתוני היצרן", `mark_a: "check"`, `mark_b: "partial"`, `text_b: "משתנה"`), `cmp_hebrew` ("עמוד מוצר בעברית מלאה", check / partial "לעיתים תרגום אוטומטי"), `cmp_ask` ("אפשר לשאול לפני ההזמנה", check `text_a: "בוואטסאפ"` / partial "משתנה"), `cmp_cancel` ("ביטול עסקה לפי חוק הגנת הצרכן", check "עד 14 יום" / partial "תלוי במוכר"), `cmp_notfit` ("נאמר לכם גם מתי המוצר לא מתאים", check / **`mark_b: "cross"`, `text_b: "לרוב לא"`**). `[elms_policy] elms-pdp-warranty-shipping` — `heading: "משלוח, ביטול והחזרות"`, `subheading: "כל הפרטים לפני שמזמינים — בלי הפתעות אחרי התשלום."`, `bg_color: "#F7F0E6"`, `text_color: "#2B2118"`, padding 56 (no `ground`/`columns` → `cream`/`"2"`); 4 `panel` blocks: `policy_time` (`clock`, "זמן אספקה", body "8–17 ימי עסקים: 1–3 ימי טיפול ועוד 7–14 ימי משלוח", `link_page: "processing-time"`, `link_text: "לפרטי זמני הטיפול"`), `policy_ship` (`truck`, "דמי משלוח", "משלוח לנקודת איסוף בישראל — חינם. שליח עד הבית — 29.90 ₪.", `link_page: "shipping-delivery"`), `policy_cancel` (`return`, "ביטול עסקה", 14 days), `policy_ask` (`chat`, "שאלה לפני ההזמנה", `link_page: "contact-us"`). `[elms_faq] elms-pdp-faq` — `heading: "שאלות ותשובות"`, `source: "both"`, `note: "לא מצאתם תשובה? שלחו לנו הודעה לפני שאתם מזמינים."`, `bg_color: "#FFFDF7"`, `text_color: "#2B2118"` (legacy, unused), padding 56 (floor 64px in CSS), no `ground`/`kicker` → `cream`/"לפני שמזמינים"; 3 enabled `question` blocks `faq_when`, `faq_cancel`, `faq_fit`. `[elmsnest_related_products] related-products` — as product.json but `nav_shape: "default"`. |
| `templates/product.elmsnest.json` | `order`: `breadcrumb, main-product, elms_trust_strip, elms_description, elms_showcase, elms_installation, elms_comparison, elmsnest_product_guidance, elms_faq, elms_policy, elmsnest_related_products`. `[main-product]` `pd: "30px,,60px,"`; group children: `_product-title` (38/26 semibold), `liquid_en_direct_answer`, `liquid_en_price`, `liquid_en_facts`, `_product-variant-picker`, `liquid_en_unit_price`, `_product-buy-button` (`show_quantity_selector: false`), `[liquid_en_trust] _liquid` → `elmsnest-pdp-trust`, `[liquid_en_specs]` **DISABLED**, `[liquid_en_faq] _liquid` → `elmsnest-pdp-faq`; **no `_product-description` block** (description moved to the `elms-pdp-description` section). `[elms_trust_strip]` — `ground: "card"`, padding 44/44, same 4 items but `label: "משלוח לנקודת איסוף חינם"`. `[elms_description] elms-pdp-description` — `aria_label: "תיאור המוצר"`, `ground: "cream"`, padding 96/96. `[elms_showcase] elms-pdp-night-gallery` — `kicker: "מקרוב"`, `heading: "ככה זה נראה בפועל"`, `source: "auto"`, `skip_first: 1`, `max_images: 6`, `ground: "night"`, `columns: "2"`, padding 96. `[elms_installation]` — `kicker: "התקנה"`, `heading: "ההתקנה  ללא חוטים וללא חשמלאי"` (double space, no dash), `show_image: true`, `image_index: 4`, `ground: "cream"`, padding 96; same 4 steps. `[elms_comparison]` — `kicker: "השוואה"`, `crest_icon: "sun"`, `ground: "night"`, `bg_color: "#12100e"`, `text_color: "#e8e3d9"`, padding 96; same 5 rows. `[elmsnest_product_guidance]` — full settings: `kicker: "לפני שמזמינים"`, `heading: "בודקים שהמוצר מתאים למקום שלכם"`, `subheading: "תאורת חוץ נבחרת לפי מיקום, מטרה, מקור חשמל ואופן התקנה לא רק לפי התמונה."`, `step1_icon: "sun"` "מיקום ושימוש", `step2_icon: "bolt"` "מקור חשמל" (text contains `<bdi dir="ltr">USB</bdi>`), `step3_icon: "ruler"` "מידות והתקנה", `link_icon: "tool"`, `link_label: "איך בוחרים תאורה לגינה?"`, `fact1_q: "זמן אספקה"` / `fact1_a` (8–17 / 1–3 / 7–14 days, 29.90 ₪), `fact2_q: "מידע טכני"`, `fact3_q: "צריכים עזרה לפני הקנייה?"`, `fact3_link: "לעמוד יצירת הקשר"`, `ground: "cream"`. `[elms_faq]` — `kicker: "שאלות"`, `source: "both"`, `ground: "cream"`, padding 96, `bg_color: "#fffdf7"`, `text_color: "#2b2118"`; the 3 `question` blocks are all **DISABLED** → only `custom.faq` metafield questions render. `[elms_policy] elms-pdp-warranty-shipping` **DISABLED** (`kicker: "משלוח ואחריות"`, `ground: "night"`, `columns: "4"`, `bg_color: "#f7f0e6"`, `text_color: "#2b2118"`, 4 panels). `[elmsnest_related_products]` as elms-pdp. |
| `templates/page.json` (default page) | `[editorial] elmsnest-content-page` (no settings) → `[heading] main-heading` — `use_dynamic_source: false`, no image, `color_scheme: S-night77`, `bg_overlay: 0`, `pd: "50px,,50px,"`, `pd_mb: "31px,,31px,"`; block `_heading_liquid` `text: "{{ page_title }}"`, `text_size: "4xl"`, `uppercase: false` → `[main] main-page` — `full_width: true`, `pd: "50px,,60px,"`, `pd_mb: "37px,,45px,"` (outputs `{{ page.content }}`, main-page.liquid l.12–14). On the 5 editorial handles the first section injects CSS that hides the `heading` and `main` sections (see §2). |
| `templates/page.elmsnest-content.json` | `[content] elmsnest-content-page` only. |
| `templates/page.contact-us.json` | `[main_heading_Gg63eE] main-heading` — `use_dynamic_source: true`, `color_scheme: S-night77`, `bg_overlay: 0`, `pd: "50px,,50px,"`; block `_heading_liquid` `text: "{{ page.title }}"`, `2xl`, `uppercase: true`. → `[contact_form_zzPDf3] contact-form` — `section_layout: "container"`, `hdt_background_parallax: false`, `padding_top: 50`, `padding_bottom: 50`, `padding_rate: 80`; 2 `_contact_col` blocks with no settings. |
| `templates/page.privacy-policy.json`, `page.shipping-delivery.json`, `page.terms-conditions.json`, `page.returns-exchanges.json` | `[heading] main-heading` — `image: "shopify://shop_images/shop-category.jpg"` (returns-exchanges: `shopping-cart-head.jpg`), `color_scheme: S-night77`, `bg_overlay: 57`, `space_item: 15`, `pd: "50px,,50px,"`, `pd_mb: "31px,,31px,"`; blocks `_heading_liquid` `"{{ page_title }}"` 2xl + `_heading_brc`. → `[main] main-page` — `full_width: false`, `pd: "70px,,70px,"`, `pd_mb: "49px,,49px,"` (terms-conditions: `pd: "40px,,40px,"`, `pd_mb: "28px,,28px,"`). |
| `templates/list-collections.json` | `[main_heading_UKnKXk] main-heading` — `image: "shopify://shop_images/bg-heading.jpg"`, `color_scheme: S-night77`, `bg_overlay: 56`, `pd: "50px,,50px,"`; blocks `_heading_liquid` `"{{ page_title }}"` `text_size: "4xl"` + `_heading_brc`. → `[main] main-list-collections` — `display_type: "all"`, `limit: 6`, `collection_des: "1"`, `content_size: "base"`, `content_weight: "semibold"`, `content_position: "end-center"`, `show_count: false`, `count_label: "Products"`, `image_rounded: false`, `enable_border: false`, `image_padding: 30`, `image_ratio: "square"`, `img_effect: "none"`, `col_dk: "3"`, `col_tb: "2"`, `col_mb: "2"`, `pagination_type: "links"`, `dis_pagination: 60`, `colors_by_section: true`, `color_scheme: "scheme-1"`, `pd: "60px,,80px,"`. Cards via `snippets/collection-item.liquid` (not dumped). |
| `templates/customers/login.json` | `[heading] main-heading` — `image: bg-heading.jpg`, `S-night77`, `bg_overlay: 54`, `pd: "50px,,50px,"`, `pd_mb: "40px,,40px,"`; block `_heading_liquid` `text: "Login"` (English literal), `uppercase: true`. → `[main] main-login` — `colors_by_section: false`, `pd: "80px,,80px,"`, `pd_mb: "40px,,40px,"`. |
| `templates/customers/register.json` | same heading with `text: "Register"`, `bg_overlay: 50`, `uppercase: false` → `[main] main-register` `pd: "80px,,80px,"`. |
| `templates/customers/account.json`, `addresses.json` | `[heading] main-heading` — `bg-heading.jpg`, `bg_overlay: 50`, block `"{{ page_title }}"` `uppercase: true` → `[main] main-account` / `main-addresses` `pd: "30px,,30px,"`. |
| `templates/customers/order.json`, `reset_password.json` | `[main_heading_Gg63eE] main-heading` — `use_dynamic_source: true`, `bg-heading.jpg`, `bg_overlay: 54`, block `"{{ page.title }}"` `uppercase: true` → `[main] main-order` / `main-reset-password` (no settings). |
| `templates/customers/activate_account.json` | `[heading] main-heading` — `image: bg-heading.jpg`, `S-night77`, `bg_overlay: 0`, no blocks → `[main] main-activate-account`. |
| `templates/article.json` | `[main_heading_kcBi3p] heading-article` — `use_dynamic_source: true`, `S-night77`, `bg_overlay: 54`, `space_item: 5`, `pd: "50px,,50px,"`; blocks `_heading_liquid` `"{{ article.title }}"` 2xl `uppercase: true`, `_article_time` (`hdt-font-3`, italic), `_article_category` DISABLED. → `[main-article] main-article` — `full_width: false`, `color_scheme: "scheme-1"`, `pd: "40px,,40px,"`, `sidebar_mode: "none"`; blocks: `_article-image` (`cate_des: "none"`), `spacer`, `_article-title` DISABLED, `_article-content`, `_article-tags`, `spacer` DISABLED, `_article-infos` DISABLED, `spacer`, `_article-shares` (facebook, x, pinterest, tumblr, telegram, whatsapp, email all `true`), `spacer`, `_article-navigation`, `_article-related` (`article_style: "1"`, `limit: 8`, `col_dk: "3"`, `col_tb: "1"`, `col_mb: "2"`, `show_date: false`), `_article-comment` (`dis_pagination: 30`). |
| `templates/blog.json` | `[blog_slider_h7yXkz] blog-slider` — **`blog: "fashion"`** (Kalles demo handle), `image_ratio: "-adapt_image"`, `show_tags: true`, `show_author: true`, `show_date: true`, `date: "%b %d"`, `col_dk: "3"`, `col_tb: "2"`, `col_mb: "1"`, `limit: 8`, `navUI: true`, `nav_style: "outline"`, `nav_shape: "rounded"`, `nav_hide_mobile: true`, `dotUI: false`, `color_scheme: "scheme-1"`, `section_layout: "full_width"`. → `[main] main-blog` — `article_des: "1"`, `show_author: true`, `show_date: true`, `show_tags: false`, `show_content: false`, `show_readmore: false`, `image_ratio: "adapt_image"`, `blogs_count: 6`, `col_dk: "2"`, `col_tb: "2"`, `col_mb: "1"`, `pagination_type: "links"`, `dis_pagination: 60`, `btn_style: "hdt-btn-outline"`, `size: "xl"`, `section_layout: "container"`, `pd: "60px,,60px,"`, `sidebar_mode: "none"`; block `_sidebar_blog`. |
| `templates/password.liquid` | `{%- layout 'password' -%}` + `{%- section 'main-password' -%}`. |
| `templates/gift_card.liquid` | `{% layout none %}` full standalone HTML document (23398 B) with the same RTL detection block as theme.liquid. |
| `templates/search.other.liquid` | `{%- section 'search-other' -%}`. |
| `templates/index.ecomposer.liquid` | `[[ECOM-WORKSPACE]]` (EComposer placeholder). |
| `templates/product.only_config.json` | `wrapper: "div#hdt-page-product-config"`; `order`: `main-quick-view` (block `_qv-group`), `main-quick-add` (blocks `_variant-info`, `_product-variant-picker` `color_picker_type: "image"` `color_size: "extra_large"`, `_quantity-selector_qa`, `_product-buy-button-qa` `btn_style: "hdt-btn-accent"` `btn_fullwidth: true`), `product-group-media`, `color-swatch`. Internal config template; theme.liquid skips head/header/footer for `template == 'product.only_config'` outside the editor. |

### 1.2 Alternate / demo templates (Kalles sample content still present)

| Template | Sections and notable settings |
|---|---|
| `collection.drawer-sidebar.json` | top-list-collections → main-heading (bg_collections.jpg) → main-collection with **`product_des: "6"`**, `products_count: 9`, `col_dk: "4"`, `pagination_type: "links"`, `sidebar_drawer: true`, `sidebar_size: "medium"`; sidebar blocks `categories` (`count: true`), `featured_products` (`widget_heading: "Sale products"`, `collection: "fashion-new"`), `iconbox` (`"FREE SHIPPING_ Free shipping for all US orders"`, `"SUPPORT 24/7_ We support 24 hours a da…"`), `gallery` (6 `ins*_2022-03-02.jpg` demo images), `social`. |
| `collection.filter-sidebar.json` | main-collection `product_des: "1"`, `col_dk: "3"`, `products_count: 9`, `sidebar_drawer: false`, `sidebar_size: "medium"`; blocks `categories`, `filter` (enabled → sidebar with facets). |
| `collection.full-width.json` | `section_layout: "full_width"`, `col_dk: "5"`, `products_count: 8`, `sidebar_size: "small"`, no blocks. |
| `collection.infinite.json` | `use_dynamic_source: true` heading; `pagination_type: "infinite"`, `enable_progressbar: true`, `products_count: 12`, `dis_pagination: 40`, `btn_style: "hdt-btn-outline"`. |
| `collection.left-sidebar.json` / `collection.right-sidebar.json` | `product_des: "1"`, `col_dk: "3"`, `products_count: 9`, `sidebar_position: "left"` / `"right"`, `sidebar_size: "medium"`, `dis_pagination: 40`/`60`; same demo sidebar blocks as drawer-sidebar (`featured_products.collection: "sale"`). |
| `collection.load-more.json` | `pagination_type: "load_more"`, `enable_progressbar: true`, `products_count: 12`, `size: "xl"`, `use_dynamic_source: true` heading. |
| `collection.progress-bar.json` | `pagination_type: "load_more"`, `enable_progressbar: true`, `products_count: 12`, `dis_pagination: 40`, `size: "m"`. |
| `collection.sub.json` | top-list-collections → main-heading (`text: "<h1>{{ collection.title }}</h1>"`) → `collections-list` carousel (`collection_des: "1"`, `layout_mode: "carousel"`, `col_dk: "4"`, demo blocks `new-arrival`, `bottom`, `dress`, `accessories` with `cl1*_400x_*.jpg` images) → main-collection (`product_des: "1"`, 12, `col_dk: "4"`, `links`) → `shipping` (3 `_shipping` blocks, `icon_themes: "none"`, `col_dk: "3"`, `use_border: true`). |
| `page.about-us.json` | main-heading (`image: shop-banner.jpg`, `"{{ page.title }}"` + second heading `"Follow your passion, and success will follow you"`) → `custom-section` (`layout: "12"`, 5 `_multi_column` blocks) → `our-team` (`col_dk: "3"`, `_ourteam_carousel`). Demo content. |
| `page.access-forbidden.json` | `[main] main-page` (no settings) → `[access_forbidden] custom-liquid` — English HTML: h1 "Access Forbidden", p "You must be at least 18 years old to access this website.", links "Return to Homepage" (`/`) and "Go Back" (`history.back()`), Tailwind-like `hdt-*` utility classes, red icon. |
| `page.brands.json` | main-heading (`bg-heading.jpg`, `"{{ page.title }}"` uppercase) → `main-page` **DISABLED** → `brands` (`source: "menu"`, `menu: "brands-page"`). |
| `page.faq-1.json` | main-heading (`bg-heading.jpg`, overlay 54, uppercase) → `main-page` (`color_scheme: "scheme-1"`, `pd: "60px,,60px,"`). |
| `page.faq-2.json` | main-heading → `main-page` DISABLED → `accordion` (`accordion_des: "1"`, `color_scheme: "scheme-1"`, 8 `_accordion-item` blocks, English Kalles demo Q&A: "Do I need to open an account in order to shop with you?", "How do I pay for my orders?" ("We accept payments via Paypal…"), etc.). |
| `page.store-locator.json` | main-heading (`text_size: "custom"`, 30px) → `main-page` DISABLED → `store-locator` (**Mapbox `access_token: "pk.eyJ1IjoibmhvY2xlb3BybyIs…"`**, `style_map: "streets"`, `zoom: 12`, `pitch: 60`, `color_marker: "#0ec1ae"`, 7 demo stores: "1471 P St NW" Washington DC, "2221 I Tokyo JP", "HaNoi", "1977 Taiwan", "16479 Shanghai", "336 DaNang", "9978 Seoul"). |
| `page.timeline.json` | main-heading (`shopping-cart-head.jpg`, overlay 57, `pd: "70px,,70px,"`) → `main-page` DISABLED → `timeline` (`line_style: "dashed"`, `color_scheme: S-e0b7`, 4 demo `timeline` blocks 1980/2000/2010/2025 "Inception and Brand Establishment"…). |
| `page.kids.json` | `heading` DISABLED, `main` DISABLED, `banners` (3 `_banner-item` → `shopify://collections/multi-brand-kids`, `fixed_height: 820`), `collections-list` (`collection_des: "5"`, 6 items "Baby Care/Boy Clothes/Girl Clothes/Toys/Shoes/Bags" with Amazon-style `71Rix6pr9L._SL1500.jpg` images), `featured-collection` (`_product-grid` `collection: "multi-brand-kids"`, `product_des: "3"`, `image_ratio: "adapt_image"`), `custom-section`, `tabs-collection` (tabs **"Best  Sellers"**, **"Top Rated"**, "Sales"), `custom-line`, `custom-section` (`color_scheme: S-c947`). |
| `page.men.json` | `heading`/`main` DISABLED, `banners` (2 → `shopify://collections/men`, `fixed_height: 950`), `collections-list` (`image_ratio: "asos"`, demo "New Arrivals/Suitcase/Shoes/Watches"), `featured-collection` (`_product-carousel` `collection: "multi-brand-men"`, `product_des: "3"`, `col_dk: "5"`), **`hero-image`** (`image: hero_image.jpg`; blocks `heading` **"HURRY UP!"**, `heading` "Special Deal", `text` "Offers You Can't Miss: These deals won't last forever", **`_countdown`** (`year: 2030`, `month: 4`, `day: 19`, `hour: 12`, `cd_style: "outline"`), `button` "Shop Now" → `shopify://collections/men`), `tabs-collection` (**"Best  Sellers"**, **"Top Rated"**, "Sales"), `lookbook-product` (3 `_pin_slide_product` pins: `vincen-sport-shoes`, `mens-t-shirt`, `combat-dop-arm-tank`, `pin_bg: "#fa3044"`), `custom-liquid` with empty `custom_liquid`. |
| `page.women.json` | `slideshow` (2 pexels slides, `autoplay: 7`, `fixed_height: 950`, `color_scheme: scheme-6707cc8e…` gold #d9ad5f), `collections-list-manual` (4 items new-arrival / multi-brand-women "Body Care" / accessories / …), `featured-collection` ×2 (`_product-grid` `collection: "multi-brand-women"`, `product_des: "3"`, `image_ratio: "asos"`), `lookbook-grid`, `image-gallery` (6 `Pic*.jpg`, `b_effect: "pervasive-circle"`), `shipping` DISABLED (4 `_shipping` with inline SVG icons). |
| `search.compare.json` | main-heading (dynamic, `_heading_brc`) → `main-compare` (`product_list` = 5+ demo handles `copy-of-caravus-classic-stethoscope`, `3-seater-sofa-w-chaiselong`, `3ce-body-whitening-shampoo`, `55-ku6470-6-series-uhd-crystal-colour-hdr-smart-tv`, …; blocks `basic`, `availability` "Availability", `vendor` "Vendor", `variants` "Color", `btn` "Return to shop" → `shopify://collections/all`). |
| `search.wishlist.json` | main-heading (`shopping-cart-head.jpg`, overlay 50, `"Wishlist"` / `"View your wishlist products"`) → `main-wishlist` (`product_des: "1"`, `image_ratio: "asos"`, `products_count: 12`, `col_dk: "4"`, **`color_scheme: "scheme-2"`** — this id does not exist in `settings_data.json` `color_schemes`, `btn` "Return to shop"). Note `settings.wishlist_mode: "0"` (wishlist off). |

### 1.3 Section groups rendered on every page (`layout/theme.liquid`)

- **`sections/header-group.json`** (`type: "header"`): one section `[header_inline_blocks_xePFbC] header-inline-blocks` — `header_layout: "logo_center"`, `header_fullwidth: true`, `header_height: 70`, `header_height_mb: 60`, `logo: "shopify://shop_images/ElmsNest_Logo_Night.png"`, `logo_width: 50`, `logo_mobile` same, `logo_width_mb: 50`, **`header_colors: "scheme-env2-night"`**, `main_menu: "main-menu"`, `lazy_mn: true`, `menu_position: "center"`, `menu_size: "base"`, `menu_weight: "normal"`, `text_transform: false`, `dropdown_arrow: false`, `spacing_nav: 30`, `open_event: "hover"`, `menu_type: "split"`, `header_menu_colors: "scheme-env2-night"`, `h_icon: "kalles"`, `hover_icon: "2"`, **`search_type: "icon"`**, `show_currency: false`, `show_language: false`, `enabled_user: false`, `enabled_wishlist: false`, **`enabled_cart: true`, `cart_des: "1"`**, `header_socials: false`, **`header_transparent: true`**, `logo_transparent` same PNG, `header_transparent_colors: "scheme-env2-night"`, `background_opacity_transparent: 0`, `header_space_top: 0`, **`sticky_type: "on_scroll_up"`**, `header_sticky_colors: "scheme-env2-night"`, `background_opacity_sticky: 0.9`, `sticky_glass: false`; block `[menu_mobile] _menu_mobile` — `only_click_icon: false`, `header_mobile_colors: "scheme-env2-night"`, `note_mobile: "<span class=\"hdt-flex hdt-flex-col hdt-text-base\" style=\"line-height:24px\"><span>צריכים עזרה?</span><span>אימייל: <b>info@elmsnest.com</b></span></span>"`.
- **`sections/footer-group.json`** (`type: "footer"`): `[footer_KwXhWr] footer` — `collapse: true`, `full_width: true`, `pd: "64px,,64px,"`, `pd_mb: "44px,,44px,"`, `colors_by_section: true`, **`color_scheme: "scheme-env2-night"`**; 4 `_footer-column` blocks all `alignment: "right"`, `color_scheme: "scheme-env2-night"`, `font_weight_text: "medium"`: `footer_logo` (`width_lg: 31`; children `_footer-image` ElmsNest_Logo_Night.png `image_w: 72`, `text` "שאלות על מוצר או על התאמה למקום? כתבו לנו: info@elmsnest.com"), `footer_collections` (`col_heading: "קולקציות"`, `width_lg: 23`; links `/collections/תאורת-שביל-סולארית` "שביל, עמוד וגינה", `/collections/solar-wall-lights` "תאורת קיר", `/collections/ספוטים-ופרוז-קטורים-סולאריים` "ספוטים ופרוז׳קטורים", `/collections/גרילנדות-ותאורה-דקורטיבית` "גרילנדות ודקורטיבי"), `footer_info` (`col_heading: "מידע"`; links `/pages/guide-garden-lighting`, `/pages/why-solar-lighting`, `/pages/מי-אנחנו`, `/pages/shipping-delivery`, `/pages/processing-time`, `/pages/help-faq`), `footer_contact` (`col_heading: "יצירת קשר"`; `/pages/contact-us` "עמוד יצירת קשר", `/pages/contact-us` "לשליחת תמונה של המקום"). `[footer_bottom] footer` — `full_width: false`, `pd: "18px,,18px,"`, `color_scheme: "scheme-env2-night"`; block `copyright` `alignment: "center"`, text `ElmsNest © 2026 · <a href="/#env2-terms">תנאי משלוח וביטול</a>`.
- **`sections/system-group.json`** (`type: "custom.system"`): `[cart-drawer] cart-drawer` — `colors_by_section: true`, `color_scheme: S-e0b7`, `show_free_shipping_bar: false`, `show_cart_note: false`, `show_gift_wrap: false`, `show_shipping_estimator: false`, `show_cart_discount: false`, `discount_error_message: "Enter a valid discount code"` (English), `show_cart_complementary: false`, `heading: "אולי יתאים לכם גם"`, `replaces_products_selected: true`, `limit: 5`; blocks `subtotal`, `tax`, `agree` DISABLED, `btn_cart` (`icon: "none"`), `btn_checkout` (`style_class: "hdt-btn-accent"`), `img_trust` DISABLED. Section markup: `<hdt-cart-drawer>` (cart-drawer.liquid l.29), line items via `snippets/item-cart.liquid` (l.59), `cart-complementary` (l.104), `shipping-calculator` (l.282). `[cookies_8EMjkp] cookies` **DISABLED**. `[back_top_RQdR9c] back_top` — `enable_hidden_mobile: false`, `enable_progress_bar: true`, `border_cl: "#d2cbc1"`, `border_cl_active: "#2b2118"`.
- **Cart type**: there is no `cart_type` key in `settings_schema.json`. The theme renders BOTH a global drawer (`cart-drawer` in system-group, above) and the full cart page (`templates/cart.json` → `main-cart`). Header `cart_des: "1"`. Which one an add-to-cart opens is decided by theme JS (not inspected).
- `sections/toolbar-mobile.liquid`, `sections/hdt_predictive-search.liquid`, `sections/ecom-predictive-search.liquid`, `sections/sidebar-product.liquid` (only as a DISABLED section in product.json) and `sections/main-product-tabs.liquid` are referenced by **no** dumped template/group/layout file (grep over the dump); they are dumped for reference only.

---

## 2. Custom sections and snippets — what each renders

Common facts for every `elms-pdp-*` and `elmsnest-*` file: markup is `dir="rtl"`; all CSS is logical-property; colours are consumed through the `--en-*` RGB-channel tokens defined in `snippets/css-variables.liquid` l.72–137 (`--en-surface-page: 247 240 230`, `--en-surface-card: 255 253 247`, `--en-surface-elevated: 255 253 247`, `--en-text-primary: 43 33 24`, `--en-text-secondary: 88 79 69`, `--en-text-muted: 114 105 96`, `--en-border-subtle: 210 203 193`, `--en-night: 18 16 14`, `--en-ink: 232 227 217`, `--en-amber: 217 173 95`, `--en-amber-soft: 242 215 159`, `--en-measured: 74 111 165`, `--en-focus-light: 43 33 24`, `--en-focus-dark: 247 240 230`, `--en-button-disabled-bg: 223 215 205`, `--en-radius-sm/-md/-lg: 0px`, `--en-radius-pill: 999px`, `--en-color-dark: 43 33 24`). No font-family is set in any custom file (they inherit the theme body font). Every `elms-pdp-*` section has `"class": "elms-pdp-section"`, an `enable` checkbox, and a `presets` entry.

### 2.1 `sections/elms-pdp-*` (10)

**`elms-pdp-trust-strip.liquid`** (14381 B, "v2"). `<section class="elms-pdp elms-trust elms-trust--{ground}">` → optional `<h2 class="elms-trust__title">` → `<ul class="elms-trust__list" role="list">` of ≤4 `<li class="elms-trust__item">` each = `elms-pdp-icon` (40–44 px) + `<span class="elms-trust__label">` + `<span class="elms-trust__note">`; hairline separators (`::before`), 2×2 under 900 px. Reads **no product data / metafields**. Settings: `enable`, `heading` (default blank), `ground` (`cream|card|night|custom`, default `cream`), `bg_color`, `text_color` (custom mode only), `padding_top`/`padding_bottom` (default 44). Block `item` (max 4): `icon` (11 options, default `truck`), `label` (default "משלוח לנקודת איסוף — חינם"), `note` (default "שליח עד הבית בתוספת תשלום"). Colours: `--en-surface-page|-card|-night`, `--en-text-primary|-secondary`, `--en-ink`, `--en-amber` (icons on night only), `--en-border-subtle`. Fabricated-claim patterns: none (comment l.13–14 "אין כאן נתון, דירוג או מלאי מומצא").

**`elms-pdp-problem-solution.liquid`** (10172 B). `<section class="elms-pdp elms-ps">` → `<h2 class="elms-ps__title">` → `.elms-ps__layout` grid: optional `.elms-ps__media` (`image_picker`, `image_tag` widths 400–1400, `aspect-ratio 4/3`, bg `rgb(var(--en-night))`) + `.elms-ps__copy` with two `.elms-ps__part` blocks (kicker span, h3, richtext) — solution part has an amber inline-start rule (`rgb(var(--en-amber))`, l.108). Reads no product data (comment l.8–11). Settings: `enable`, `heading` (default "החצר חשוכה — ואין שקע איפה שצריך אור"), `problem_kicker/title/text`, `solution_kicker/title/text`, `image`, `image_position` (`start|end`), `bg_color` #F7F0E6, `text_color` #2B2118, padding 56. Colours via inline `--elms-ps-bg/-fg/-fg-soft/-line` (`color_mix` 70/20). Claims: none.

**`elms-pdp-night-gallery.liquid`** (12954 B, "v2"). `<section class="elms-pdp elms-night elms-night--{ground}">` → header (kicker `p`, `h2`, sub `p`) → `<div class="elms-night__grid">` of `<figure class="elms-night__cell">` with `image_tag` (widths `400…1400`, `sizes '(max-width: 760px) 92vw, min(560px, 46vw)'`, square `object-fit: cover`) → note `p`. Source `auto` = `product.media | where: 'media_type','image'` with `offset: skip_first limit: max_images` (l.96); `manual` = `image` blocks (image + caption). Renders nothing if the resulting list is empty. Settings: `enable`, `kicker` (default "מקרוב"), `heading` ("ככה זה נראה בפועל"), `subheading`, `note` ("התמונות להמחשה…"), `source` (`auto|manual`, default `auto`), `skip_first` 0–4 (1), `max_images` 2–10 (6), `ground` (`night|cream`, default `night`), `columns` (`2|3`), padding 96. Colours: `--en-night/--en-ink/--en-amber` (night), `--en-surface-page/-card`, `--en-text-*`, `--en-border-subtle`. Claims: none.

**`elms-pdp-feature-grid.liquid`** (10894 B). `<section class="elms-pdp elms-feat">` → header → `<dl class="elms-feat__grid">` of `<div class="elms-feat__cell">` = icon + `<dt>` label + `<dd><bdi dir="auto">value</bdi>`; → note. Each `fact` block reads **`product.metafields.custom[block.settings.source] | metafield_text`** (l.28, l.62); `source` options: `ip_rating, charge_time_hours, light_hours, color_temp, sensor_range_m, sensor_angle_deg, material, dimensions, power_watt, lumens, battery, light_modes, custom` (manual `value`). Empty value → cell skipped; zero cells → section not rendered (l.36). Settings: `enable`, `heading` ("הנתונים שחשוב לבדוק"), `subheading`, `note`, `bg_color` #FFFDF7, `text_color` #2B2118, padding 56 (×0.7 on mobile). Colours via inline `--elms-feat-*` (`color_mix` 68/20). Claims: none (schema `value.info`: "אין להמציא נתון").

**`elms-pdp-installation.liquid`** (21576 B, "v2"). `<section class="elms-pdp elms-install elms-install--{ground}">` → header (kicker/h2/sub) → `.elms-install__layout[--media]`: optional `<figure class="elms-install__media">` (manual `image` or `product.media` image #`image_index`, l.37–50, `image_tag` widths 400–1200 square) + `<ol class="elms-install__list">` of `<li class="elms-install__step">` = big numeral (`<bdi dir="ltr">n</bdi>`, loop-derived), title span, text span, optional per-step thumb → note. Settings: `enable`, `kicker` ("שלב אחר שלב"), `heading` ("ההתקנה — ללא חוטים וללא חשמלאי"), `subheading`, `note`, `show_image` (true), `image`, `image_index` 1–8 (3), `image_caption`, `ground` (`cream|night|custom`, default `cream`), `bg_color`, `text_color`, padding 96 (CSS floor 64 px via `clamp(64px, 8vw, var(--elms-install-pt, 96px))`). Block `step` (max 6): `title`, `text`, `image`. Colours: numeral `color-mix(in srgb, rgb(var(--en-amber)) 62%, rgb(var(--en-text-primary)))` on cream, `--en-amber-soft` on night. Reads no metafields (comment l.16–17). Claims: none.

**`elms-pdp-comparison.liquid`** (28017 B, "v2"). `<section class="elms-pdp elms-compare elms-compare--{ground}">` → header (kicker/h2/sub) → `<div class="elms-compare__scroll" role="region" tabindex="0">` → real `<table class="elms-compare__table" role="table">`: `<thead>` with `criterion_label`, column A (`elms-pdp-icon` crest 44 px + `col_a_label`), column B; `<tbody>` one `<tr>` per `row` block: `<th scope="row">criterion</th>` + 2 `<td>` marks (`check|cross|partial|none` → glyph + word יש/אין/חלקי/לא צוין, plus merchant `text_a`/`text_b`) → note. Stacks to cards ≤760 px. No product data. Settings: `enable`, `kicker` ("השוואה"), `heading` ("מה ההבדל מול מנורה סולארית זולה"), `subheading`, `note`, `criterion_label` ("מה בודקים"), `col_a_label` ("ElmsNest"), `col_b_label` ("מנורה גנרית זולה"), `crest_icon` (`sun|bolt|shield|check`), `ground` (`night|cream|custom`, default `night`), `bg_color` #12100E, `text_color` #E8E3D9, padding 96. Block `row` (max 8): `criterion`, `mark_a` (default `check`), `text_a`, `mark_b` (default `partial`), `text_b` (default "משתנה"). Colours: amber hero column (`--en-amber`, `--en-amber-soft`) on night; on cream amber is not used (comment l.200–203). **Claim pattern (comparative, merchant-typed):** the preset/template rows compare "ElmsNest" against an unnamed "מנורה גנרית זולה"; row `cmp_notfit` marks the competitor `cross` "לרוב לא" (section preset l.783–792; `templates/product.elms-pdp.json` + `product.elmsnest.json` block `cmp_notfit`). No numeric/rating/count claims; comment l.19–23 documents the intent.

**`elms-pdp-reviews.liquid`** (9321 B). Renders only when ≥1 `review` block with non-blank `body` or ≥1 `@app` block exists (l.18–34); **in the dev theme neither template gives it any block → it outputs nothing.** Markup when active: `<section class="elms-pdp elms-reviews">` → h2 → `<ul class="elms-reviews__list" role="list">` of `<li>` = optional rating (`<span class="elms-reviews__stars" aria-hidden>` 5× `elms-pdp-icon name:'star'`, empty ones `.is-empty`, + text "`N` מתוך `5`", l.53–66) + `<blockquote class="elms-reviews__quote">` + byline (`author`, `meta`) → `@app` blocks → note. Settings: `enable`, `heading` ("מה לקוחות כתבו"), `note`, `bg_color` #F7F0E6, `text_color` #2B2118, padding 56. Block `review` (max 9 incl. `@app`): paragraph "יש להזין רק חוות דעת אמיתית…", `body`, `author`, `meta`, `rating` (`none|1..5`, default `none`). **Flag — star-rating + customer-quote pattern present in code** (`sections/elms-pdp-reviews.liquid` l.53–79) but inactive; no seeded review, no aggregate/"based on N reviews" (comment l.4–8).

**`elms-pdp-warranty-shipping.liquid`** (18795 B, "v2"). `<section class="elms-pdp elms-policy elms-policy--{ground}">` → header (kicker/h2/sub) → `<div class="elms-policy__grid">` of `<article class="elms-policy__panel">` = 44 px icon + amber hairline, `<h3>` title, richtext `body`, optional `<a class="elms-policy__link" href="{{ pages[link_page].url | default: routes.root_url }}">link_text</a>` (l.70) → note. A panel with blank `body` is skipped (l.24). Settings: `enable`, `kicker` ("לפני שמזמינים"), `heading` ("משלוח, ביטול והחזרות"), `subheading`, `note` (default blank), `ground` (`cream|night|custom`), `columns` (`2|3|4`, default `2`), `bg_color` #F7F0E6, `text_color` #2B2118, padding 96 (floor `clamp(64px, 8vw, 112px)`). Block `panel` (max 6): `icon` (`none|truck|clock|return|shield|chat|tool|check`), `title`, `body`, `link_page` (page handle), `link_text`. Preset includes an "אחריות" panel with `body: ""` (l.518–527) → never rendered. Claims: none active (comment l.10–13: no warranty text is authored by the theme).

**`elms-pdp-faq.liquid`** (18116 B, "v2"). Sources: **`product.metafields.custom.faq.value`** (JSON array of `{q, a}`, l.26, l.73–88) and/or `question` blocks (l.92–105), controlled by `source`. `<section class="elms-pdp elms-faq elms-faq--{ground}">` → header → `<div class="elms-faq__list">` of native `<details class="elms-faq__item"><summary class="elms-faq__summary" aria-expanded="false"><h3 class="elms-faq__q">…</h3><span class="elms-faq__mark">+/−</span></summary><div class="elms-faq__a">…</div></details>` → note; then `<script src="{{ 'elms-pdp-faq.js' | asset_url }}" defer>` (l.115). Settings: `enable`, `kicker` ("לפני שמזמינים"), `heading` ("שאלות ותשובות"), `subheading`, `source` (`metafield|blocks|both`, default `both`), `note`, `ground` (`cream|night`, default `cream`), padding 96 (CSS floor 64 px), legacy `bg_color`/`text_color` "(אינם בשימוש)". Block `question` (max 8): `question`, `answer` richtext. Colours: `--en-amber` for kicker rule / open edge / open mark; `--en-amber-soft` open background on cream. Claims: none.

**`elms-pdp-description.liquid`** (9675 B). If `product.description != blank`: `<section class="elms-pdp elms-desc elms-desc--{ground}" aria-label="{{ aria_label }}">` → `<div class="elms-desc__wrap">{{ product.description }}</div>` (l.16–27). The stylesheet re-styles merchant-authored classes inside the description (`.elms-sales`, `__hero`, `__kicker`, `__lead`, `__promise`, `__story`, `__benefits` 2-col grid, `__choice`, `__specs` on `--en-night` with amber bullets, `__cta`) — radius 0, no shadow. Settings: `enable`, `aria_label` ("תיאור המוצר"), `ground` (`cream|card`), padding 96. Claims: none in the section (whatever the product description itself says is product data).

### 2.2 `sections/elmsnest-*` (3 side-page sections)

**`elmsnest-product-guidance.liquid`** (25433 B, "v2"). `<section class="en-pdp-guide elms-pdp en-pdp-guide--{ground}">` → header (kicker/h2/sub) → `<div class="en-pdp-guide__grid">` (2 cols): up to 3 `<article class="en-pdp-guide__step">` (badge = numeral `01..03` in amber + 44 px `elms-pdp-icon`, `<h3>` title, `<p>` text; a step with blank title is skipped, l.63) + **`{% render 'elmsnest-pdp-not-fit', product: product %}`** as the 4th cell (l.89; reads `custom.not_fit_for`, styled here l.330–353) → `.en-pdp-guide__foot`: 3 `<details class="en-pdp-guide__fact">` (fact1 `open`, l.95) with chevron SVG, fact3 adds `<a href="{{ pages['contact-us'].url | default: routes.root_url }}">fact3_link</a>` (l.128) + `<aside class="en-pdp-guide__cta">` icon + `<a href="{{ pages['guide-garden-lighting'].url | default: routes.root_url }}">link_label</a>` (l.141). Padding fixed `clamp(64px, 8vw, 112px)` (not a setting). Settings (all defaulted in Hebrew): `enable`, `kicker` ("לפני שמזמינים"), `heading` ("בודקים שהמוצר מתאים למקום שלכם"), `subheading`, `step1_icon` (`sun`)/`step1_title` ("מיקום ושימוש")/`step1_text`, `step2_icon` (`bolt`)/"מקור חשמל"/text with `<bdi dir="ltr">USB</bdi>`, `step3_icon` (`ruler`)/"מידות והתקנה"/text, `link_icon` (`tool`), `link_label` ("איך בוחרים תאורה לגינה?"), `fact1_q` ("זמן אספקה") / `fact1_a` ("8–17 ימי עסקים: 1–3 … 7–14 … משלוח לנקודת איסוף — חינם; שליח עד הבית — 29.90 ₪"), `fact2_q` ("מידע טכני") / `fact2_a`, `fact3_q` ("צריכים עזרה לפני הקנייה?") / `fact3_a`, `fact3_link` ("לעמוד יצירת הקשר"), `ground` (`cream|night`). Colours: `--en-guide-*` mapped to `--en-surface-page/-card`, `--en-text-primary/-secondary/-muted`, `--en-amber`, `--en-border-subtle`, `--en-night`, `--en-ink`. Claims: none fabricated; shipping figures are the policy figures.

**`elmsnest-cart-guidance.liquid`** (2784 B). Static `<section class="en-cart-guide" dir="rtl">` → `<h2>` "לפני שממשיכים לתשלום" → 3 `<article>`: "זמן אספקה" (`8–17` ימי עסקים: `1–3` + `7–14`), "אפשרויות משלוח" ("האפשרויות והעלות יוצגו בתשלום…"), "בודקים את הפרטים" → `<p>` "חסר לכם פרט חשוב על מוצר? <a href="{{ pages['contact-us'].url | default: routes.root_url }}">פנו אלינו לפני ההזמנה</a>." No settings, no product data. CSS: bg `rgb(var(--en-surface-elevated))`, wrap `rgb(var(--en-surface-page))` + `--en-border-subtle` + `var(--en-radius-md)`, cards `--en-surface-card` / `--en-radius-sm`; also **global overrides** `body .hdt-main-cart__button-checkout{min-height:48px!important}`, `body .hdt-quantity__button{min-width:44px!important;min-height:44px!important}`, `body .hdt-newsletter__submit{min-height:44px!important}` (l.15). Claims: none.

**`elmsnest-content-page.liquid`** (37196 B). Keyed on **`page.handle`** (l.18–28): renders full hard-coded Hebrew HTML for exactly five handles — `guide-garden-lighting`, `why-solar-lighting`, `processing-time`, `מי-אנחנו`, `help-faq` — inside `<main class="en-doc" dir="rtl">`; for any other page it outputs **nothing** (the `{%- else -%}` branch at l.333 printing `{{ page.content }}` sits inside `{% if editorial_page %}` and is unreachable). When active it injects `<style>#shopify-section-{{ section.id }} ~ [id$="__heading"], … ~ [id$="__main"]{display:none!important}</style>` (l.32–35) to hide the sibling `heading` and `main` sections of `templates/page.json`. Structure per page: `<header class="en-doc__band">` (night ground; `מי-אנחנו` adds `images['pexels-taryn-elliott-4112233.jpg']` eager/`fetchpriority: high` + shade) with `<h1>` + `.en-doc__role`; `processing-time` adds a `<dl class="en-doc__plate">` 1–3 / 7–14 / 8–17 ימי עסקים; then `.en-doc__shell.en-doc__main` with `nav.en-doc__toc`, `section.en-doc__sec` blocks, `ol.en-doc__sched` ruled lists, `.en-doc__links` rows to the 4 collections (`collections['תאורת-שביל-סולארית']`, `['solar-wall-lights']`, `['ספוטים-ופרוז-קטורים-סולאריים']`, `['גרילנדות-ותאורה-דקורטיבית']`, each showing `products_count`), `.en-doc__grid2` panels, `.en-doc__faq` native `<details>` (help-faq), `.en-doc__cta` buttons; closing `<nav class="en-doc__foot">` (home / all products / `pages['guide-garden-lighting']` / `pages['contact-us']`). Links also to `shop.shipping_policy.url`, `shop.refund_policy.url`, `pages['why-solar-lighting']`. Figures stated: 1–3 / 7–14 / 8–17 business days, 29.90 ₪, 14 days, cancellation fee "5% … או 100 ₪ — הנמוך מביניהם" (l.319). Colours: `--en-surface-page`, `--en-text-primary/-secondary/-muted`, `--en-night`, `--en-ink`, `--en-amber` (plate values only), `--en-color-dark`; radius 0. No settings. Claims: no reviews/ratings/counts/countdowns; content is policy + guidance text.

### 2.3 `sections/elmsnest-home-*` (old v1 homepage, 5 — not used by any dumped template)

**`elmsnest-home-hero.liquid`** (20010 B). `<section class="en-hero">` → `<picture>`: if the picked images are `pexels-taryn-elliott-4112233.jpg` + `pexels-taryn-elliott-4112236_2.jpg` it swaps in `assets/elmsnest-hero-{desktop,mobile}-performance.webp` (l.5–16), else `image_tag` 1920 + mobile `<source>`; shade/glow/dusk layers (`--en-amber` radial glow, `mix-blend-mode: screen`, keyframe animations) → `<h1>` heading → **hard-coded `<dl class="en-plate">`: עמידות `IP65`, גוון אור `3000K`, שעות תאורה `8–10`, חיווט חשמל `0`** (l.46–63) + scope line "נתונים מתוך מפרטי מוצרים בחנות · המפרט המלא של כל דגם — בעמוד המוצר" (l.70) → richtext `text` → two CTAs (`#elmsnest-home-products`; `secondary_url | default: pages['guide-garden-lighting'].url | default: '#elmsnest-home-shop'`) → `<nav class="en-hero__places">` of the 4 collection handles that have `products_count > 0` (l.96–103). Settings: `image`, `image_mobile`, `heading` ("תאורת חוץ סולארית — מה מתאים ומה לא"), `text`, `primary_label` ("לקטלוג המלא"), `secondary_label` ("לא בטוחים? למדריך הבחירה"), `secondary_url`. Schema `class: "hdt-section section-allow-transparent"`. **Flag — generalised spec plate:** the four values are literals in Liquid, not read from any product (`sections/elmsnest-home-hero.liquid` l.49, l.53, l.57, l.61); comments l.41–44 and l.65–68 assert they are transcribed from product pages and that the catalogue also has 2700K / IP67 / wired models.

**`elmsnest-home-trust.liquid`** (10017 B, "terms strip"). `<section class="en-terms">` → 4 `.en-terms__cell` (label/value/note): משלוח ("חינם לנקודת איסוף" / "29.90 ₪ עד הבית"), זמן אספקה ("8–17 ימי עסקים" / code default l.26 "1–3 ימי טיפול ועוד 7–14 ימי משלוח · מוצרים עשויים להישלח ממחסנים מחוץ לישראל" — schema default l.147 is the shorter string), לפני ההזמנה (**if `settings.whatsapp_number` non-blank → `<a href="https://wa.me/{{ number }}">שאלו אותנו בוואטסאפ</a>`, else `<a href="/pages/contact-us">נבדוק התאמה למקום</a>`**, l.34–50), ביטול עסקה ("עד 14 ימים מקבלת המוצר" / "לפי חוק הגנת הצרכן · דמי ביטול עד 5% או 100 ₪, הנמוך מביניהם" + `<a href="{{ term_4_cancel_url | default: '/pages/contact-us' }}">שליחת הודעת ביטול</a>` + `/policies/refund-policy`). Settings: `term_1..4_label/value/note`, `term_3_value_wa`, `term_3_note_wa`, `term_4_cancel_label`, `term_4_cancel_url`. Colours: `--en-surface-page`, `--en-text-primary/-secondary`. Claims: none (comment l.5–10).

**`elmsnest-home-places.liquid`** (11814 B). `<section id="elmsnest-home-places" class="en-places">` (night ground) → header (`heading`, richtext `intro`) → `.en-places__table` of ≤4 `<a class="en-place">` per `place` block with `collection.products_count > 0`: name (`place_label | default: collection.title`) + count (`products_count` + `count_word`), job column (`job_label` + block `job`), not-for column (`not_label` + block `not_for`), arrow → optional guide button (`guide_url | default: pages['guide-garden-lighting'].url`). Settings: `heading` ("איפה צריך אור?"), `intro`, `job_label` ("מה האור צריך לעשות"), `not_label` ("לא מתאים כאשר"), `count_word` ("מוצרים"), `guide_label`, `guide_url`; block `place` (limit 4): `collection`, `place_label`, `job`, `not_for` (schema info: "חובה לצטט מעמוד מפורסם"). Preset blocks are empty. Claims: none.

**`elmsnest-home-shop.liquid`** (23411 B). `<section id="elmsnest-home-shop" class="en-sheet">` → header (`heading` "תאורה לכל פינה מחוץ לבית", `intro`) → `#elmsnest-home-products` chapters, one `<section class="en-chapter">` per `collection` block with `products_count > 0`: `<a class="en-chapter__band">` (collection image or first product image, `object-position: 50% {{ focal }}%`, title, `description`, `products_count` "מוצרים", "לקולקציה ←") + `.en-chapter__grid` of **`chapter.products limit: 3`** rendered via **`{% render 'elmsnest-home-product-card', product: product, category_label: product.metafields.custom.home_card_line.value %}`** (l.64) + mobile-only `.en-cell--more` link → "לכל המוצרים" link. Inline JS: IntersectionObserver lazy-swap of `[data-en-secondary-image]` on hover-capable devices, and `.en-motion-ready` / `.is-lit` band reveal (amber radial glow `::before`). Settings: `heading`, `intro`; block `collection` (limit 4): `collection`, `focal` 0–100 (40), `description` ("בחרו לפי המקום והשימוש."). Claims: none.

**`elmsnest-home-growth.liquid`** (14265 B, "closing spread"). `<section class="en-close">` → `.en-close__scene` (grid; image = `collections['גרילנדות-ותאורה-דקורטיבית'].image | default: …products.first.featured_image`; `<h2>` "לא רק להאיר. להפוך את החוץ למקום שנעים להיות בו."; lede; `<ol class="en-close__places">` 3 links → `collections['solar-wall-lights']`, `['תאורת-שביל-סולארית']`, `['גרילנדות-ותאורה-דקורטיבית']`; CTA "לתאורה דקורטיבית") → `<dl class="en-close__notes">` 4 hard-coded notes ("קולקציות ממוקדות", "הפרטים לפני ההזמנה" — "בכל עמוד מוצר כתבנו את מקור החשמל, האפשרויות ואופן ההתקנה", "התקנה ללא חיבור לחשמל" — "בתאורה הסולארית אין כבלים ואין צורך להזמין חשמלאי", "צריכים עזרה בבחירה?") → `.en-close__learn` two guide links (`pages['guide-garden-lighting']`, `pages['why-solar-lighting']` with quoted teaser "\"היא לא תמיד מחליפה תאורה חשמלית חזקה\"") → `.en-close__final` ("כל תאורת החוץ, במקום אחד." + "ואם מוצר לא מתאים למקום שלכם — נגיד את זה מראש." + buttons all-products / `pages['contact-us']`). No settings. Claims: no reviews/ratings/counts; the "בכל עמוד מוצר כתבנו…" line (l.63) is a store-wide statement.

### 2.4 Custom snippets

- **`snippets/elms-pdp-icon.liquid`** (4785 B): `case name` → 13 inline 24×24 SVGs (`truck` [gets class `elms-icon--flip`], `drop`, `return`, `chat`, `sun`, `tool`, `clock`, `shield`, `bolt`, `check`, `cross`, `star`, `ruler`), `stroke="currentColor"`, `aria-hidden="true" focusable="false"`. Used by comparison, feature-grid, reviews, trust-strip, warranty-shipping, product-guidance, compare-cell.
- **`snippets/elms-pdp-compare-cell.liquid`** (1625 B): `<span class="elms-compare__mark">` glyph + sr word for `mark` `check|cross|partial|none` and optional `text`. **Referenced by no dumped file** (comparison section inlines its own cells) — orphan.
- **`snippets/elmsnest-pdp-direct-answer.liquid`** (415 B): `<p class="en-direct-answer" dir="rtl">{{ product.metafields.custom.direct_answer | metafield_text | newline_to_br }}</p>`; nothing when blank. Rendered by block `liquid_en_direct_answer` in `product.elms-pdp.json` / `product.elmsnest.json`.
- **`snippets/elmsnest-pdp-price.liquid`** (5268 B): Kalles-compatible price DOM (`<hdt-compare-at-price class="hdt-compare-at-price">`, `<hdt-price class="hdt-price[ hdt-price-on-sale]">`, `<hdt-unit-price>` with `.hdt-money` spans) wrapped in `#en-pdp-price-{{ product.id }}`; sr label "מחיר" or `products.product.price.sale_price` only when `compare_at_price > price`; honours `settings.pick_mode` ("first_available"), `settings.hide_sold_out`, `settings.currency_code_enabled`; inline script sets the `form` attribute to `form.hdt-main-product-form`'s id and re-inserts the elements (l.80–110) when the product has variants. No claims.
- **`snippets/elmsnest-pdp-facts.liquid`** (927 B): `{{ 'elmsnest-pdp.css' | asset_url | stylesheet_tag }}` (sole loader of that CSS) + `<div class="en-facts">` two static lines: "המחיר כולל מס. משלוח לנקודת איסוף חינם; שליח עד הבית בתוספת 29.90 ₪." and "אספקה משוערת: 8–17 ימי עסקים. <a href="/policies/shipping-policy">למדיניות המשלוחים המלאה</a>".
- **`snippets/elmsnest-pdp-unit-price.liquid`** (2758 B): only for products with an option named exactly "כמות": one `<span class="en-unit-price__line" data-en-variant=id>` per variant ("`price` · `price/qty` ליחידה", integer-division on cents), the non-selected ones `hidden`; inline script toggles on the form's `variant:change` event.
- **`snippets/elmsnest-pdp-trust.liquid`** (745 B): `<div class="en-trust">` two static lines: "ניתן לבטל עסקה עד 14 יום מקבלת המוצר. <a href="/policies/refund-policy">למדיניות הביטולים</a>" and "שאלות לפני ההזמנה? <a href="mailto:info@elmsnest.com">info@elmsnest.com</a>". Comment l.4–6: no warranty wording / locality claim / phone number allowed.
- **`snippets/elmsnest-pdp-specs.liquid`** (4214 B): reads `product.metafields.custom.{battery, power_watt, lumens, charge_time_hours, light_hours, sensor_range_m, sensor_angle_deg, ip_rating, dimensions, material, color_temp, light_modes} | metafield_text`; if any is non-blank renders `<section class="en-specs">` → `<h2>` "מפרט טכני" → `<table class="en-specs__table">` one `<tr><th scope="row">label</th><td><bdi dir="auto">value</bdi></td></tr>` per filled field (12 max) → `<p class="en-specs__note">` "נתונים שאינם מפורסמים על ידי היצרן אינם מופיעים בעמוד". Styled in `elmsnest-pdp.css` (night ground, amber `td`).
- **`snippets/elmsnest-pdp-faq.liquid`** (3523 B): from `product.metafields.custom.faq` — if `type == 'json'` iterate `value` objects `{q, a}`; else parse `metafield_text` lines "שאלה | תשובה"; outputs `<section class="en-faq">` h2 "שאלות ותשובות" + `<details class="en-faq__item"><summary><h3 class="en-faq__q">` items and a `<script type="application/ld+json">` `FAQPage` with `Question`/`Answer` entries; nothing when 0 items.
- **`snippets/elmsnest-pdp-not-fit.liquid`** (663 B): `<aside class="en-pdp-guide__notfit"><h3>למי זה פחות מתאים</h3><p>{{ product.metafields.custom.not_fit_for | metafield_text | newline_to_br }}</p></aside>`; nothing when blank.
- **`snippets/elmsnest-product-schema.liquid`** (5520 B): JSON-LD `ProductGroup` mirroring Shopify `structured_data` (`@id` `/products/<handle>#product`, `brand`, `category` from `product.category.name | default: product.type`, `description`, `hasVariant[]` with `offers` price/`priceCurrency: cart.currency.iso_code`/availability/url/sku) plus `additionalProperty[]` from the 12 `custom.*` spec metafields (Hebrew labels), `offers.shippingDetails` (pickup 0.00 ILS, courier 29.90 ILS, handling 1–3 days, transit 7–14 days, `addressCountry: "IL"`) and `hasMerchantReturnPolicy` (14 days, IL). Comment l.18–19: **"Deliberately contains NO aggregateRating and NO review"**. Its render site is `snippets/structured-data.liquid` per its own comment (that snippet was not dumped; not verified).
- **`snippets/elmsnest-measurement-preview.liquid`** (1546 B): `placement: 'head'` → Google consent-mode default (all denied, `wait_for_update: 500`) + GTM loader for **`GTM-NCK2MKW2`**; `placement: 'body'` → GTM `<noscript>` iframe. **Not rendered anywhere in the dumped layout** — `layout/theme.liquid` l.22–36 (Arabic comment) explains it was removed on 2026-08-24 because the Google & YouTube app already injects the same container (double-counting `AW-17540075563` / `GT-PBSXTWDK` conversions).
- **`snippets/elmsnest-home-product-card.liquid`** (4743 B): `<article class="en-cell[ is-sold-out]">` → `<a class="en-cell__media">` with `product.featured_image` `image_tag` (widths 260–900, `sizes '(max-width: 749px) 78vw, (max-width: 1024px) 33vw, 25vw'`) + lazy secondary `product.images[1]` via `data-src`/`data-srcset` (1×1 GIF placeholder), flags `<b class="en-cell__flag--sale">מבצע</b>` when `compare_at_price_max > price` and `<b class="en-cell__flag--out">אזל מהמלאי</b>` when unavailable → `.en-cell__body`: optional `<p class="en-cell__category">{{ category_label }}</p>`, `<h3><a>title</a></h3>`, price (`price_varies` → single `<bdi dir="ltr">` "price–price_max", else single price; compare-at in `<s>`) → `<a class="en-cell__action">` "לרכישה" (available) / "לצפייה במוצר" (sold out) — a link to the product page, not an ATC. Claims: none.
- **`snippets/elmsnest-whatsapp.liquid`** (3020 B): reads `settings.whatsapp_number` (strips spaces, `-`, `+`, parentheses); **renders nothing when blank (current state)**. When set: fixed `<a class="en-wa" href="https://wa.me/{{ en_wa }}" target="_blank" rel="noopener" aria-label="שאלות לפני ההזמנה בוואטסאפ">` bottom-left (`bottom:18px; left:18px; z-index:60`), WhatsApp SVG + label "שאלות? וואטסאפ", `background:#25d366`, hover `#1ebe5b`, `border-radius:0`, `min-height:54px`; ≤640 px icon-only 54×54; hidden in print.

### 2.5 Custom assets

- **`assets/elmsnest-pdp.css`** (6729 B): loaded only by `elmsnest-pdp-facts.liquid`. Type ladder (h1 `clamp(26px, 3vw, 38px)/600` on `#block-product_title_ehRqkq .hdt-h_custom`; price `.hdt-product__price .hdt-price .hdt-money clamp(26px, 2vw, 28px)/600`; section titles `clamp(22px, 2vw, 24px)`); block-gap overrides on `.hdt-product-info__list > #block-{product_title_ehRqkq, liquid_en_direct_answer, liquid_en_price, liquid_en_facts, product_variant_picker_XNdArY, liquid_en_unit_price, product_buy_button_aPmAbD, liquid_en_trust, liquid_en_specs}`; `[id^="block-liquid_en_"]:not(:has(*)) {display:none}`; `.en-direct-answer`, `.en-facts`, `.en-unit-price__line`, `.en-trust__*`, `.en-specs` (bg `rgb(var(--en-night))`, `td` `rgb(var(--en-amber))` tabular), `.en-faq__*`; sticky ATC image `border-radius: 0`; disabled `.hdt-product-form__submit` / `.hdt-sticky-atc__submit` use `--en-button-disabled-*`; `.hdt-sticky-atc__qty-selector {display:none}`. Comment l.8–9: weights 400/600 only, 700 reserved.
- **`assets/elms-pdp-faq.js`** (1806 B): IIFE guarded by `window.__elmsPdpFaqBound`; mirrors `details.open` → `summary[aria-expanded]` for `.elms-faq__item` / `.elms-faq__summary`; document-level capture `toggle` listener, runs on `DOMContentLoaded` and `shopify:section:load`.

### 2.6 Kalles files that the side templates depend on (facts from the dump)

- `sections/main-collection.liquid` (85678 B): loads `hdt-main-pages.css`, `facets.css`, `hdt-widget.css`, `hdt-pagination.css`; column counts from `cart.attributes.products_items_per_row` else `col_mb/col_tb/col_dk`; `has_sidebar = section.blocks.size > 0` (l.27–39); toolbar = filter button + `render 'facets'` (if `enable_filtering`), `render 'layout-switch'` (if `enable_num_cols_selector`), `render 'sort-by-popover'` (if `enable_sorting`), `render 'active-filters'`; `paginate collection.products by section.settings.products_count`; card switch `case section.settings.product_des` `'1'..'10'` → `render 'card-product1'…'card-product10'` with `class: "hdt-pr-styleN"`, `image_ratio: section.settings.image_ratio`; when any column count is 0 → `card-product-list`; `render 'pagination'` with `pagination_type`; sidebar (`<aside class="hdt-main-sidebar">` + `<hdt-drawer>` + `render 'sidebar-collection'`) only when `has_sidebar`.
- `sections/main-search.liquid` (40782 B): same toolbar/card switch; `search.results` paginated; collections via `collection-item`, articles/pages via `pr_loop_item`.
- `snippets/card-product1.liquid` (6121 B): `<hdt-card-product class="hdt-card-product {{ class }}[ hdt-pr-sold_out]">` → `.hdt-card-product__media` = `render 'card-product-media'` (image with widths `165…940`, secondary media on hover if `settings.show_secondary_image` [schema default `true`]) + `render 'badges'` + wishlist/compare buttons (`settings.wishlist_mode: "0"`, `compare_mode: "0"` → none) + `countdown` **only if `card_product.metafields.theme.countdown.value`** (l.82) + quick-view (`settings.show_quick_view: false`) / `btn-ultra` (if `settings.show_ultra_btn`, schema default `true`, key absent from settings_data) + `size-list` (if `settings.size_position == '1'`, default) + `render 'scrolling_badge'` (l.93) → `.hdt-card-product__info` (`hdt-card-align-left`): rating only if `settings.show_rating` (`false`), vendor if `show_vendor`, `<h3><a class="hdt-card-product__title hdt-text-base hdt-font-medium hdt-line-clamp">`, `render 'card-price'` (`show_volume_note: true`), `render 'color-list'`.
- `sections/main-product.liquid` (19966 B): `{% content_for 'block', type: '_product_sidebar' %}`, `{% content_for 'block', type: '_product-medias' %}`, `{% content_for 'blocks' %}` (theme blocks = `blocks/_product-*.liquid`); sticky ATC renders `product-qty-selector`; `product-form-bundle2` for FBT; one block type `_group-product`.
- `sections/related-products.liquid` (26834 B): `<product-recommendations data-fetch-type="{{ display_type }}">`; `display_type '1'` uses `routes.product_recommendations_url?section_id=…&product_id=…`; cards via `render 'related_option'` (not dumped); `hdt-slider` carousel.
- `sections/main-cart.liquid` (54244 B): `<hdt-main-cart>` → `<cart-items-component>` → per line `render 'item-cart-page'` + `line-item-price`, `hdt-quantity-wrapp`; optional `cart-shipping-bar`, `hdt-cart-note`, `hdt-cart-discount`, `shipping-protection`, `hdt-shipping-calculator` + `hdt-country-province-selector`, `cart-checkbox` (`agree`), blocks `subtotal/tax/agree/btn_checkout/accelerated_btns/img_trust/payment-terms`.
- `sections/cart-drawer.liquid` (55975 B): `<hdt-cart-drawer>` → `render 'item-cart'`, `render 'cart-complementary'`, `render 'shipping-calculator'`, `<hdt-cart-action>`, blocks `subtotal/tax/agree/btn_cart/btn_checkout/img_trust`.
- `sections/main-heading.liquid` (7379 B): `use_dynamic_source` → collection `featured_image` / article `image` else `image`; `.hdt-heading-image` with `--bg-overlay: bg_overlay/100`; blocks `_heading_liquid` (Liquid text), `_heading_brc`, `_heading_search`, `_article_time`, `_article_category`.
- `sections/main-page.liquid` (2736 B): outputs `{{ page.content }}` when non-blank (l.12–14); settings `full_width`, `color_scheme`, `pd`.
- `sections/main-404.liquid` (2519 B): h1/h3/p with translations; `color_scheme` schema default `"background-1"` (template sets `scheme-1`).
- `sections/brc-nav-product.liquid` (8575 B): breadcrumb + prev/next product; prev/next resolve from `product.collections.first` **only when `settings.remove_collection_url == false`** (l.24–25) — settings_data has `remove_collection_url: true`.
- `sections/back_top.liquid` (6398 B), `toolbar-mobile.liquid` (20983 B; blocks `shop/search/account/wishlist/cart/compare/blog/home/nav/filter/sidebar/link`), `hdt_predictive-search.liquid` (13645 B; renders `pr_loop_item`, `collection_loop_item`, `article_loop_item`), `ecom-predictive-search.liquid` (10524 B), `sidebar-product.liquid` (60277 B), `main-product-tabs.liquid` (25402 B), `main-list-collections.liquid` (25513 B; `collection-item` + `pagination`), `contact-form.liquid` (12255 B), `main-login.liquid` (9823 B), `main-register.liquid` (8120 B), `recently-products.liquid` (37189 B; same `card-productN` switch): dumped verbatim, not further analysed.
- Snippets `card-price.liquid`, `card-product-media.liquid`, `item-cart.liquid`, `item-cart-page.liquid`, `product-variant-options.liquid`, `product-media.liquid`, `product-qty-selector.liquid`, `cart-shipping-bar.liquid`, `facets.liquid`, `css-variables.liquid`: dumped verbatim.

### 2.7 Fabricated-claim pattern audit (file → line)

Grep terms: review, rating, star, trusted, countdown, best seller, testimonial, customer quotes, "N+ customers", "verified", "only N left", "hurry", "people are viewing".

| File · line | Pattern | State |
|---|---|---|
| `sections/elms-pdp-reviews.liquid` l.53–66 (stars + "N מתוך 5"), l.69 (`<blockquote>` quote), l.72–78 (author/meta) | star rating + customer quote markup | **Inactive**: renders only from merchant `review` blocks / `@app`; no blocks in either product template; no seeded text; no aggregate. |
| `sections/elms-pdp-comparison.liquid` preset l.783–792; `templates/product.elms-pdp.json` & `product.elmsnest.json` block `cmp_notfit` (`mark_b: "cross"`, `text_b: "לרוב לא"`) and rows with `text_b: "משתנה"/"תלוי במוכר"/"לעיתים תרגום אוטומטי"` | comparative table vs. unnamed "מנורה גנרית זולה" | **Active** on both PDP templates (merchant-typed settings). |
| `sections/elmsnest-home-hero.liquid` l.49, 53, 57, 61 | hard-coded spec plate `IP65` / `3000K` / `8–10` / `0` presented as catalogue-wide figures | Section not in any dumped template (old v1 homepage). |
| `sections/elmsnest-home-growth.liquid` l.63–64, l.83 | store-wide statements ("בכל עמוד מוצר כתבנו…", "אין כבלים ואין צורך להזמין חשמלאי") and a quoted teaser | Old v1 homepage, unused. |
| `snippets/card-product1.liquid` l.82 (`countdown` from `metafields.theme.countdown`), l.93 (`scrolling_badge`), l.96 (`hdt-pr-card-rating` if `settings.show_rating`) | Kalles countdown / badge / rating hooks | `show_rating: false`; countdown only if the product carries `theme.countdown`. |
| `templates/page.men.json` `hero_image_rAaEAD` blocks (`"HURRY UP!"`, `"Special Deal"`, `"Offers You Can't Miss: These deals won't last forever"`, `_countdown` to 2030-04-19) and `tabs-collection` "Best  Sellers"/"Top Rated"; `templates/page.kids.json` `tabs-collection` "Best  Sellers"/"Top Rated" | urgency countdown + "best seller"/"top rated" labels | Kalles demo alternate page templates (assigned to no page as far as the theme files show). |
| `templates/collection.{drawer-sidebar,left-sidebar,right-sidebar}.json` `iconbox` block: "FREE SHIPPING_ Free shipping for all US orders", "SUPPORT 24/7_ …" | demo trust claims | alternate collection templates only. |
| `templates/cart.json` `img_trust` DISABLED; `system-group.json` cart-drawer `img_trust` DISABLED (visa,paypal,master,american_express,amazon) | payment-logo trust strip | disabled. |
| `snippets/elmsnest-product-schema.liquid` l.18–19 | explicitly **no** `aggregateRating` / `review` | — |
| `elms-pdp-trust-strip`, `problem-solution`, `night-gallery`, `feature-grid`, `installation`, `warranty-shipping`, `faq`, `description`, `elmsnest-product-guidance`, `elmsnest-cart-guidance`, `elmsnest-content-page`, `elmsnest-home-{places,shop,trust}`, all `elmsnest-pdp-*` snippets, `elmsnest-home-product-card`, `elmsnest-whatsapp` | — | No review/rating/star/trusted-by/countdown/best-seller/customer-quote patterns found. |

---

## 3. `config/settings_data.json` — `current` (not presets)

### 3.1 Colour schemes (`current.color_schemes`)

Each scheme carries `text, text2, background, background_gradient, background2, button, button_label, button_border, secondary_button_label, accent_color, line_border, overlay, pr_text, price, sale_price, input_primary, input_secondary`. Values:

| id | text | text2 | background | background2 | button / button_label | accent_color | line_border |
|---|---|---|---|---|---|---|---|
| `scheme-1` | `#2b2118` | `#584f45` | `#f7f0e6` | `#fffdf7` | `#2b2118` / `#f7f0e6` | `#2b2118` | `#d2cbc1` |
| `scheme-77e4ef58-56a9-4631-876a-12098ba7d57d` | `#f7f0e6` | `#dfd7cd` | `#2b2118` | `#21180f` | `#f7f0e6` / `#2b2118` | `#f7f0e6` | `#726960` |
| `scheme-e0b73555-c767-4eda-aa6d-4e44e10aefce` | `#2b2118` | `#584f45` | `#fffdf7` | `#f7f0e6` | `#2b2118` / `#f7f0e6` | `#2b2118` | `#d2cbc1` |
| `scheme-23cf8966-8e57-4acd-9bb3-bb85f222fbb9` | same as e0b7 (`#fffdf7` bg) | | | | | | |
| `scheme-fe88a2ec-47a1-478d-8953-54da0178e18c` | same as 77e4 (dark) | | | | | | |
| `scheme-6707cc8e-6558-49d9-8759-f7d934d9fe4f` | `#2b2118` | `#493521` | **`#d9ad5f`** | `#f2d79f` | `#2b2118` / `#f7f0e6` | `#2b2118` | `#80591f` |
| `scheme-c9476b69-abc7-454f-a946-2d5a9f5f3a37` | same as scheme-1 | | | | | | |
| `scheme-edd9a918-cf44-41ea-abd8-c8c15db59eed` | same as 77e4 (dark) | | | | | | |
| `scheme-bb64fce5-5f42-4f83-9a58-b663f6326109` | same as scheme-1 | | | | | | |
| `scheme-1f531d74-6002-43b8-b794-098b5f102899` | same as scheme-1 | | | | | | |
| `scheme-deb451b2-5215-4164-90e0-74b4a5ec1a57` | same as e0b7 | | | | | | |
| `scheme-env2-night` | `#f4eee3` | `#c9c4b8` | **`#020306`** | `#070b15` | **`#ffd394`** / `#1a1206` | `#ffd394` | `#1f1e1d` |

`scheme-env2-night` also: `button_border: "#ffd394"`, `secondary_button_label: "#f4eee3"`, `overlay: "#020306"`, `pr_text: "#f4eee3"`, `price: "#ffd394"`, `sale_price: "#ffd394"`, `input_primary: "#f4eee3"`, `input_secondary: "#c9c4b8"`. All schemes have `background_gradient: ""` and `overlay: "#2b2118"` except env2-night. Global: `color_scheme_body: "scheme-1"`, `color_scheme_dialog: "scheme-1"`, `color_focus: "#2b2118"`. There is **no `scheme-2`** (referenced by `search.wishlist.json` and by schema default `color_scheme_search: "scheme-2"`).

### 3.2 Typography

`library_font: "shopify"`, **`font_family_shopify_1: "assistant_n4"`, `font_family_shopify_2: "assistant_n4"`, `font_family_shopify_3: "assistant_n4"`** (schema defaults were `karla_n4` / `karla_n4` / `lora_i4`), `rules_google_font: ""`, `font_custom_heading: "--f_family_1"`, `font_custom_sheading: "--f_family_3"`, `font_base: "--f_family_1"`, `hd_fweight: 600`, `heading_scale: 100`, `heading_lh: 1.5`, `heading_spacing: 0`, `body_lh: 1.7`, `body_spacing: 0`, `h0_size: 64`, `h0_size_mb: 32`, `h1_size: 54`, `h2_size: 42`, `h3_size: 34`, `h4_size: 28`, `h5_size: 24`, `h5_size_mb: 18`, `h6_size: 16`, `h6_size_mb: 14`.

### 3.3 Layout / radius / buttons

`page_width: 1200`, `spacing_sections: 0`, `spacing_grid_horizontal: 30`, `spacing_grid_vertical: 30`, **`button_radius: 0`**, `input_radius: 0`, **`block_radius: 0`**, **`pr_card_radius: 0`**, **`pr_media_radius: 0`**, `collection_card_radius: 0`, `blog_card_radius: 0`, `thickness_btn: 2`, `padding_btn_1: 15`, `min_width_dt_btn_1: 160`, `min_width_btn_1: 160`, `min_height_btn_1: 25`, `layout_boxed` absent (default `false`).

### 3.4 Product card / badges / product page behaviour

`pr_card_alignment: "left"`, `pr_title_weight: "medium"`, `pr_price_weight: "normal"`, `show_quick_view: false`, `show_quick_add: true`, `show_qty: false`, `show_rating: false`, `hdt_show_countdown_timer: false`, `effect_secondary_image: "opacity"`, `minimal_products: true`, `hidden_badges: false`, `hidden_wishlist: false`, `hidden_compare: false`, `wishlist_mode: "0"`, `wis_atc_added: "2"`, `compare_mode: "0"`, `enable_compare_popup: false`, `price_varies_style: "1"`, `badge_shape: "circle"`, `label_onsale_style: "percentage"`, `use_new_badge: false`, `badge_sale: "#91212a"`, `badge_new: "#2b2118"`, `badge_soldout: "#f7f0e6"`, `badge_preorder: "#e8f3f8"`, `badge_custom: "#2b2118"`, `tooltip_bg: "#2b2118"`, `tooltip_text: "#f7f0e6"`, `color_success: "#175a38"`, `color_warning: "#604000"`, `color_error: "#91212a"`, `color_mode: "color"`, `color_type: "all"`, `image_source: "custom_img"`, `color_event: "click"`, `enable_color_limit: false`, `color_maximum: 0`, `color_action: "all"`, `size_mode: "size"`, `size_type: "all"`, `enable_size_limit: false`, `remove_collection_url: true`, `use_url_collection_vendor: true`, `use_url_collection_type: true`, `pick_mode: "first_available"`, `hide_sold_out: true`, `animations_reveal_on_scroll: true`. Keys absent (schema defaults apply): `pr_title_size` "base", `pr_price_size` "base", `uppercase_title` false, `hidden_quickview` false, `show_secondary_image` true, `show_ultra_btn` true, `size_position` "1".

### 3.5 Cart

`empty_cart_link: "shopify://collections"`, `free_ship_pr: ""`, `free_ship_pr_fast: ""`, `free_ship_pr_express: ""`, `gift_wrap_pr: ""`; `enable_shipping_protection` / `protect_product` absent (default false). Cart UI itself is configured in `system-group.json` (`cart-drawer`) and `templates/cart.json` (`main-cart`) — see §1.3.

### 3.6 Search

`predictive_search_enabled: true`, `predictive_search_show_vendor: true`, `predictive_search_show_price: true`, `search_fullwidth: false`, `products_suggest: []`; absent → defaults: `search_form_by_type` true, `predictive_search_product_des` "1", `color_scheme_search` "scheme-2", `search_quick_search` null. Header `search_type: "icon"`.

### 3.7 Currency / money

`currency_type: "1"`, `currency_code_enabled: false`, `flag_currency: true`, `round_currency: true`, `supported_currencies: "EUR - EUR\nUSD - USD\nGBP - GBP\nCAD - CAD\nEUR - GER"` (Kalles default list; header `show_currency: false`); shop-level format `{{amount}} ₪`, currency `ILS`.

### 3.8 `elms*` / `whatsapp` keys

- **`whatsapp_number`: not present in `current`** (grep count 0) → value `nil` → `snippets/elmsnest-whatsapp.liquid` and the WhatsApp branch of `elmsnest-home-trust.liquid` render nothing. The key is defined in `settings_schema.json` group `"ElmsNest"` (`type: "text"`, label "מספר וואטסאפ", info "בפורמט בינלאומי בלי + … כשהשדה ריק — כפתור הוואטסאפ ותא \"לפני ההזמנה\" בוואטסאפ לא מוצגים כלל.").
- No key beginning with `elms` exists in `current`. `social_whatsapp_link` (schema) is absent. `social_instagram_link: "https://www.instagram.com/elmsnest/"`, `social_tiktok_link: "https://www.tiktok.com/@elmsnest"`, all other `social_*_link: ""`.
- Other: `purchase_code: "81735458-438b-4c2c-8567-19f0c1054a04"`, `favicon: "shopify://shop_images/k-fav.jpg"`, `checkout_accent_color: "#2B2118"`, `checkout_button_color: "#2B2118"`, `sections: { "color-swatch": { "type": "color-swatch", "settings": { "code": "" } } }`, `content_for_index: []`, app blocks: `sense-rtl` accessibility + `sense-rtl` (`rtl: true`, `ignore_ltr: true`, `font: false`, `button_buy_now: true`, `custome_css: true`, `footer_banners: true`, `payment_banners: true`). `platform_customizations.custom_css`: `.hdt-card-product hdt-wishlist:not([action="add"]) svg {color: rgb(var(--en-error-text));}`, `.hdt-badge.hdt-badge__on-sale {background-color: rgb(var(--en-error-text));}`, `/* cache-refresh-2026-08-05 */`. `right_to_left` does not exist in the schema → theme.liquid falls back to `'enable-with-list'`.

---

## 4. `layout/theme.liquid` (11123 B) — what is rendered globally

- l.2–19: RTL detection — `config_rtl = settings.right_to_left | default: 'enable-with-list'`; with the list, `dir="rtl"` when `request.locale.iso_code` starts with one of `ae;ar;arc;bqi;ckb;dv;fa;glk;ha;he;kwh;ks;ku;mzn;nqo;pnb;ps;sd;ug;ur;yi` (Hebrew `he` included).
- l.21: `<html class="no-js hdt-pr-img__effect-{{ settings.effect_secondary_image }} hdt-badge__shape-{{ settings.badge_shape }} dir--{{ dir }}[ shopify-design-mode][ hdt-minimal-products][ hdt-hidden-pr-badges][ hdt-hidden-pr-quickview][ hdt-hidden-pr-wishlist][ hdt-hidden-pr-compare][ hdt-pr-uppercase_title][ hdt-layout-boxed]" lang="{{ request.locale.iso_code }}" dir="{{ dir }}">`.
- l.22–36: Arabic comment documenting that `elmsnest-measurement-preview` is intentionally NOT rendered (GTM `GTM-NCK2MKW2` double-loaded with the Google & YouTube app; `AW-17540075563`, `GT-PBSXTWDK` counted twice).
- l.38: `{%- capture ecom_header_content -%}{%- render 'ecom_header' -%}{%- endcapture -%}{%- unless ecom_header_content contains 'Liquid error' -%}{{ ecom_header_content }}{%- endunless -%}` (EComposer hook).
- l.39 / l.173: the whole `<head>` body is wrapped in `{%- unless template == 'product.only_config' and request.design_mode == false -%}`.
- l.43: `<meta name="theme-color" content="#020306">`; l.45–47: `{%- if request.page_type == 'search' or request.page_type == 'cart' -%}<meta name="robots" content="noindex,follow">{%- endif -%}`; canonical; favicon 32/48 from `settings.favicon`.
- l.53–59: `preconnect https://fonts.shopifycdn.com` when `library_font == "shopify"`; Google-font async embed when `'google'`.
- l.60–116: `{%- if request.page_type == 'index' -%}` — preloads the v2 hero photo: `all_products['stainless-steel-solar-path-light-ip65'].images[0]` as two `<link rel="preload" as="image" fetchpriority="high">` (mobile `media="(max-width: 900px)"` widths 1000/1400 capped by image width; desktop `media="(min-width: 901px)"` widths 900/1400/1800/2400) — comment says keep in sync with `templates/index.json → env2_hero.settings`.
- l.117–150: `{%- elsif request.page_type == 'product' -%}` — preloads `product.media.first.preview_image` only when `product.media.size > 1`, first media is an image, `product.metafields.theme.group_media_obj.value == blank`, and `selected_or_first_available_variant.featured_media` is that first media; `imagesizes="(min-width: 768px) 50vw, 100vw"`, widths `246…1946`.
- l.151–156: `<title>` = `page_title` + tag/page suffixes + ` – {{ shop.name }}` unless already contained; meta description.
- l.158–168: `render 'social-meta-tags'`, `render 'css-variables', is_rtl: is_rtl` (defines `--en-*` tokens and per-scheme `--color-*`), `render 'product-styles'` **unless** `request.page_type == 'index'`, `render 'js-variables'`, `render 'scripts'`, `settings.custom_html_head` if set; then `{{ content_for_header }}`.
- l.175: `<body class="hdt-page-type-{{ request.page_type }}">`.
- l.177: commented-out `{%- section 'product-card-configs' -%}`.
- l.178–182: unless `product.only_config` (outside editor): `render 's-dom-temp'`, skip link `#MainContent`, **`{%- sections 'header-group' -%}`**.
- l.184–189: `<main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">{{ content_for_layout }}{%- if template.suffix == 'only_config' -%}{%- section 'color-swatch' -%}{%- endif -%}</main>`.
- l.191–205: unless `product.only_config`: `<footer>{%- sections 'footer-group' -%}</footer>`, then `sections 'system-group'` (cart-drawer, cookies DISABLED, back_top), `render 'compare-offcanvas'` if `settings.enable_compare_popup` (false), `render 'system-bottom'`, `settings.custom_html_footer` if set, `render 'structured-data'`.
- **l.207: `{%- render 'elmsnest-whatsapp' -%}`** — rendered on every template, outside the `only_config` guard, immediately before the EComposer footer hook (l.208) and `</body>`; it outputs nothing while `settings.whatsapp_number` is blank.
- No other `elmsnest-*` render exists in the layout; the `elmsnest-measurement-preview` snippet is not referenced.

---

## 5. Not fetched / caveats

- Nothing requested failed to fetch. All 21 batched queries returned; every requested file was written (121 files).
- Skipped by instruction: `templates/index.json` (in repo already), `templates/article.post-with-*.json` ×3, `templates/blog.{masonry,sidebar-drawer,sidebar-left,sidebar-right}.json`, all `blocks/_product-*.liquid` (listed in §0.2 only).
- Not dumped because outside the requested list (exist in the theme): `snippets/related_option.liquid` (renders the related-products cards), `snippets/structured-data.liquid` (where `elmsnest-product-schema` is said to be rendered — not verified), `snippets/collection-item.liquid`, `snippets/sidebar-collection.liquid`, `snippets/layout-switch.liquid`, `snippets/sort-by-popover.liquid`, `snippets/active-filters.liquid`, `snippets/pagination.liquid`, `snippets/badges.liquid`, `snippets/scrolling_badge.liquid`, `snippets/countdown.liquid`, `snippets/system-bottom.liquid`, `snippets/scripts.liquid`, `snippets/search-form.liquid`, `sections/header-inline-blocks.liquid`, `sections/footer.liquid`, `sections/top-list-collections.liquid`, `sections/heading-article.liquid`, `sections/main-article.liquid`, `sections/main-blog.liquid`, `sections/blog-slider.liquid`, `sections/main-account.liquid`, `sections/main-addresses.liquid`, `sections/main-order.liquid`, `sections/main-activate-account.liquid`, `sections/main-reset-password.liquid`, `sections/main-compare.liquid`, `sections/main-wishlist.liquid`, the `elmsnest-v2-*` homepage sections/snippets, `sections/elmsnest-{collections,hero,places,products,terms}.liquid`, `layout/password.liquid`, `layout/ecom.liquid`, `config/settings_schema.json` (fetched to scratchpad only), `locales/*` (translation strings such as `templates.404.title` were not resolved), `assets/*` other than the two custom files.
- The JSON-size discrepancy for 5 editor-saved files is explained in the header; the dumped content is the API's serialisation, not the compact on-disk bytes.
- Behaviour that lives in theme JS (`global.min.js` etc.) — e.g. whether add-to-cart opens `<hdt-cart-drawer>` — was not inspected.
