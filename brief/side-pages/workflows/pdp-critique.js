export const meta = {
  name: 'pdp-critique',
  description: 'Four adversarial critics audit the deployed ElmsNest PDP from the real renders and by executing the buy flow, each writing its findings to a file; then a fix pass closes the blockers and majors',
  phases: [{ title: 'Critique', detail: '4 critics, file-backed findings' }, { title: 'Fix', detail: 'triage, fix, redeploy, verify, BUILD-REPORT' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/pdp`
const COMMON = `Repo ${ROOT}. Shopify DEV theme gid://shopify/OnlineStoreTheme/154726400174 — UNPUBLISHED; the MAIN theme is blocked; NEVER publish; never change a product, collection, page or metafield.
The new product page is BUILT and DEPLOYED: snippets/elmsnest-v2-{ground-product,pdp-image,pdp-variants,pdp-photo-cta,pdp-card,pdp-buybar}.liquid, sections/elmsnest-v2-pdp-{stage,fit,night,ledger,facts,terms,ask,related}.liquid and templates/product.elmsnest.json (all 27 products carry templateSuffix "elmsnest", so that file IS the live product template on this dev theme). Repo copies are in ${ROOT}/theme/.
Fresh JS-enabled renders of the three archetypes (mirrored AFTER the deploy):
- ${ROOT}/brief/inventory/pdp-multi/ — solar-crystal-ball-string-lights, 24 variants — desktop 8393 / mobile 8980 px
- ${ROOT}/brief/inventory/pdp-single/ — stainless-steel-solar-path-light-ip65, 1 variant — 8127 / 8695 px
- ${ROOT}/brief/inventory/pdp-wall/ — waterproof-led-wall-light-ip65-6w-12w, 8 variants, MAINS — 8245 / 8626 px
Each holds index.html (the real HTML) plus http-desktop.png, http-mobile.png, http-desktop-fold.png, http-mobile-fold.png. All three: 0 Liquid errors, no horizontal overflow, only the two known mirror JS errors. The cart drawer opens on add-to-cart and is night (${DIR}/build-preview/buyflow-mobile.png).
Read the specs you audit against: ${DIR}/WINNING-SPEC.md §4 (what each section must be), §6 (do-not list) and §8 (the lead's addendum); ${DIR}/BRIEF.md §4 (the eight-answer persuasion spine, in order) and §11; ${ROOT}/brief/WINNING-SPEC.md §3 (the homepage design system); ${ROOT}/brief/side-pages/core/REPORT.md §9 (known core bugs — do not report them as new).
Tooling: Chromium has no internet. Serve a mirror with python3 -m http.server from inside ${ROOT}/brief/inventory/<key>/ and drive it with Playwright (/tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/node_modules/playwright, chromium /opt/pw-browsers/chromium-1194/chrome-linux/chrome, --no-sandbox); stub /cart/add.js, /cart.js and /cart/update.js with page.route. Re-shoot with node ${ROOT}/brief/shot-http.js <dir>/index.html <dir>/http. Crop full-page PNGs into readable slices with PIL before reading them. Do not commit with git. Do not use gh.`
const FIND = { type: 'object', required: ['lens', 'file', 'verdict', 'counts', 'top'], properties: { lens: { type: 'string' }, file: { type: 'string' }, verdict: { type: 'string' }, wouldOwnerSayNineties: { type: 'boolean' }, counts: { type: 'object', properties: { blocker: { type: 'integer' }, major: { type: 'integer' }, minor: { type: 'integer' }, nit: { type: 'integer' } } }, top: { type: 'array', items: { type: 'object', required: ['id', 'severity', 'where', 'what', 'fix'], properties: { id: { type: 'string' }, severity: { type: 'string' }, where: { type: 'string' }, what: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }

const CRITICS = [
  ['shopper', 'Israeli mobile shopper', `You are an Israeli shopper on a phone who genuinely wants string lights for a terrace, and you are impatient and sceptical. EXECUTE the purchase at 390x844 with Playwright on the served mirror of pdp-multi: find the price, understand what 24 variants mean, choose 11 metres, add it to the cart, and see what happens. Then do the same on pdp-single (one variant) and pdp-wall (8 variants, mains). Report every moment you hesitated, everything you could not find without scrolling twice, every tap target under 44px (measure them), every place the page made you work, and whether you learned it might NOT suit you before being asked to pay. Screenshot each step you describe. Then say plainly: would you buy, and what would make you leave.`],
  ['typographer', 'Hebrew typographer', `You are a Hebrew typographer working for premium Israeli brands. Judge the type from the renders at 1440 and 390 for all three products: hierarchy and scale, line length, leading on display Hebrew, RTL correctness, <bdi> discipline on Latin tokens and prices (IP65, 6W, 3000K, USB, 89.90 ₪), the maqaf and geresh and quotation marks, ragged reading edges on end-aligned paragraphs, product titles never set in the serif, mixed-direction variant strings ("5 מ׳ / 20 נורות", "6W / 3000K"), numerals and tabular alignment in the ledger, and anything that reads as translated rather than written. Grep the mirrored HTML to confirm each <bdi> claim.`],
  ['qa', 'front-end QA engineer', `You are a front-end QA engineer. TEST, do not read. With Playwright on the served mirrors: (1) keyboard-only path to buying — tab order, visible focus, whether the ledger is a real control; (2) prefers-reduced-motion: reduce — every lamp lit, no transitions; (3) JavaScript disabled (browser context javaScriptEnabled:false) — every price visible, every ledger row a real <form method="post" action="/cart/add">, no black page, nothing hidden at opacity 0; (4) 320px width and 390x664 (iPhone small viewport) — no overflow, nothing clipped, the sticky buy bar not covering the last ledger row or the footer; (5) screen-reader semantics — one h1 per page, heading order, alt text, aria on the ledger and the fit switch. Also audit the code: every setting templates/product.elmsnest.json writes exists in the section's schema, presets present, {{ block.shopify_attributes }} on block roots, no Liquid inside {% stylesheet %}/{% javascript %}, no unprefixed selectors, and no console error beyond the two known mirror ones. Report each test as pass/fail with the evidence you produced.`],
]

phase('Critique')
const crits = await parallel(CRITICS.map(([key, name, role]) => () => agent(`${COMMON}

${role}

**Write your findings to ${DIR}/CRITIQUE-${key}.md AS YOU GO — before you return, and ideally in stages as you work, because this container gets restarted and an unwritten finding is a lost finding.** The file must contain: a one-paragraph verdict; a table of every finding (id ${key.toUpperCase()}-01…, severity blocker/major/minor/nit, where — file or section or screen, what, evidence you produced yourself with the path of the PNG you read or the measurement or the file:line, and the fix you propose); then a short "what is genuinely good" section (three items maximum, so the fixer knows what not to break). Severity: blocker = the owner would reject the page, or a buyer cannot buy; major = a spine answer fails, or a rule in WINNING-SPEC §6 / BRIEF §11 is broken, or an accessibility failure; minor; nit. Do not pad. Do not report the known core bugs from REPORT §9 as new.
Return: your lens, the file path, the verdict in one sentence, the counts per severity, and your top findings (up to 8, most severe first).`, { label: `critic:${key}`, phase: 'Critique', schema: FIND, effort: 'high' })))
const cok = crits.filter(Boolean)
log(`critique: ${cok.map(c => `${c.lens} ${c.counts.blocker || 0}B/${c.counts.major || 0}M`).join(' · ')}`)

phase('Fix')
const fix = await agent(`${COMMON}

You are the integrator, fix pass. Four critics audited the deployed PDP. Each wrote a file in ${DIR}: CRITIQUE-creative.md (written in an earlier run — 3 blockers, 5 majors), CRITIQUE-shopper.md, CRITIQUE-typographer.md, CRITIQUE-qa.md — ls ${DIR}/CRITIQUE-*.md and READ EVERY FILE THAT EXISTS, IN FULL; the summaries below are only an index.
${JSON.stringify(cok.map(c => ({ lens: c.lens, verdict: c.verdict, nineties: c.wouldOwnerSayNineties, counts: c.counts, top: c.top })), null, 1)}
Fix EVERY blocker and EVERY major, and every minor whose fix is one line. For each: reproduce it first (read the PNG, run the Playwright check, open the file), fix it in ${ROOT}/theme/, redeploy only the changed files with themeFilesUpsert (one file per call, GraphQL block string; a userErrors entry means it was NOT written), then re-mirror and re-shoot the affected product pages (python3 ${ROOT}/brief/mirror.py "https://elmsnest.com/products/<handle>?preview_theme_id=154726400174" ${ROOT}/brief/inventory/<key> && node ${ROOT}/brief/shot-http.js ${ROOT}/brief/inventory/<key>/index.html ${ROOT}/brief/inventory/<key>/http) and verify the fix with the same method the critic used. Run python3 ${ROOT}/brief/lint.py after any schema or template change.
Where a critic is wrong, say so with evidence rather than changing the page — but the burden is on you, and "it looks fine to me" is not evidence.
Write ${DIR}/BUILD-REPORT.md: what was built (files, sizes), the deploy log, a finding-by-finding table (id → severity → what changed → the evidence it is closed → or why it is deliberately left, with the round that will handle it), the three archetypes' final measurements (desktop/mobile height, buy action inside the 390 fold, section anchors present, Liquid errors), the buy-flow result, exactly what degrades without JavaScript, and the open items for the lead and for the owner.
Then look at the three folds and the three full pages yourself, and end the report with a plain answer to two questions: does this page sell, and does it belong to the same store as the homepage.
Return the structured result.`, { label: 'fixer', phase: 'Fix', schema: { type: 'object', required: ['deployed', 'pages', 'issues'], properties: { deployed: { type: 'array', items: { type: 'string' } }, pages: { type: 'array', items: { type: 'object', required: ['key', 'heightDesktop', 'heightMobile', 'liquidErrors'], properties: { key: { type: 'string' }, heightDesktop: { type: 'integer' }, heightMobile: { type: 'integer' }, liquidErrors: { type: 'integer' }, foldHasBuy: { type: 'boolean' }, note: { type: 'string' } } } }, closed: { type: 'array', items: { type: 'string' } }, left: { type: 'array', items: { type: 'string' } }, sells: { type: 'string' }, issues: { type: 'array', items: { type: 'string' } } } }, effort: 'high' })
return { critics: cok.map(c => ({ lens: c.lens, verdict: c.verdict, counts: c.counts, nineties: c.wouldOwnerSayNineties })), fix }
