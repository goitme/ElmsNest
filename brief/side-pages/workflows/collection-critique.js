export const meta = {
  name: 'collection-critique-round-2',
  description: 'Four adversarial critics audit the deployed collection page one at a time (writing findings to files), then a fix pass closes the blockers and majors',
  phases: [{ title: 'Critique', detail: '4 critics, sequential, file-backed findings' }, { title: 'Fix', detail: 'triage, fix, redeploy, BUILD-REPORT' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/collection`
const URLS = `The five URLs (mirror key → path):
- coll-decor → /collections/%D7%92%D7%A8%D7%99%D7%9C%D7%A0%D7%93%D7%95%D7%AA-%D7%95%D7%AA%D7%90%D7%95%D7%A8%D7%94-%D7%93%D7%A7%D7%95%D7%A8%D7%98%D7%99%D7%91%D7%99%D7%AA (7 products, 105 variants, every product a range)
- coll-path → /collections/%D7%AA%D7%90%D7%95%D7%A8%D7%AA-%D7%A9%D7%91%D7%99%D7%9C-%D7%A1%D7%95%D7%9C%D7%90%D7%A8%D7%99%D7%AA (8 products, 69.90–999.90, the 14x span)
- coll-wall → /collections/solar-wall-lights (6, narrow span, no shared unit)
- coll-spot → /collections/%D7%A1%D7%A4%D7%95%D7%98%D7%99%D7%9D-%D7%95%D7%A4%D7%A8%D7%95%D7%96-%D7%A7%D7%98%D7%95%D7%A8%D7%99%D7%9D-%D7%A1%D7%95%D7%9C%D7%90%D7%A8%D7%99%D7%99%D7%9D (6, almost every image is a marketing poster so the glyph branch fires)
- coll-all → /collections/all (27, mixed units)`
const COMMON = `Repo ${ROOT} (branch claude/design-sidebar-pages-3991tn). Shopify DEV theme gid://shopify/OnlineStoreTheme/154726400174 — UNPUBLISHED; the MAIN theme is blocked; NEVER publish; never change a product, collection, page or metafield.
The collection plumbing and all seven sections are WRITTEN in ${ROOT}/theme/ but NOT yet deployed and the template is NOT yet written: snippets/elmsnest-v2-{ground-collection,coll-axis,coll-rail,coll-paginate,coll-glyph}.liquid (new), snippets/elmsnest-v2-pdp-{card,image}.liquid (additively edited — the plumbing engineer proved 486 renders identical to the pre-edit versions, so the product page must not change), sections/elmsnest-v2-coll-{scene,ruler,bands,span,ledger,terms,goodnight}.liquid.
Read: ${DIR}/WINNING-SPEC.md (binding; §5 is the template and file plan), ${DIR}/build-preview/CONTRACT-COLL.md (the plumbing contract and its 14 risks), ${DIR}/BRIEF.md, ${ROOT}/brief/WINNING-SPEC.md §3, ${ROOT}/brief/side-pages/core/REPORT.md §9 (known core bugs — never report them as new), ${ROOT}/brief/DEPLOY.md (upsert mechanics), ${ROOT}/brief/side-pages/OWNER-NOTES.md.
${URLS}
Constraints: no sales (no badge, strikethrough or "-N%"), no WhatsApp label, no invented facts, no stars or counts; every primary CTA is a <button> in a form or sets its own colour (core REPORT §9.1); never use --en-warning-text/-success-/-error-/-info-; Latin tokens and prices in <bdi>; logical CSS properties only; radius 0 except pills; no three consecutive double-quotes in any file.
Tooling: Chromium has no internet. Mirror with python3 ${ROOT}/brief/mirror.py "https://elmsnest.com<path>?preview_theme_id=154726400174" ${ROOT}/brief/inventory/<key> then shoot with node ${ROOT}/brief/shot-http.js ${ROOT}/brief/inventory/<key>/index.html ${ROOT}/brief/inventory/<key>/http (serves the mirror so theme JS runs). For interaction, serve a mirror with python3 -m http.server and drive Playwright (/tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/node_modules/playwright, chromium /opt/pw-browsers/chromium-1194/chrome-linux/chrome, --no-sandbox); stub /cart/add.js and /cart.js with page.route. Crop full-page PNGs into readable slices with PIL before reading them. Do not commit with git. Do not use gh.
**This container is restarted every hour or two: write every artefact to disk as soon as it exists, never hold work in memory across steps, and if files from an interrupted earlier run are on disk, continue from them.**`
const INTEG = { type: 'object', required: ['deployed', 'pages', 'issues'], properties: { deployed: { type: 'array', items: { type: 'string' } }, pages: { type: 'array', items: { type: 'object', required: ['key', 'heightDesktop', 'heightMobile', 'liquidErrors'], properties: { key: { type: 'string' }, heightDesktop: { type: 'integer' }, heightMobile: { type: 'integer' }, liquidErrors: { type: 'integer' }, foldHasPriceAndBuy: { type: 'boolean' }, note: { type: 'string' } } } }, rulerNoJs: { type: 'string' }, pdpUnchanged: { type: 'string' }, issues: { type: 'array', items: { type: 'string' } } } }

// Integrate phase is DONE and deployed (run wf_1243caab-e40): 15 files on the dev theme, templates/collection.json
// written, all five URLs mirrored and shot at 13:17-13:18, no-JS ruler verified, PDP proved unchanged (MAD 0.000000).
const integ = {
  pages: [
    { key: 'coll-decor', heightDesktop: 10706, heightMobile: 12339, liquidErrors: 0, foldHasPriceAndBuy: true, note: '6 sections (span gated off). Ruler stops הכול/1.5/6/7/10/12 מ׳. 3 glyph plates. Buy bottom 790px desktop / 718px mobile.' },
    { key: 'coll-path', heightDesktop: 12940, heightMobile: 15273, liquidErrors: 0, foldHasPriceAndBuy: true, note: 'All 7 sections. Ruler stops הכול/1/2/4/6/8/12 נק׳. 6 glyph plates. A real <button> add-to-cart in the fold.' },
    { key: 'coll-wall', heightDesktop: 8743, heightMobile: 9718, liquidErrors: 0, foldHasPriceAndBuy: true, note: '5 sections — the ledger gated itself off because every wall lamp is measured in price, not metres; the spec headline says six, so this needs the lead\'s call. Ruler in PRICE mode. 2 glyph plates.' },
    { key: 'coll-spot', heightDesktop: 9978, heightMobile: 10867, liquidErrors: 0, foldHasPriceAndBuy: true, note: '6 sections. Ruler in price mode. 6 glyph plates — every spot product except dual-head.' },
    { key: 'coll-all', heightDesktop: 18836, heightMobile: 22305, liquidErrors: 0, foldHasPriceAndBuy: true, note: '6 sections; the ruler is the table of contents (27, then 17/6/4 by unit, then four place doors). No narrowing pills by design. 16 glyph plates.' },
  ],
  rulerNoJs: 'Verified in a javaScriptEnabled:false context (html class never gained env2-js): at 10 מ׳ rope 12 מ׳/99.90, crystal 11 מ׳/109.90, globe 10 מ׳/169.90, Edison dimmed printing האורך המרבי: 8 מ׳. Path quantity stops likewise.',
  issues: ['coll-wall ships five sections, not the six the spec headline names, because the ledger has no unit to rank — the lead must rule whether that is correct or a gap.', 'The lead noticed, unprompted, that the pinned-lamp tag on coll-decor mobile sits flush against the viewport edge — check it.'],
  deployed: ['15 files, see brief/side-pages/collection/build-preview/_deploy-log.txt'],
}
log('integrate replayed from the completed run; starting the critique')

phase('Critique')
const FIND = { type: 'object', required: ['lens', 'file', 'verdict', 'counts', 'top'], properties: { lens: { type: 'string' }, file: { type: 'string' }, verdict: { type: 'string' }, wouldOwnerSayNineties: { type: 'boolean' }, counts: { type: 'object', properties: { blocker: { type: 'integer' }, major: { type: 'integer' }, minor: { type: 'integer' }, nit: { type: 'integer' } } }, top: { type: 'array', items: { type: 'object', required: ['id', 'severity', 'where', 'what', 'fix'], properties: { id: { type: 'string' }, severity: { type: 'string' }, where: { type: 'string' }, what: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }
const CRITICS = [
  ['creative', `You are the creative director who rejected this store's first attempt as "from the nineties, everything repeated, trivial text, no creativity". REFUTE the claim that this collection page is world-class. From the renders only, all five URLs: is every screen composed differently or does a band pattern repeat; is the "one evening in one garden" idea actually visible in a still, or is it a grid with a photo on top; is there scale contrast; does the page get weaker as it descends; do the five URLs feel like five rooms of one house or five copies of one file; and does it belong to the same store as the finished product page (${ROOT}/brief/inventory/pdp-multi/http-desktop.png) and the homepage (${ROOT}/brief/inventory/home/http-desktop.png)? Name the weakest screen and what you would kill. Judge the 15 glyph plates honestly: do they read as a deliberate choice or as missing images?`],
  ['shopper', `You are an Israeli shopper on a phone who came from the homepage having chosen "מרפסת ופינת ישיבה". EXECUTE the journey at 390x844 with Playwright on the served mirrors: land on coll-decor, work out where you are, narrow the seven garlands to the one that fits a six-metre pergola, and get to a product page or into the cart. Then do the same on coll-path (where prices run 69.90 to 999.90 — does the page explain why?), on coll-spot (where almost every product shows an illustration instead of a photo — would you buy?), and on coll-all (27 products — is it navigable or a wall?). Report every hesitation, everything you could not find without scrolling twice, every tap target under 44px (measure them), and whether the ruler actually helped or was decoration. Screenshot each step. End with: would you buy, and what would make you leave.`],
  ['typographer', `You are a Hebrew typographer for premium Israeli brands. Judge the type on all five URLs at 1440 and 390: hierarchy and scale, the collection h1 over the photograph, line length, leading on display Hebrew, RTL correctness, <bdi> discipline on every price, range, metre and unit figure (grep the mirrored HTML to verify each claim — the ruler prints hundreds of numbers), the maqaf/geresh/gershayim, tabular alignment in the measure table and the price ladders, ragged reading edges, product titles never in the serif, and any string that reads translated rather than written. The parser prints numbers with units (5 מ׳, 780 LED, 6W, 3000K, ₪ למטר) — check every mixed-direction case.`],
  ['qa', `You are a front-end QA engineer. TEST, do not read, on the served mirrors: (1) the narrowing device with JavaScript DISABLED (context javaScriptEnabled:false) on decor and path — it is pure CSS :checked, so it must fully work; (2) keyboard-only: reach a stop, change it, reach a product, buy; visible focus throughout; (3) prefers-reduced-motion: reduce — every lamp lit, no transitions; (4) 320px and 390x664 — no overflow, nothing clipped; (5) screen-reader semantics: one h1 per URL, heading order, the radios as a real radiogroup with labels, alt text on the pinned products and the glyph plates; (6) the sort links work server-side (follow one and confirm the order changed) and pagination renders only when needed. Also audit the code: every setting templates/collection.json writes exists in the schema, presets present, block.shopify_attributes on block roots, no Liquid inside stylesheet/javascript blocks, no unprefixed selectors, and the CONTRACT-COLL.md risks (the 6.5 → 6_5 id transform, area-vs-length, "x contains y == false") are actually respected in the shipped sections. Report each test pass/fail with the evidence you produced.`],
]
const crits = []
for (const [key, role] of CRITICS) { const r = await agent(`${COMMON}

${role}

The page is deployed. Renders: ${JSON.stringify(integ.pages)}. No-JS ruler as the integrator left it: ${integ.rulerNoJs || 'not reported'}. Issues the integrator could not fix: ${JSON.stringify(integ.issues)}.
Audit against ${DIR}/WINNING-SPEC.md §4 (what each section must be) and §6 (the do-not list), and ${DIR}/BRIEF.md §6 (the bar) and §2 (the owner's decisions).
**Write your findings to ${DIR}/CRITIQUE-${key}.md AS YOU GO — before you return and in stages as you work, because this container gets restarted and an unwritten finding is a lost finding.** The file must contain: a one-paragraph verdict; a table of every finding (id ${key.toUpperCase()}-01…, severity blocker/major/minor/nit, where, what, the evidence YOU produced with the path of the PNG you read or the measurement, and the fix you propose); then "what is genuinely good" (three items maximum, so the fixer knows what not to break). Severity: blocker = the owner would reject it, or a visitor cannot find or buy a lamp; major = a rule in §6 / BRIEF §2 is broken, or the bar in BRIEF §6 fails, or an accessibility failure; minor; nit. Do not pad. Do not report the known core bugs from core REPORT §9 as new.
Return: your lens, the file path, the verdict in one sentence, the counts per severity, and your top findings (up to 8, most severe first).`, { label: `critic:${key}`, phase: 'Critique', schema: FIND, effort: 'high' }); if (r) crits.push(r); log(`${key} done (${crits.length}/4)`) }
const cok = crits.filter(Boolean)
log(`critique: ${cok.map(c => `${c.lens} ${c.counts.blocker || 0}B/${c.counts.major || 0}M`).join(' · ')}`)

phase('Fix')
const fix = await agent(`${COMMON}

You are the integrator, fix pass. Four critics audited the deployed collection page. Each wrote a file — ls ${DIR}/CRITIQUE-*.md and READ EVERY FILE THAT EXISTS, IN FULL; the summaries below are only an index.
${JSON.stringify(cok.map(c => ({ lens: c.lens, verdict: c.verdict, nineties: c.wouldOwnerSayNineties, counts: c.counts, top: c.top })), null, 1)}
Fix EVERY blocker and EVERY major, and every minor whose fix is one line. For each: reproduce it first, fix it in ${ROOT}/theme/, redeploy only the changed files, re-mirror and re-shoot the affected URLs, and verify with the same method the critic used. Run python3 ${ROOT}/brief/lint.py after any schema or template change. Re-prove the product page is untouched (PIL diff of pdp-multi's related row) after any edit to elmsnest-v2-pdp-card or -image.
Where a critic is wrong, say so with evidence rather than changing the page — the burden is on you, and "it looks fine to me" is not evidence.
Write ${DIR}/BUILD-REPORT.md: what was built, the deploy log, a finding-by-finding table (id → severity → what changed → the evidence it is closed → or why it is deliberately left and which round handles it), the five URLs' final measurements (heights, price and buy in the 390 fold, section anchors, Liquid errors), the no-JS ruler result, exactly what degrades without JavaScript, the glyph-plate count per collection, and the open items for the lead and the owner.
Then look at the five folds and the five full pages yourself and end the report with a plain answer to three questions: can a visitor find the right lamp, does the page sell, and does it belong to the same store as the homepage and the product page.
Return the structured result.`, { label: 'fixer', phase: 'Fix', schema: INTEG, effort: 'high' })
return { integ, critics: cok.map(c => ({ lens: c.lens, verdict: c.verdict, counts: c.counts, nineties: c.wouldOwnerSayNineties })), fix }
