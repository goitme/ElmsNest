export const meta = {
  name: 'home2-concepts',
  description: 'Home round 2: five divergent concepts for 2-3 image-led sections, each rendered as a mockup with the shortlisted photographs; five judges from the screenshots; ranked result for the lead\'s synthesis',
  phases: [
    { title: 'Concepts', detail: 'five designers, five angles, real images' },
    { title: 'Judge', detail: 'five judges score every concept from the shots' },
  ],
}
const REPO = '/home/user/ElmsNest', H = `${REPO}/brief/side-pages/home2`, IMG = `${H}/images`, SIM = `${REPO}/brief/side-pages/simplify`
const PW = 'playwright is at /tmp/claude-0/-home-user-ElmsNest/*/scratchpad/node_modules/playwright (resolve the glob), chromium executablePath /opt/pw-browsers/chromium-1194/chrome-linux/chrome; fonts: the offline pack is /home/user/ElmsNest/brief/assets/fonts (FrankRuhlLibre-*.woff2, Heebo-*.woff2) — declare @font-face from file:// paths so the shots use the real faces'
const COMMON = `Repo ${REPO}. You design ONE concept for the ElmsNest home page, round 2. Read first: ${H}/BRIEF.md (all of it — §2 constraints and §4 deliverable are binding), ${IMG}/SHORTLIST.md (the photographs you may use; ids map to files through ${IMG}/manifest.jsonl — own_* files are under ${IMG}/own/, the rest under ${IMG}/candidates/), and look at ${IMG}/contact-sheet.png. Then look at the page you are adding to: ${SIM}/verify-after/home-m-js-full.png (390 wide, the whole current home) and ${SIM}/verify-after/home-d-js-full.png — your sections sit between existing ones and must feel like the same page. Tokens: ${REPO}/theme/snippets/elmsnest-v2-core.liquid (read the :root block for the exact colours, type sizes, --env2-* names) and the skin ${REPO}/theme/snippets/elmsnest-s-skin.liquid for how stock parts are dressed.
Deliver in ${H}/concepts/<name>/ exactly per BRIEF §4: index.html (self-contained; dir="rtl" lang="he"; the 2–3 new sections in full with real copy and the real image files referenced by relative path ../../images/...; the neighbouring existing sections sketched as labelled grey bands of their true heights at 390 (hero 844, tiles 761, cards 781, fit 690, terms 397) so the rhythm is judged; say in a comment where each new section is inserted), shots/ (m-fold.png 390×844 at scroll 0 of the FIRST new section, m-full.png 390 full page, d-fold.png 1366×900 of the first new section, d-full.png), COPY.md, IMAGES.md, NOTES.md. ${PW}. Copy: Hebrew, claim-free, every sentence short; numbers/Latin inside Hebrew in <bdi dir="ltr">; no «+», no «;». Images: crop by CSS object-fit/object-position from the real files (no editing). Keep each section ≤ 675 px tall at 390 and say the measured heights in NOTES.md (measure with playwright: getBoundingClientRect of each section). No JS beyond what a stock theme has; no carousel, no parallax, no scroll-lamps. Do not touch anything outside your concept folder.`
const CONCEPT = { type: 'object', required: ['name', 'sections', 'heightsAt390', 'imagesUsed', 'sentences'], properties: { name: { type: 'string' }, sections: { type: 'array', items: { type: 'string' } }, heightsAt390: { type: 'array', items: { type: 'number' } }, imagesUsed: { type: 'array', items: { type: 'string' } }, sentences: { type: 'array', items: { type: 'string' } } } }
const ANGLES = [
  ['dusk', 'ANGLE «two frames»: the whole concept is built on ONE idea — day and night. One section shows the same kind of place by day (panel charging) and by night (lit), as two frames side by side or stacked, with three plain steps (charges by day · turns on by itself at dusk · no cable); a second, smaller section is the honest winter note (shorter days, less charge) on a moody image. Two sections only. Cinematic, quiet, big type on the images.'],
  ['questions', 'ANGLE «before you buy»: the guide\'s three questions (how much sun reaches the spot · how far must the light reach · indoors or out) as three photographic cards with one line each, leading to /pages/guide-garden-lighting — this section takes over the guide link from the fit block. Plus one short image-led section of your choice (not places, not products, not terms). Practical, warm, the store as the friend who says «ask this first».'],
  ['kinds', 'ANGLE «what light does what»: four small photographs of four KINDS of light — a pool of light on a path, a wash on a wall, a beam on a tree, a string over a table — one line each about the light itself (not the place, not the product), as a 2×2 mosaic with the photos doing the talking; a second section is the solar fact in one sentence over one wide image. Editorial, graphic, the photos cropped tight and consistent.'],
  ['band', 'ANGLE «the breath»: the page needs air, not more blocks. One full-bleed night photograph with a single sentence, placed between the product cards and the fit rows; one compact three-step solar fact section (tiny photos, mostly type) placed after the fit rows; and, if the shortlist has it, a slim day/night pair strip. Restraint: the fewest words on the page, the images carry it.'],
  ['mosaic', 'ANGLE «one section, one mosaic»: a single, larger image-led section that does everything the round asks — a mosaic of 5–6 photographs (mixed sizes, one big) with three short captions woven in (the solar fact · the winter note · the guide question) — plus a second tiny section only if needed. The mosaic is the creative move; typography sits inside the grid. Beautiful, but simple to read top to bottom on a phone (the grid collapses to one column of 2–3 images, the rest hidden by design, not by JS).'],
]
phase('Concepts')
const concepts = await parallel(ANGLES.map(([name, angle]) => () => agent(`${COMMON}\nYour concept folder: ${H}/concepts/${name}/. ${angle}\nReturn the section names, the measured heights at 390, the image ids used and every Hebrew sentence.`, { label: `concept:${name}`, phase: 'Concepts', schema: CONCEPT, effort: 'high' })))
const ok = concepts.filter(Boolean)
log(`concepts: ${ok.map(c => c.name).join(', ')}`)

phase('Judge')
const SCORE = { type: 'object', required: ['scores', 'verdict'], properties: { verdict: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['concept', 'score', 'best', 'worst'], properties: { concept: { type: 'string' }, score: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' } } } } } }
const JUDGES = [
  ['owner', 'You are the store owner. Your verdict of 2026-09-05 (read ${SIM}/SPEC.md §0 and §1) still stands: beautiful but SIMPLE for the customer, nothing twice, the night look, no invented product photos. You asked today for 2–3 image-led sections, «creative and beautiful», not many. Score what you would approve as the owner of this store.'],
  ['shopper', 'You are a first-time shopper in Israel on a 390×844 phone who wants garden lighting. Score: do the new sections help you decide or delay you? Do the photos make you want the garden, and do you understand what is being said in one read? Do they repeat something the page already told you?'],
  ['honesty', 'You are the brand guardian. Read ${H}/BRIEF.md §2 (Honesty, Images) and ${REPO}/brief/BRIEF.md §3. For every concept read COPY.md and IMAGES.md: any claim the store cannot back? any image that pretends to be the product? faces, foreign brands, text baked into photos? credits where the license needs them? Score 10 = nothing to fix; each violation costs points; a fabricated claim is a 2.'],
  ['designer', 'You are the design lead of a small studio known for restraint. Score craft from the shots: image treatment (crop, scale, veil, how type sits on photographs), rhythm with the existing sections (read the grey bands), typography on the night ground, the 390 and the 1366 renders, whether the creative move is one clear idea or decoration.'],
  ['engineer', 'You are the theme engineer. Score feasibility under BRIEF §2: sections ≤ 675 px at 390 (read NOTES.md heights), page stays ≤ 7.5 screens, stock patterns only, images as theme assets with width/height and lazy loading, schema-editable copy and images, RTL logical properties, P6 bdi, weight of the images. Penalise anything that needs JS or a Kalles change.'],
]
const judged = await parallel(JUDGES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role.replace(/\$\{SIM\}/g, SIM).replace(/\$\{H\}/g, H).replace(/\$\{REPO\}/g, REPO)}\nThe concepts are in ${H}/concepts/<name>/ for names: ${ok.map(c => c.name).join(', ')}. For EACH concept Read shots/m-fold.png, shots/m-full.png, shots/d-fold.png, then COPY.md, IMAGES.md, NOTES.md. Score each 0–10 with one sentence of best and one of worst, and a 4–6 sentence verdict naming a winner and what you would take from the others. Do not modify files.`, { label: `judge:${who}`, phase: 'Judge', schema: SCORE, effort: 'high' }).then(s => ({ who, ...s }))))
const jok = judged.filter(Boolean)
const weight = { owner: 1.5, shopper: 1.5 }
const totals = {}
for (const j of jok) for (const s of j.scores) { totals[s.concept] = (totals[s.concept] || 0) + s.score * (weight[j.who] || 1) }
const ranking = Object.entries(totals).sort((a, b) => b[1] - a[1])
log(`ranking: ${ranking.map(([c, t]) => `${c} ${t.toFixed(1)}`).join(' · ')}`)
return { concepts: ok, judges: jok, ranking }
