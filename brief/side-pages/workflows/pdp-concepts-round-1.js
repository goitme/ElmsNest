export const meta = {
  name: 'pdp-concepts-round-1',
  description: 'Five divergent PDP concepts as rendered offline mockups, judged from screenshots by five lenses (conversion 40%), synthesized into a build-ready spec for the owner checkpoint',
  phases: [{ title: 'Concepts', detail: '5 designers, 5 seeds, 3 mockups each' }, { title: 'Judges', detail: '5 lenses score from the PNGs' }, { title: 'Synthesis', detail: 'lead writes WINNING-SPEC + RULING' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/pdp`
const SEEDS = [
  { key: 'place', name: 'המקום קודם', seed: 'THE PLACE FIRST. Screen 1 is the buyer\'s place at night (the collection scene or the product\'s context photo), and the product is what lights it. The whole page is organised as the place\'s own questions (how long is the terrace / how dark is the path / how wide is the wall), the not-for device is a place picker that shows the ONE approved negative, and the variant ledger is expressed as coverage of the place (metres of string, units along a path, one or a pair on a wall). Sell by making the buyer see their own place lit.' },
  { key: 'ledger', name: 'הפנקס', seed: 'THE LEDGER. A typographic page: the variant matrix IS the hero — every length / quantity / wattage as a row on a hairline with an enormous numeral and its price in glow, metres and bulbs and hours as numerals, editorial Frank Ruhl Libre at wall scale, photographs as evidence beside the numbers. Motion: numerals light as they arrive; selecting a row lights its photo. Sell by radical clarity: 24 choices read as one table you can decide in ten seconds.' },
  { key: 'switch', name: 'המתג', seed: 'THE SWITCH. One stage you operate. For the string lights, choosing a length lights the bulbs along a real string one by one and the price counts up; for the wall light, 6W vs 12W changes the halo on the wall and 3000K/6000K changes its colour; for the path light, quantity adds bollards along the path. The not-for device is the same stage going dark (the approved negative appears when the lamp cannot light). Sell by letting the buyer play with the product\'s own light before buying.' },
  { key: 'dialogue', name: 'השיחה', seed: 'THE CONVERSATION. The page is a dialogue with the specialist: each of the eight buyer questions is a beat, set as the buyer\'s own words in large serif, answered in the brand voice in Heebo with the device that proves it (photo, ledger row, the four numbers). The buy box is always within reach (sticky / recurring compact form). The page ends with the smallest step ("שלחו תמונה של המקום"). Sell by handling every hesitation in the order it arises, like a good salesperson who also says no.' },
  { key: 'walk', name: 'הליכת לילה', seed: 'THE NIGHT WALK. Cinematic scroll: the product photographed from far, mid and close as three full-bleed night moments, almost no words, the buy box a scrim card that floats with you, the variant ledger a caption over the photograph, the not-for line the only sentence on a dark screen. Sell by atmosphere and restraint: the lamp is the argument; the words are captions.' },
]
const DESIGN_SCHEMA = { type: 'object', required: ['key', 'files', 'pngs', 'idea', 'spine'], properties: { key: { type: 'string' }, files: { type: 'array', items: { type: 'string' } }, pngs: { type: 'array', items: { type: 'string' } }, idea: { type: 'string' }, spine: { type: 'array', items: { type: 'string' } }, weakest: { type: 'string' }, heights: { type: 'string' } } }

phase('Concepts')
const designerPrompt = (s, i, mode) => `You are designer ${i + 1} of 5 on the ElmsNest product page (PDP) concept panel. Repo ${ROOT}. Read, in full and in this order: ${DIR}/BRIEF.md · ${ROOT}/brief/WINNING-SPEC.md §3 (design system, binding) · ${ROOT}/brief/build-preview/CONTRACT.md · ${ROOT}/brief/inventory/AUDIT-product.md (what you are replacing) · ${DIR}/products.json (your copy source: description_text, variants with prices, options) · ${ROOT}/brief/concepts/dusk/index.html (the winning homepage mockup — lift its tokens, lamp CSS/JS and buttons so your page is the same store). Look (Read) at ${ROOT}/brief/inventory/home/http-desktop-fold.png and http-mobile-fold.png — the level to match — and at ${ROOT}/brief/inventory/pdp-single/http-desktop-fold.png — what the owner rejected.

YOUR CREATIVE SEED (commit to it; the other four designers have radically different seeds): ${s.seed}
Concept key: "${s.key}" ("${s.name}").
${mode === 'finish' ? `RESUME MODE: a previous designer with your seed was interrupted. FIRST run \`ls ${DIR}/concepts/${s.key}/\` and see exactly which of index.html, path.html, wall.html, the shot-*.png sets and CRITIQUE.md already exist. Do NOT rebuild what exists: READ the existing HTML and its PNGs, keep the idea and the CSS/JS, and fix only what the PNGs show as broken (horizontal overflow, a buy action below the mobile fold, a missing spine answer, a BRIEF §11 violation). Then produce ONLY the missing artefacts (any missing HTML, any missing screenshots via node ${ROOT}/brief/shot.js, and CRITIQUE.md). Spend your effort on quality, not on redoing finished work.` : ''}
Build, offline, with real content only:
- ${DIR}/concepts/${s.key}/index.html — product A (solar-crystal-ball-string-lights, 24 variants), the complete page: all eight spine answers (BRIEF §4), the variant/price ledger device (prices by length from products.json — colour never changes price), the not-for device with the approved pair for מרפסת verbatim, night gallery lamps, the facts from the description bullets, the four numbers, the specialist line, the small step (mailto, never "בוואטסאפ"), the related module with the product card (BRIEF §7.8), a static Kalles-like header strip and a dark footer block. RTL, Hebrew, responsive 1440 and 390, vanilla JS for the device + lamps (IntersectionObserver; no-JS and reduced-motion = all lit), local assets only: images ../../../../assets/img/<handle>-<i>.jpg per the ledger in BRIEF §6, fonts ../../../../assets/fonts.css (Frank Ruhl Libre + Heebo are there).
- ${DIR}/concepts/${s.key}/path.html — product B (stainless-steel-solar-path-light-ip65, single variant 169.90): screen 1 + the not-for device (שביל pair) at minimum; no one-value picker.
- ${DIR}/concepts/${s.key}/wall.html — product C (waterproof-led-wall-light-ip65-6w-12w, 8 variants, MAINS — no solar sentence anywhere): screen 1 + the ledger for colour × W × K with the real prices.
Then screenshot each: node ${ROOT}/brief/shot.js ${DIR}/concepts/${s.key}/index.html ${DIR}/concepts/${s.key}/shot ; same for path.html → shot-path and wall.html → shot-wall (writes -desktop.png, -mobile.png, -desktop-fold.png, -mobile-fold.png). READ your PNGs (the Read tool renders images) — the mobile fold must show price + the buy action; fix and re-shoot until it does and until nothing overflows (shot.js prints horizontal-overflow and JS errors). Then write ${DIR}/concepts/${s.key}/CRITIQUE.md (BRIEF §12: the one idea; the spine answer by answer with the screen it lands on; where it sells hardest / weakest; one-more-day fixes).
Rules you will be judged against (BRIEF §11): no cream/brown, no boxes or equal-card grids, no comparison table, no reviews/urgency, no typed facts, no solar copy on C, no product title in the serif, no emoji, nothing fixed bottom-left, no four of anything in a row, no English UI strings. Every screen composed differently; one enormous thing per screen; lamps light on arrival; the product's light is the only light. The conversion judge carries 40 %: make the page SELL — buy inside the mobile fold, the not-for answer inside screen 2, 24 choices as one easy decision, risk lowered with the real numbers, a small step at the end.
Return: key, the three file paths, the PNG paths, the idea in one sentence, the spine map (8 strings: "#n → screen/device"), your weakest point, and the page heights printed by shot.js.`
const DONE = { place: true, ledger: true }
const FINISH = { switch: true, dialogue: true }
// state on disk 2026-09-02: switch has index/path/wall.html + index shots (needs shot-path, shot-wall, CRITIQUE);
// dialogue has index/path/wall.html + all shots (needs CRITIQUE only); walk is empty (full build).
const designs = await parallel(SEEDS.map((s, i) => () => {
  if (DONE[s.key]) return agent(`Repo ${ROOT}. Concept "${s.key}" is already complete on disk at ${DIR}/concepts/${s.key}/ (index.html, path.html, wall.html, CRITIQUE.md, shot-*.png). Do NOT redesign anything. Read CRITIQUE.md and index.html's first 80 lines, and Read shot-desktop-fold.png, shot-mobile-fold.png, shot-path-mobile-fold.png, shot-wall-mobile-fold.png to confirm the files render (if a PNG is missing, regenerate it with node ${ROOT}/brief/shot.js <html> <prefix>). Then return the structured record: key "${s.key}", the three file paths, the PNG paths (all that exist), the idea in one sentence (from CRITIQUE.md), the spine map (8 strings "#n → screen/device" from CRITIQUE.md), the weakest point (from CRITIQUE.md), and page heights (read from the PNG sizes: height/2 in CSS px).`, { label: `design:${s.key}:reuse`, phase: 'Concepts', schema: DESIGN_SCHEMA, effort: 'low' })
  return agent(designerPrompt(s, i, FINISH[s.key] ? 'finish' : 'new'), { label: `design:${s.key}`, phase: 'Concepts', schema: DESIGN_SCHEMA, effort: 'high' })
}))
const ok = designs.filter(Boolean)
log(`concepts ready: ${ok.map(d => d.key).join(', ')}${ok.length < 5 ? ` (missing ${5 - ok.length})` : ''}`)

phase('Judges')
const SCORE = { type: 'object', required: ['lens', 'scores', 'ranking', 'grafts', 'summary'], properties: { lens: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['key', 'sells', 'idea', 'composition', 'typography', 'honesty', 'feasibility', 'total', 'best', 'worst'], properties: { key: { type: 'string' }, sells: { type: 'number' }, idea: { type: 'number' }, composition: { type: 'number' }, typography: { type: 'number' }, honesty: { type: 'number' }, feasibility: { type: 'number' }, total: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' }, violations: { type: 'array', items: { type: 'string' } } } } }, ranking: { type: 'array', items: { type: 'string' } }, grafts: { type: 'array', items: { type: 'object', required: ['from', 'device', 'why'], properties: { from: { type: 'string' }, device: { type: 'string' }, why: { type: 'string' } } } }, summary: { type: 'string' } } }
const LENSES = [
  ['creative-director', 'You are a creative director who has rejected three agencies for templated e-commerce. Judge the idea, the composition (every screen different, scale contrast, full-bleed moments, no boxes) and the typography first; then the rest.'],
  ['conversion', 'You are a conversion strategist who runs Israeli DTC stores and buys from her phone. Walk each concept as a shopper on 390 px: where is the price, where is the buy action, when do I learn it will not suit me, how do I pick a length among 24 variants, what lowers my risk, what small step exists if I hesitate. Score "sells" hardest — it carries 40 % of the total — and be merciless about a buy action below the fold, a ledger that becomes rows of rectangles, or a page that is beautiful and does not answer the eight questions in order.'],
  ['hebrew-copy-chief', 'You are a Hebrew copy chief for premium Israeli brands. Judge whether the words sell: specificity, the brand voice (the specialist who also says no), headline craft, micro-copy on buttons and captions, <bdi> discipline on Latin tokens and prices, no English UI, no lorem, no filler. Score all criteria, with typography and sells weighted by copy quality.'],
  ['brand-honesty', 'You are the brand guardian. Check the approved pair verbatim and no other negative, no fabricated proof or urgency, no comparison claims, no solar copy on the mains wall light (wall.html), no typed facts beyond products.json, the four consumer numbers present, no "בוואטסאפ", no sale devices — and whether the page is unmistakably the same store as the homepage (ground, type, lamps, gold/glow). Every violation is listed by file and screen.'],
  ['liquid-feasibility', 'You are the Shopify/Kalles engineer who will build the winner. Judge feasibility: can the variant device be rendered from product.variants in Liquid with a no-JS ledger of per-variant /cart/add forms and JS enhancement; does the concept survive Kalles contracts or require a clean rebuild of the buy stack (estimate); image sources map to product.images[i]; sticky mobile bar; performance (first screen must not wait on JS); reduced-motion and RTL discipline (logical properties). Score feasibility hardest; score the rest as an engineer who also cares about selling.'],
]
const judgeInputs = ok.map(d => `- ${d.key}: idea "${d.idea}"; files ${d.files.join(', ')}; PNGs ${d.pngs.join(', ')}; heights ${d.heights || '?'}; designer's own weakest point: ${d.weakest || '-'}`).join('\n')
const judges = await parallel(LENSES.map(([lens, role]) => () => agent(`${role}
Repo ${ROOT}. Read ${DIR}/BRIEF.md (the brief; §4 spine, §11 do-not list, §13 criteria and weights) and ${ROOT}/brief/WINNING-SPEC.md §3 (the design system). The five concepts:
${judgeInputs}
Judge from the RENDERS first: for each concept Read shot-desktop-fold.png, shot-mobile-fold.png, shot-mobile.png (full mobile page), and the full desktop page cropped into readable slices (python3 - <<'EOF' … use PIL to slice ${DIR}/concepts/<key>/shot-desktop.png into 4–6 vertical slices at 50 % scale into /tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/judge-${'${lens}'}-<key>/ and Read them), plus shot-path-*-fold.png and shot-wall-*-fold.png. Only then read CRITIQUE.md and skim the HTML (for the ${lens === 'liquid-feasibility' ? 'feasibility judgement read the HTML/JS carefully' : 'copy and structure'}).
Score every concept 0–10 on each criterion (sells, idea, composition, typography, honesty, feasibility) and compute total = 0.40·sells + 0.15·idea + 0.15·composition + 0.10·typography + 0.10·honesty + 0.10·feasibility (0–10). List violations of BRIEF §11 per concept. Rank all five. Name up to 6 grafts: devices from non-winners worth carrying into the winner (from, device, why). Summary: 5–8 sentences, decisive, evidence from the screenshots (name the screen).`, { label: `judge:${lens}`, phase: 'Judges', schema: SCORE, effort: 'high' })))
const jok = judges.filter(Boolean)
const agg = {}
for (const j of jok) for (const s of j.scores) { const w = j.lens === 'conversion' ? 1.5 : 1; agg[s.key] = agg[s.key] || { sum: 0, w: 0 }; agg[s.key].sum += s.total * w; agg[s.key].w += w }
const table = Object.entries(agg).map(([k, v]) => ({ key: k, mean: +(v.sum / v.w).toFixed(2) })).sort((a, b) => b.mean - a.mean)
log(`judged: ${table.map(t => `${t.key} ${t.mean}`).join(' · ')}`)

phase('Synthesis')
const lead = await agent(`You are the design lead for the ElmsNest PDP. Repo ${ROOT}. Inputs: ${DIR}/BRIEF.md (binding), ${ROOT}/brief/WINNING-SPEC.md (the homepage spec — the MODEL of a build-ready spec: its §3 design system is binding here, its structure is what you must produce), ${ROOT}/brief/build-preview/CONTRACT.md, ${ROOT}/brief/inventory/AUDIT-product.md §"three hardest technical constraints", ${ROOT}/brief/side-pages/OWNER-NOTES.md.
The five concepts: ${JSON.stringify(ok.map(d => ({ key: d.key, idea: d.idea, files: d.files, pngs: d.pngs, spine: d.spine })))}.
The five judges (lens → scores, ranking, grafts, summary): ${JSON.stringify(jok).slice(0, 30000)}.
Weighted aggregate (conversion judge ×1.5): ${JSON.stringify(table)}.
Do this: (1) Look at every concept's renders yourself (folds + mobile full + desktop slices) before reading the judges' text; disagree with them where the screenshots say otherwise, and say so. (2) Choose the winner (the aggregate is advice, not a ruling; a page that does not sell on the phone cannot win). (3) Write ${DIR}/WINNING-SPEC.md, build-ready, in the homepage spec's structure: §1 the one idea and the ruling (why this concept, what the judges got right/wrong); §2 grafts table (device → where it lands); §3 global decisions for the PDP: the ground (sky or photo+sky), type sizes per role at 1440/390, motion rules, the Kalles strategy (restyle main-product keeping contracts vs rebuild the buy stack in our own section — decide, with the reason), the image ledger rule (index per product via schema/metafield, never index 0 of the never-use list), the power-source branch; §4 section-by-section build spec in page order — for each: purpose, exact Hebrew copy (headlines you write; product facts only from products.json/description bullets; the approved pair verbatim; the four numbers), desktop and mobile layout sketches, imagery, motion, schema settings/blocks, non-negotiables — covering: screen 1 + buy box, the variant/price ledger device (no-JS full ledger with per-variant forms; JS selector), the not-for device, the night gallery, the facts, the four numbers, the specialist line + small step (mailto until WhatsApp exists), the related module + the product card spec (this card becomes the catalogue card), the sticky mobile bar; §5 templates/product.json plan (which sections, in what order, what happens to the old elms-pdp-* sections and templates: retire), the description handling (.elms-sales HTML: restyled via CSS scoped to our section, or parsed — decide); §6 the do-not list; §7 references (which concept files/lines to lift CSS/JS from). Be exact: px, hex, filenames, schema ids. (4) Write ${DIR}/RULING.md — for the owner: 10–15 lines: the winner and why in plain words, the runner-up and what was taken from it, the two or three things that will look different from the mockup once built, and the one risk. Return: winner key, ranking, the two file paths, and the five one-line verdicts (one per concept) you would say to the owner.`, { label: 'lead:synthesis', phase: 'Synthesis', effort: 'high' })
return { designs: ok.map(d => ({ key: d.key, idea: d.idea, pngs: d.pngs, heights: d.heights })), table, judges: jok.map(j => ({ lens: j.lens, ranking: j.ranking, summary: j.summary })), lead }