export const meta = {
  name: 'pdp2-concepts',
  description: 'PDP round 2: five divergent concepts for 2-3 image-led sections on the product page (a sales page), each rendered for three archetype products with the store\'s own frames; five judges from the shots; ranked result for the lead\'s synthesis',
  phases: [
    { title: 'Concepts', detail: 'five designers, five angles, three archetype products each' },
    { title: 'Judge', detail: 'five judges score every concept from the shots' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pdp2`, IMG = `${REPO}/brief/side-pages/home2/images`, SIM = `${REPO}/brief/side-pages/simplify`
const PW = 'playwright is at /tmp/claude-0/-home-user-ElmsNest/*/scratchpad/node_modules/playwright (resolve the glob with fs.readdirSync), chromium executablePath /opt/pw-browsers/chromium-1194/chrome-linux/chrome with args [--no-sandbox]; fonts: the offline pack is /home/user/ElmsNest/brief/assets/fonts (FrankRuhlLibre-hebrew-*.woff2, FrankRuhlLibre-latin-*.woff2, Heebo-hebrew-*.woff2, Heebo-latin-*.woff2 — ls it) — declare @font-face from file:// paths in your index.html so the shots use the real faces'
const COMMON = `Repo ${REPO}. You design ONE concept for the ElmsNest PRODUCT page, round 2 — a sales page. Read first, in full: ${P}/BRIEF.md (§2 the sales-page tuning, §3 the constraints and the stricter image rule, §5 the deliverable are binding), ${P}/images/INVENTORY.md (every store-owned frame per product: clean / crop-only / unusable, and the solar gate per product), and look at ${P}/images/contact-sheet.png (the 41 usable frames). The frame files are ${IMG}/own/own_<handle cut to 40 chars>_<n>.jpg (n = gallery position; the id in INVENTORY.md's product heading is the full handle — the file name uses the first 40 characters, e.g. own_outdoor-bidirectional-led-wall-light-ip6_6.jpg; ls the folder). Then look at the page you are adding to: ${SIM}/verify-after/pdp-path-m-js-full.png (390 wide, the whole current product page of the stainless path light), pdp-rope-m-js-full.png, pdp-deck-m-js-full.png and pdp-path-d-js-full.png / pdp-rope-d-js-full.png (1366) — your sections sit between the buy box, the facts and the related grid and must feel like the same page. Read the two neighbours you sit between: ${REPO}/theme/sections/elmsnest-s-pdp-facts.liquid and ${REPO}/theme/sections/elmsnest-s-products.liquid, and ${REPO}/theme/snippets/elmsnest-s-place.liquid (the licensed place vocabulary and the solar gate). Tokens: ${REPO}/theme/snippets/elmsnest-v2-core.liquid (the :root block: exact colours, type sizes, --env2-* names); the house image treatment of the home round: ${REPO}/theme/sections/elmsnest-s-home-solar.liquid and elmsnest-s-home-band.liquid (scrims, crops, headline-on-photo).
Deliver in ${P}/concepts/<name>/ exactly per BRIEF §5: index.html (self-contained; dir="rtl" lang="he"; the 2–3 new sections rendered THREE times, once per archetype product A/B/C, each inside its PDP context — the existing sections as labelled grey bands of their true heights at 390 (BRIEF §5 gives them) — with real copy and the real frame files by relative path ../../../home2/images/own/...; a comment per archetype saying where each new section is inserted in the template order), shots/ (for each of A, B, C: <X>-m-fold.png = 390×844 with the FIRST new section at the top of the viewport, <X>-m-full.png = 390 full page of that archetype's block, <X>-d-fold.png = 1366×900 of the first new section, <X>-d-full.png; give each archetype its own id so you can scroll to it), COPY.md, IMAGES.md, NOTES.md. ${PW}. Copy: Hebrew, claim-free, every sentence short; digits/Latin inside Hebrew in <bdi dir="ltr">; no «+», no «;»; nothing the page already says (the kicker's «מתאים כדי…», the not-for line, the terms, the specs). Images: crop by CSS object-fit/object-position from the real files (no editing) but say in IMAGES.md where a baked pre-crop would be needed to get baked text out of the file. Honesty: a frame that shows another store product than the archetype's carries the «בתמונה: …» line (BRIEF §3). Each section ≤ 520 px at 390 and all new sections together ≤ 1500 px; measure with playwright (getBoundingClientRect per section per archetype) and write the numbers in NOTES.md. No JS beyond a stock theme; no carousel, no parallax, no scroll-lamps, no second buy button, no second price. Do not touch anything outside your concept folder.`
const CONCEPT = { type: 'object', required: ['name', 'sections', 'order', 'heightsAt390', 'imagesUsed', 'sentences', 'twice'], properties: { name: { type: 'string' }, sections: { type: 'array', items: { type: 'string' } }, order: { type: 'string' }, heightsAt390: { type: 'object', properties: { A: { type: 'array', items: { type: 'number' } }, B: { type: 'array', items: { type: 'number' } }, C: { type: 'array', items: { type: 'number' } } } }, imagesUsed: { type: 'array', items: { type: 'string' } }, sentences: { type: 'array', items: { type: 'string' } }, twice: { type: 'string' } } }
const ANGLES = [
  ['scene', 'ANGLE «see it at night»: the whole concept is ONE page-wide night photograph of this product in its place, right after the buy box, with one sentence — the desire the carousel hides behind a swipe; own frame when the product has one, the collection scene with the honest «בתמונה: …» line otherwise. A second, much smaller section for solar products only: the moment the light comes on («כשמחשיך»), as a detail — the panel in the sun beside the lit head — framed unlike the home diptych. Cinematic, quiet, big type on the photograph. Two sections.'],
  ['checks', 'ANGLE «before you order»: the three checks that prevent a return — how much sun reaches the spot · how far the light must reach · winter means less charge (solar only) — as three small photographic tiles with one question each and «למדריך לבחירת תאורה ←» to /pages/guide-garden-lighting; placed after the facts. Plus one scene band (own frame or collection scene, captioned) after the buy box. Practical, warm, the store as the friend who says «check this first». It asks, it never warns (the not-for line already warns).'],
  ['places', 'ANGLE «the same light, three places»: a triptych of the family\'s clean frames in three places (steps · hedge · entrance for a path light; patio · slate wall · terrace for a wall light; fence · table · ferns for decor; the spot family has one clean frame — design the degrade), one word or one short line each; the frames that show another product carry the «בתמונה: …» line. A second small section: the winter note for solar products, or nothing for mains. Editorial, graphic, tight consistent crops.'],
  ['dusk', 'ANGLE «what happens when it gets dark»: the main section is the dusk moment for solar products — a compact pair (a panel detail in daylight · the lit light at night, from the store\'s frames) with three short lines: charges by day, turns on by itself at dusk, less charge in winter; on mains / USB / battery products the section collapses to ONE line about the stated connection or to nothing (design both states and show them: A solar, B mains). A second section: one wide scene band with one sentence. Honest, explanatory, the sales argument is «no electrician, no cable» said once.'],
  ['one', 'ANGLE «one section, done»: a single larger image-led section that does everything the round asks and nothing more — one wide night photograph (own frame or collection scene, captioned) with a short heading and two or three lines inside or under it (what it looks like at night · what happens at dusk for solar · the guide link «למדריך לבחירת תאורה ←»), placed between the buy box and the facts. Fewer, better: ≤ 620 px at 390 in total. Restraint, the photograph carries it, typography sits on the night ground not on the image unless the crop is dark enough.'],
]
phase('Concepts')
const concepts = await parallel(ANGLES.map(([name, angle]) => () => agent(`${COMMON}\nYour concept folder: ${P}/concepts/${name}/. ${angle}\nReturn the section names, the template order you propose, the measured heights at 390 per archetype (A, B, C), the image ids used, every Hebrew sentence, and your honest one-line answer to «is any of this twice?».`, { label: `concept:${name}`, phase: 'Concepts', schema: CONCEPT, effort: 'high' })))
const ok = concepts.filter(Boolean)
log(`concepts: ${ok.map(c => c.name).join(', ')}`)

phase('Judge')
const SCORE = { type: 'object', required: ['scores', 'verdict'], properties: { verdict: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['concept', 'score', 'best', 'worst'], properties: { concept: { type: 'string' }, score: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' } } } } } }
const JUDGES = [
  ['owner', `You are the store owner. Your verdict of 2026-09-05 (read ${SIM}/SPEC.md §0 and §1) still stands: beautiful but SIMPLE for the customer, nothing twice, the night look, no invented product photos. Today you asked for the same 2–3 image-led sections on the PRODUCT page, «tuned harder because it is a sales page». Score what you would approve as the owner: does it sell without shouting, is it beautiful, is anything said twice, does it look like MY products and MY store.`],
  ['shopper', `You are a first-time shopper in Israel on a 390×844 phone, on this product page, about to decide. Score: do the new sections help you press the button or delay you? Do the photos make you want this light in YOUR garden, and do you understand in one read what is being said? Do they repeat something the page already told you (the kicker, the not-for line, the terms, the specs)? Would you scroll past them to reach «עוד לאותו מקום» or the footer?`],
  ['honesty', `You are the brand guardian. Read ${P}/BRIEF.md §3 (Honesty, Images — the PDP rule: a photograph on a product page is read as THIS product) and ${REPO}/brief/BRIEF.md §3. For every concept read COPY.md and IMAGES.md and look at the shots: any claim the store cannot back? any frame that shows another product without the «בתמונה: …» line? any baked text, badge, brand sign or claim number inside a frame? a solar sentence on a mains product (archetype B is MAINS)? a spec repeated? Score 10 = nothing to fix; each violation costs points; a photograph implying a product it does not show is a 2; a fabricated claim is a 2.`],
  ['designer', `You are the design lead of a small studio known for restraint. Score craft from the shots: image treatment (crop, scale, scrim, how type sits on photographs on the night ground), rhythm with the buy box above and the facts / related grid below (read the grey bands), typography, the 390 and the 1366 renders, whether the creative move is one clear idea or decoration, and whether archetype C (no clean frame) still looks designed rather than degraded.`],
  ['engineer', `You are the theme engineer. Score feasibility under ${P}/BRIEF.md §2–§3: sections ≤ 520 px at 390 and ≤ 1500 px together (read NOTES.md heights for A/B/C), the PDP stays ≤ 6 screens, stock patterns only, per-product images without any product data change (blocks with product + image_picker, or a Liquid handle map, plus a per-collection fallback), the solar gate as elmsnest-s-place does it, images as theme assets with width/height and lazy loading and sizes that fit the rendered width, ≤ 3 photographs per page, schema-editable copy and images, RTL logical properties, P6 bdi, the sticky bar's 78 px, weight. Penalise anything that needs JS, a Kalles change, a metafield or a tag.`],
]
const judged = await parallel(JUDGES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role}\nThe concepts are in ${P}/concepts/<name>/ for names: ${ok.map(c => c.name).join(', ')}. For EACH concept Read shots/A-m-fold.png, shots/A-m-full.png, shots/B-m-full.png, shots/C-m-full.png, shots/A-d-fold.png, then COPY.md, IMAGES.md, NOTES.md. Score each concept 0–10 with one sentence of best and one of worst, and a 4–6 sentence verdict naming a winner and what you would take from the others. Do not modify files.`, { label: `judge:${who}`, phase: 'Judge', schema: SCORE, effort: 'high' }).then(s => s && ({ who, ...s }))))
const jok = judged.filter(Boolean)
const weight = { owner: 1.5, shopper: 1.5 }
const totals = {}
for (const j of jok) for (const s of j.scores) { totals[s.concept] = (totals[s.concept] || 0) + s.score * (weight[j.who] || 1) }
const ranking = Object.entries(totals).sort((a, b) => b[1] - a[1])
log(`ranking: ${ranking.map(([c, t]) => `${c} ${t.toFixed(1)}`).join(' · ')}`)
return { concepts: ok, judges: jok, ranking }