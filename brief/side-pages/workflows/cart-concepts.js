export const meta = {
  name: 'cart-concepts-round-3',
  description: 'Five divergent cart-drawer + cart-page concepts as rendered offline mockups, judged from the screenshots by five lenses, synthesized into a build-ready spec',
  phases: [{ title: 'Concepts', detail: '5 designers, 5 seeds, 2 mockups each' }, { title: 'Judges', detail: '5 lenses score from the PNGs' }, { title: 'Synthesis', detail: 'lead writes WINNING-SPEC + RULING' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/cart`
const SEEDS = [
  { key: 'slip', name: 'הפתק', seed: 'THE SLIP. The drawer is not a table of rows — it is a short note the shop writes back to you, in the display serif, saying in words what you just chose: "שתי מנורות שביל מנירוסטה, באור צהוב חם — לשביל." The numbers sit under the sentence as a quiet ledger, not as columns. Proof that the right lamp is in the basket is delivered by LANGUAGE: the store repeats the choice back in a full Hebrew sentence built from the product type, the variant title and the quantity, so the buyer recognises their own decision rather than auditing a spreadsheet. The cart page is the same note, longer, on headed paper.' },
  { key: 'meters', name: 'המידה', seed: 'HOW MUCH LIGHT IS IN THE BASKET. The basket is measured in light, not in SKUs. The drawer opens with one derived line the buyer can check against their own garden — "12 מטר · 70 נורות · 3 מנורות" — and every item contributes a visible share of it, drawn on the same rail the collection page uses to ask "how much light does the place need?". Buying becomes assembling an amount of light; the cart is where you see how much you have assembled. Whatever cannot be measured (a wall light has no metres) is shown as itself and never guessed at. The cart page draws the full rail; the drawer draws its compressed form.' },
  { key: 'threestep', name: 'שלושה צעדים', seed: 'THREE STEPS TO PAID. The drawer is a countdown, not a container: three bands stacked in a fixed 324px column, and NOTHING in it scrolls at two or three items — what you chose / what it will cost you to receive it (the four approved terms compressed into one honest strip) / pay. The terms are the spine of the drawer, not a footnote: the buyer\'s two unanswered questions (what does delivery cost, can I cancel) are answered before the finger reaches the button, so the button is the only thing left to do. Checkout is the widest, tallest, brightest object on the screen and the secondary route to /cart is a text link, not a twin button. Ruthless, conversion-first, one screen.' },
  { key: 'window', name: 'החלון', seed: 'THE LIT WINDOW. The drawer\'s problem today is that a black panel opens over a black page and nothing separates them. So invert it: the cart is where the light comes on. The drawer is a raised, warmer surface — a lit window cut into the night page — and the page behind it genuinely darkens, so the drawer reads as the only lit thing on screen. Each line item is a small lit plate with its own soft falloff; the subtotal glows; the checkout button is the brightest point in the composition, the way a lamp is the brightest point in the store\'s photographs. Material and light do the work that layout cannot at 324px. The cart page is the same window at full size.' },
  { key: 'paydesk', name: 'הקופה', seed: 'THE PAY DESK. Invert the order: the drawer opens AT the checkout, not at the list. The top of the panel is the total and the button, sized as the decision; underneath, the basket is one collapsed honest line ("3 פריטים · 2 מנורות") that the buyer can open if they want to check, and the four approved terms sit as a single reassurance line under the button. A buyer who is sure pays in one tap without scrolling; a buyer who wants proof taps once to unfold the full list with names, variants and line totals. Proof is offered rather than imposed. The cart page is the opposite of the drawer: everything unfolded by default, because that is what it is for.' },
]
const DESIGN_SCHEMA = { type: 'object', required: ['key', 'files', 'pngs', 'idea'], properties: { key: { type: 'string' }, files: { type: 'array', items: { type: 'string' } }, pngs: { type: 'array', items: { type: 'string' } }, idea: { type: 'string' }, leaks: { type: 'string' }, weakest: { type: 'string' }, heights: { type: 'string' } } }

phase('Concepts')
const designs = await parallel(SEEDS.map((s, i) => () => agent(`You are designer ${i + 1} of 5 on the ElmsNest CART concept panel (round 3). Repo ${ROOT}. Read, in full and in this order: ${DIR}/BRIEF.md · ${DIR}/INVENTORY-DRAWER.md (what the drawer does today, measured, and the Kalles DOM contract that survives) · ${ROOT}/brief/WINNING-SPEC.md §3 (design system, binding) · ${ROOT}/brief/side-pages/collection/WINNING-SPEC.md §4.6 (the terms strip and its four approved numbers) · ${ROOT}/brief/side-pages/pdp/WINNING-SPEC.md §3.5-§3.6 (image ledger) · ${ROOT}/brief/side-pages/OWNER-NOTES.md.
Look (Read) at these renders before you design: ${DIR}/shots/drawer-full-mobile.png and drawer-full-desktop.png and drawer-empty-mobile.png (what you replace), ${DIR}/shots/cart-full-mobile.png, and ${ROOT}/brief/inventory/pdp-multi/http-mobile-fold.png plus ${ROOT}/brief/inventory/home/http-desktop.png (the level and the store you belong to).

YOUR CREATIVE SEED (commit to it; the other four are radically different): ${s.seed}
Concept key: "${s.key}" ("${s.name}").

**This container is restarted every hour or two, so work in a way that survives it: FIRST run \`ls ${DIR}/concepts/${s.key}/\` — if files are already there, a previous run of you was interrupted: READ them, keep their idea and CSS, and produce only what is missing. Then work strictly in this order, writing each artefact to disk and screenshotting it BEFORE starting the next: drawer.html -> shot -> cart.html -> shot-cart -> NOTE.md. Never hold work in memory across steps.**

Build offline, real content only, in ${DIR}/concepts/${s.key}/:
- drawer.html — the drawer OPEN over a dimmed page behind it (paint a plausible dark page behind: a couple of product cards and a heading is enough — do not design that page, it is context). Below a <hr>, the SAME drawer in its EMPTY state, again over a page. Panel width: 90vw capped, so it is 351px at 390 and 324px at 360; on desktop 1440 pick your own width and justify it in NOTE.md.
- cart.html — the full /cart page (header strip 70px + your cart + a dark footer block; do not design header/footer). Below a <hr>, the EMPTY cart page.
The real basket, exactly: «מנורת שביל סולארית מנירוסטה – תאורה אוטומטית IP65» / «צהוב חם» / x2 / 169.90 ₪ each (line 339.80 ₪), image ../../../../assets/img/stainless-steel-solar-path-light-ip65-0.jpg ; and «גרילנדת כדורי קריסטל סולארית – 20 עד 200 נורות» / «5 מ׳ / 20 נורות / צהוב» / x1 / 89.90 ₪, image ../../../../assets/img/solar-crystal-ball-string-lights-0.jpg . Subtotal 429.70 ₪. Both images are clean (not on the never-use list).
Each: RTL Hebrew, responsive 1440 and 390, the night ground and the type system, local assets only (fonts ../../../../assets/fonts.css), logical CSS properties only, radius 0 except pills, no letter-spacing on Hebrew, every control >=44px, <bdi> around number+unit.

Then screenshot: node ${ROOT}/brief/shot.js ${DIR}/concepts/${s.key}/drawer.html ${DIR}/concepts/${s.key}/shot ; then cart.html -> shot-cart. READ your PNGs (the Read tool renders images) and check the things the current drawer gets wrong: the product name must be readable and not truncated; a line total must be visible or provably unnecessary; the checkout control must be unmistakably dominant and >=44px; there must be no dead hole above the subtotal; the panel must separate from the page behind it. Fix and re-shoot until shot.js reports no horizontal overflow and no JS errors. Then write ${DIR}/concepts/${s.key}/NOTE.md per BRIEF §8.
Judged on (BRIEF §9): closing the three leaks 35, unmistakably this store 25, works at 324px and with JS off 20, honesty including what it refuses to claim 10, the empty state 10.
Rules: no progress bar to free shipping, no discount/note/gift/estimator field, no badge or strikethrough or "-N%", no stars or counts or urgency, no English UI, no cream/brown, product titles never in the serif, no invented facts, the four approved terms verbatim if used and never a fifth.
Return: key, the two files, the PNG paths, the idea in one sentence, how you close leak 1 / leak 2 / leak 3, your weakest point, and the heights shot.js printed.`, { label: `design:${s.key}`, phase: 'Concepts', schema: DESIGN_SCHEMA, effort: 'high' })))
const ok = designs.filter(Boolean)
log(`concepts: ${ok.map(d => d.key).join(', ')}`)

phase('Judges')
const SCORE = { type: 'object', required: ['lens', 'scores', 'ranking', 'grafts', 'summary'], properties: { lens: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['key', 'leaks', 'store', 'narrow', 'honesty', 'empty', 'total'], properties: { key: { type: 'string' }, leaks: { type: 'number' }, store: { type: 'number' }, narrow: { type: 'number' }, honesty: { type: 'number' }, empty: { type: 'number' }, total: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' }, violations: { type: 'array', items: { type: 'string' } } } } }, ranking: { type: 'array', items: { type: 'string' } }, grafts: { type: 'array', items: { type: 'object', required: ['from', 'device', 'why'], properties: { from: { type: 'string' }, device: { type: 'string' }, why: { type: 'string' } } } }, summary: { type: 'string' } } }
const LENSES = [
  ['shopper', 'You are an Israeli shopper on a 390x844 phone who has just tapped "הוספה לסל" on the product page and the drawer has opened. You have never bought from this store. Judge the three leaks hardest (35 of 100): can you tell at a glance that the RIGHT lamp and the RIGHT variant are in the basket; do you know what delivery costs and when it arrives without leaving the drawer; do you know you can cancel; and is it obvious which control pays? Count the taps. Say where your thumb goes and whether it lands on something 44px. Say honestly which of the five you would actually pay through.'],
  ['creative-director', 'You are the creative director who rejected this store\'s first attempt as "from the nineties, everything repeated". Judge from the renders first, and judge the hardest problem in this round: a 324px column is a TYPOGRAPHIC problem before it is a layout problem. Is there an idea only a lighting store could have, visible in a still? Does the drawer separate from the night page behind it? Does it belong to the same store as the finished product and collection pages, or is it a generic dark drawer? Is the empty state a real composition or a placeholder?'],
  ['conversion', 'You are a conversion specialist for a store with ONE test order, no reviews, no brand recognition and no ability to fabricate any of them. Judge purely on whether the design gets a decided buyer paid: the dominance and reachability of the checkout control, the number of competing controls, whether anything invites the buyer to leave the flow (a link to /cart, a link to a product, a cross-sell), whether the reassurance arrives before the decision rather than after, and whether anything creates NEW doubt. Flag every element that costs more attention than it returns. Be specific about the phone fold.'],
  ['brand-honesty', 'You are the brand guardian. Check: no free-shipping progress bar or any manufactured urgency, no badge/strikethrough/-N%, no stars, no review counts, no "trusted by", no invented facts, no fifth term; the four approved terms if used are verbatim; derived numbers (metres, bulbs, units) are actually derivable from variant titles and degrade honestly when a product has none; the empty state does not imply popularity or scarcity; and the whole thing is unmistakably the same store as the homepage, the PDP and the collection page.'],
  ['liquid-feasibility', 'You are the Shopify engineer who will build the winner inside sections/cart-drawer.liquid and sections/main-cart.liquid. The contract in ' + DIR + '/INVENTORY-DRAWER.md ("The contract a rebuild must keep") is binding: hdt-cart-drawer, dialog#CartDrawer, hdt-line-item, hdt-quantity-wrapp, updates[], wrapp-remove-item-oncart, button[name=checkout]. Judge: what survives that contract and what fights it; whether any derived number (metres, bulbs) can be computed in Liquid from variant.title without a metafield; whether the cart PAGE works with JS off (native form, updates[], /cart/change links); whether a collapsed/expanding device has a no-JS fallback; what breaks with 1 item, with 8 items, with a 30-variant product, and with an item that has an error_message; and how much of it is CSS the core already provides.'],
]
const judgeInputs = ok.map(d => `- ${d.key}: "${d.idea}" · leaks: ${d.leaks || '?'} · files ${d.files.join(', ')} · PNGs ${d.pngs.join(', ')} · heights ${d.heights || '?'} · designer's weakest point: ${d.weakest || '-'}`).join('\n')
const judges = await parallel(LENSES.map(([lens, role]) => () => agent(`${role}
Repo ${ROOT}. Read ${DIR}/BRIEF.md (§1 the three leaks, §6 the bar, §9 criteria and weights), ${DIR}/INVENTORY-DRAWER.md and ${ROOT}/brief/WINNING-SPEC.md §3.
The five concepts:
${judgeInputs}
Judge from the RENDERS first: for each concept Read shot-mobile.png and shot-desktop.png (drawer) and shot-cart-mobile.png and shot-cart-desktop.png. When a PNG is too tall to read, use PIL to cut it into 3-5 vertical slices at 60% into /tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/cart-${'${lens}'}-<key>/ and Read those. Only then read NOTE.md and the HTML.
Score each concept 0-10 per criterion and compute total = 0.35*leaks + 0.25*store + 0.20*narrow + 0.10*honesty + 0.10*empty, where "narrow" means "works at 324px and with JS off". List rule violations. Rank all five. Name up to 6 grafts worth carrying into the winner. Summary: 5-8 decisive sentences with evidence from the screenshots.`, { label: `judge:${lens}`, phase: 'Judges', schema: SCORE, effort: 'high' })))
const jok = judges.filter(Boolean)
const agg = {}
for (const j of jok) for (const s of j.scores) { const w = (j.lens && (j.lens.indexOf('shopper') === 0 || j.lens.indexOf('conversion') === 0)) ? 1.5 : 1; agg[s.key] = agg[s.key] || { sum: 0, w: 0 }; agg[s.key].sum += s.total * w; agg[s.key].w += w }
const table = Object.entries(agg).map(([k, v]) => ({ key: k, mean: +(v.sum / v.w).toFixed(2) })).sort((a, b) => b.mean - a.mean)
log(`judged: ${table.map(t => `${t.key} ${t.mean}`).join(' · ')}`)

phase('Synthesis')
const lead = await agent(`You are the design lead for the ElmsNest cart (round 3). Repo ${ROOT}. Inputs: ${DIR}/BRIEF.md (binding), ${DIR}/INVENTORY-DRAWER.md (binding contract), ${ROOT}/brief/side-pages/collection/WINNING-SPEC.md (the MODEL of a build-ready spec — match its structure and precision), ${ROOT}/brief/WINNING-SPEC.md §3, ${ROOT}/brief/side-pages/core/REPORT.md §9, ${ROOT}/brief/side-pages/OWNER-NOTES.md.
Concepts: ${JSON.stringify(ok.map(d => ({ key: d.key, idea: d.idea, leaks: d.leaks, files: d.files, pngs: d.pngs })))}
Judges: ${JSON.stringify(jok).slice(0, 30000)}
Weighted aggregate (shopper and conversion x1.5): ${JSON.stringify(table)}
(1) Look at every concept's renders yourself before reading the judges' text; disagree where the pixels say otherwise and say so explicitly. (2) Choose the winner — the aggregate is advice; a drawer that does not close leak 1 (proof the right lamp is in the basket) cannot win, and neither can one whose checkout is not unmistakably dominant. (3) Write ${DIR}/WINNING-SPEC.md, build-ready, in the collection spec's structure: §1 the idea and the ruling; §2 grafts and where each comes from; §3 global decisions (the drawer's surface and how it separates from the night page, type scale at 324px, the line-item anatomy, the derived-number rule and its honest degradation, motion, the terms compression, what the drawer refuses to show); §4 build spec surface by surface — DRAWER (full + empty) and CART PAGE (full + empty) — with exact Hebrew copy, mobile and desktop layouts, imagery, schema settings/blocks, and non-negotiables including the fold contract (checkout inside the fold at 360x640 on the cart page); §5 the files: sections/cart-drawer.liquid and sections/main-cart.liquid rewrites, snippets/item-cart.liquid and item-cart-page.liquid, templates/cart.json, what is retired from Kalles (main-heading band, elmsnest-cart-guidance boxes), and exactly which Kalles elements/attributes survive verbatim; §6 the do-not list; §7 references (which concept file and lines to lift from). Be exact: px, hex, filenames, schema ids, Liquid objects, and the Liquid needed to derive any number from variant.title. (4) Write ${DIR}/RULING.md in Hebrew for the owner: 10-15 lines — the winner and why in plain words, the runner-up and what was taken from it, two or three things that will look different once built, and the one risk.
Return: winner key, ranking, the two file paths, and one line per concept as you would say it to the owner.`, { label: 'lead:synthesis', phase: 'Synthesis', effort: 'high' })
return { designs: ok.map(d => ({ key: d.key, idea: d.idea, leaks: d.leaks, heights: d.heights })), table, judges: jok.map(j => ({ lens: j.lens, ranking: j.ranking, summary: j.summary })), lead }
