export const meta = {
  name: 'pdp3-concepts',
  description: 'PDP round 3: five divergent concepts for 2-3 MORE image-led sections on the product page in a DIFFERENT look (daylight, golden hour, detail, framed) with licensed internet photographs; rendered for three archetypes; five judges; ranked result for the lead\'s synthesis',
  phases: [
    { title: 'Concepts', detail: 'five designers, five angles, three archetype products each' },
    { title: 'Judge', detail: 'five judges score every concept from the shots' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pdp3`, IMG = `${P}/images`, H2 = `${REPO}/brief/side-pages/home2/images`, P2 = `${REPO}/brief/side-pages/pdp2`, SIM = `${REPO}/brief/side-pages/simplify`
const PW = 'playwright is at /tmp/claude-0/-home-user-ElmsNest/*/scratchpad/node_modules/playwright (resolve the glob with fs.readdirSync), chromium executablePath /opt/pw-browsers/chromium-1194/chrome-linux/chrome with args [--no-sandbox]; fonts: the offline pack is /home/user/ElmsNest/brief/assets/fonts (FrankRuhlLibre-hebrew-*.woff2, FrankRuhlLibre-latin-*.woff2, Heebo-hebrew-*.woff2, Heebo-latin-*.woff2 — ls it) — declare @font-face from file:// paths in your index.html so the shots use the real faces'
const COMMON = `Repo ${REPO}. You design ONE concept for the ElmsNest PRODUCT page, round 3. The owner asked (in Arabic) for MORE content on the product page: two or three more image sections, creative, «not the same look/colour» as what is there, and the photographs may come from the internet. Read first, in full: ${P}/BRIEF.md (§2 what «a different look» means, §3 the seeds, §4 the constraints — licence, honesty, the 7.5-screen cap, ≤ 520 px per section, ≤ 1500 px together, ≤ 3 photographs per page — and §5 the deliverable are binding). Then the photographs: ${IMG}/SHORTLIST.md (this round's licensed internet shortlist with roles, tones and credit lines) and look at ${IMG}/contact-sheet.png; the files are ${IMG}/candidates/<id>.<ext> (ls the folder; ids in ${IMG}/manifest.jsonl carry source, author, licence, page URL, width and height). The home round's pool is also available: ${H2}/SHORTLIST.md, files ${H2}/candidates/…, manifest ${H2}/manifest.jsonl. The store's own frames: ${P2}/images/INVENTORY.md and ${H2}/own/ (already used by the round-2 scene — new sections should bring NEW images). Then look hard at the page you are adding to, as it is deployed today: ${P2}/verify-after/pdp-path-m-js-full.png (390 wide: the stainless path light = archetype A, solar, own photo), pdp-wall-m-js-full.png (B, mains wall light), pdp-flood-m-js-full.png (C, solar floodlight, text-only scene), and pdp-path-d-js-full.png (1366). Your sections must feel like the same store and NOT repeat the device of the two round-2 sections (a full-bleed night photograph with words under it; a hairline row). Read the neighbours: ${REPO}/theme/sections/elmsnest-s-pdp-scene.liquid, elmsnest-s-pdp-dusk.liquid (the solar gate), elmsnest-s-pdp-facts.liquid, elmsnest-s-products.liquid, and ${REPO}/theme/snippets/elmsnest-s-place.liquid (place vocabulary). Tokens: ${REPO}/theme/snippets/elmsnest-v2-core.liquid (the :root block); the house image treatment of the home round: ${REPO}/theme/sections/elmsnest-s-home-solar.liquid and elmsnest-s-home-band.liquid.
Deliver in ${P}/concepts/<name>/ exactly per BRIEF §5: index.html (self-contained; dir="rtl" lang="he"; the 2–3 new sections rendered THREE times, once per archetype A/B/C, each inside its PDP context — the existing sections as labelled grey bands of their true heights at 390: A main-product 1085 / scene 596 / dusk 152 / facts 723 / related 781; B main 1085 / scene 596 / no dusk / facts 723 / related 781; C main 1085 / scene 135 / dusk 135 / facts 723 / related 781 — with real copy and the real photograph files by relative path ../../images/candidates/… or ../../../home2/images/candidates/…; a comment per archetype saying where each new section is inserted in the template order), shots/ (for each of A, B, C: <X>-m-fold.png = 390×844 with the FIRST new section at the top of the viewport, <X>-m-full.png = 390 full page of that archetype's block, <X>-d-fold.png = 1366×900 of the first new section, <X>-d-full.png; give each archetype its own id so you can scroll to it), COPY.md (every Hebrew sentence, per section, with the gate it lives under), IMAGES.md (per photograph: id, author, licence, the credit line as drawn or 'none', crop, role, and the sentence or heading that says what it is so it never reads as this product), NOTES.md (the device and why it is a different look, the order in the template, the measured heights at 390 per archetype, the gate per family, weight estimate, the honest «is any of this twice?»). ${PW}. Copy: Hebrew, claim-free (no hours, lumens, IP, «best», reviews, counts, urgency), every sentence short; digits/Latin inside Hebrew in <bdi dir="ltr">; no «+», no «;», no terminal period on labels; nothing the page already says (kicker, price, suits/not-for, the scene's place line, the dusk row's two sentences, the specs, terms, contact). Images: crop by CSS object-fit/object-position from the real files (no editing); credits for CC BY / BY-SA drawn as an 11–12 px muted caption inside the section; CC0/PDM need none; refuse faces, brand marks, baked text. Honesty: an internet photograph is NEVER presented as this product — a caption or heading names what it is. Measure each section with playwright (getBoundingClientRect per section per archetype) and write the numbers in NOTES.md. No JS beyond a stock theme; no carousel, no parallax, no reveal, no second buy button, no second price. Do not touch anything outside your concept folder.`
const CONCEPT = { type: 'object', required: ['name', 'sections', 'order', 'heightsAt390', 'imagesUsed', 'sentences', 'twice'], properties: { name: { type: 'string' }, sections: { type: 'array', items: { type: 'string' } }, order: { type: 'string' }, heightsAt390: { type: 'object', properties: { A: { type: 'array', items: { type: 'number' } }, B: { type: 'array', items: { type: 'number' } }, C: { type: 'array', items: { type: 'number' } } } }, imagesUsed: { type: 'array', items: { type: 'string' } }, sentences: { type: 'array', items: { type: 'string' } }, twice: { type: 'string' } } }
const ANGLES = [
  ['works', 'ANGLE «how it works, in daylight»: the main section is «איך זה עובד» — the solar mechanism as a FRAMED triptych of three small photographs inside the wrap (a real solar cell surface in the sun · the light at dusk · the light at night), one word or one short line each, warm daylight tones against the night ground; gated to solar products; on mains/USB products (B) the section shows a different, honest triptych (the wall, the bracket, the beam — or collapses to nothing: design and show it). A second section: one golden-hour photograph of a real Mediterranean or Israeli garden, framed with a caption that names it as a place («גינה בשעת בין ערביים»). Bright, explanatory, the sun does the work.'],
  ['install', 'ANGLE «what installing looks like»: the main section is «התקנה» — a photograph BESIDE text (not under it): hands pushing a stake into soil for the path family, a wall bracket with two screws for the wall family, a string on a hook for decor, stake or bracket for spot — with one line each, only facts the listings state (נעיצה באדמה, ברגים). Daylight or detail tones, the photograph framed at 4:5 in the wrap, the text column beside it on desktop and under it on phone. A second smaller section: two detail photographs side by side (rain on a lamp · sun on a panel) with one honest line each — outdoor lights live outside — no claim number. Practical, warm, close-up.'],
  ['weather', 'ANGLE «outside, all year»: the main section is a pair of DETAIL photographs (rain drops on a lamp head · a panel in hard sun; winter dust or wet leaves welcome) framed side by side with one honest line each and no number (the IP code stays in the facts); the tone is wet-grey and hard daylight — the opposite of the night band. A second section: «מקומות» — one warm dusk photograph of a real balcony or courtyard framed inside the wrap with a caption naming what it is, and a single line about the family\'s place using the licensed place word. Optionally a third: one line with the guide link as an image-led strip. Material, textured, close.'],
  ['daylight', 'ANGLE «the same garden by day»: the round-2 scene shows the night; this concept shows the DAY — a wide golden-hour or noon photograph of a real Mediterranean garden, framed (not full-bleed) with a cream or amber tone, a heading «ביום» and one line (for solar products: the sun on the panel by day is what the night uses — said differently from the dusk row; for mains: the light waits for the evening). A second section: one solar-cell close-up beside a very short caption for solar products only (B collapses). Consider a third: a small three-up of «before you buy» in image-led, statement form (sun on the spot · distance · outdoors), never questions. Light, warm, the opposite tonality of the page.'],
  ['pair', 'ANGLE «one place, day and dusk»: the main section is a PAIR of two photographs of one kind of place (a garden path, a wall, a courtyard) — one by day, one at dusk — side by side at the same crop, with one heading and one line; the pairing itself is the device (no scrim, no text on the photographs, the two frames on the night ground with a hairline gap). A second section: «לפני שקונים» as three tiny square photographs in a row with a three-word statement each (sun · distance · outdoors) and the guide link — image-led, not a checklist. Editorial, graphic, two-tone.'],
]
phase('Concepts')
const concepts = await parallel(ANGLES.map(([name, angle]) => () => agent(`${COMMON}\nYour concept folder: ${P}/concepts/${name}/. ${angle}\nReturn the section names, the template order you propose, the measured heights at 390 per archetype (A, B, C), the image ids used, every Hebrew sentence, and your honest one-line answer to «is any of this twice?».`, { label: `concept:${name}`, phase: 'Concepts', schema: CONCEPT, effort: 'high' })))
const ok = concepts.filter(Boolean)
log(`concepts: ${ok.map(c => c.name).join(', ')}`)

phase('Judge')
const SCORE = { type: 'object', required: ['scores', 'verdict'], properties: { verdict: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['concept', 'score', 'best', 'worst'], properties: { concept: { type: 'string' }, score: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' } } } } } }
const JUDGES = [
  ['owner', `You are the store owner. Your verdict of 2026-09-05 (read ${SIM}/SPEC.md §0 and §1) still stands: beautiful but SIMPLE for the customer, nothing twice, the night look, honest. Today you asked for MORE content on the product page — two or three more image sections, «creative, not the same look/colour», with photographs from the internet. Look at the deployed page first (${P2}/verify-after/pdp-path-m-js-full.png) so you know what «the same look» would be. Score what you would approve: is it really a different look, is it creative, is it still my store at night, is anything said twice, do the internet photographs stay honest (they never pretend to be my products), does the page still sell simply.`],
  ['shopper', `You are a first-time shopper in Israel on a 390×844 phone, on this product page, about to decide. Score: do the new sections help you press the button or delay you? Do the photographs make the light or the garden desirable, and do you understand in one read what each section says? Do they repeat something the page already told you (the kicker, the not-for line, the night scene, the dusk row, the specs, the terms)? Is the page now too long to reach the related products and the footer? Would you trust a page whose photographs are of other gardens — is that clear?`],
  ['honesty', `You are the brand guardian. Read ${P}/BRIEF.md §4 (licence, credit, no baked text, never presented as the product) and ${REPO}/brief/BRIEF.md §3. For every concept read COPY.md and IMAGES.md, check each image id against ${IMG}/manifest.jsonl or ${H2}/manifest.jsonl (licence must be CC0, PDM, CC BY or CC BY-SA; author recorded; a CC BY / BY-SA photograph without a drawn credit costs 3 points), look at the shots for faces, brand marks, baked text, and for any photograph that could be read as THIS product without a caption naming what it is; any claim the store cannot back (hours, lumens, «best», weather promises), a solar sentence on a mains product (archetype B is MAINS), a spec or a sentence the page already says. Score 10 = nothing to fix; each violation costs points; a photograph implying the product is a 2; a fabricated claim is a 2.`],
  ['designer', `You are the design lead of a small studio known for restraint. Look at the deployed page first (${P2}/verify-after/pdp-path-m-js-full.png and pdp-path-d-js-full.png) — the round-2 device is a full-bleed night photograph with words under it and a hairline row. Score craft from the shots: is the new look ONE clear idea and genuinely different (tonality, framing, device) while still the same store; how daylight or warm photographs sit on the night ground (frames, gaps, captions, credits as typography); crops and scale at 390 and 1366; rhythm with the buy box above and the facts / related grid below (read the grey bands); typography; whether archetype B (mains) and C (text-only scene) still look designed rather than degraded.`],
  ['engineer', `You are the theme engineer. Score feasibility under ${P}/BRIEF.md §4: sections ≤ 520 px at 390 and ≤ 1500 px together (read NOTES.md heights for A/B/C; the deployed pages are 4.4–5.0 screens today and the cap is 7.5), stock patterns only, no JS, the solar gate as elmsnest-s-pdp-dusk does it, the family by elmsnest-s-place emit:'word', images as theme assets (≤ 220 KB, ≤ 1800 px) with width/height, lazy loading, sizes that fit the rendered width and srcset candidates ≤ the file, ≤ 3 photographs per page, image_picker overrides and schema-editable copy, credits as settings, RTL logical properties, P6 bdi, the sticky bar's 78 px, weight. Penalise anything that needs JS, a Kalles change, a metafield, a tag, or a photograph over the limits.`],
]
const judged = await parallel(JUDGES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role}\nThe concepts are in ${P}/concepts/<name>/ for names: ${ok.map(c => c.name).join(', ')}. For EACH concept Read shots/A-m-fold.png, shots/A-m-full.png, shots/B-m-full.png, shots/C-m-full.png, shots/A-d-fold.png, then COPY.md, IMAGES.md, NOTES.md. Score each concept 0–10 with one sentence of best and one of worst, and a 4–6 sentence verdict naming a winner and what you would take from the others. Do not modify files.`, { label: `judge:${who}`, phase: 'Judge', schema: SCORE, effort: 'high' }).then(s => s && ({ who, ...s }))))
const jok = judged.filter(Boolean)
const weight = { owner: 1.5, shopper: 1.5 }
const totals = {}
for (const j of jok) for (const s of j.scores) { totals[s.concept] = (totals[s.concept] || 0) + s.score * (weight[j.who] || 1) }
const ranking = Object.entries(totals).sort((a, b) => b[1] - a[1])
log(`ranking: ${ranking.map(([c, t]) => `${c} ${t.toFixed(1)}`).join(' · ')}`)
return { concepts: ok, judges: jok, ranking }
