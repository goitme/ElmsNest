export const meta = {
  name: 'pdp2-critique',
  description: 'PDP round 2: adversarial critique of the DEPLOYED product-page sections from the real renders — five lenses (shopper, owner, honesty, designer, engineer), each finding then attacked by a skeptic; the lead rules afterwards',
  phases: [
    { title: 'Critique', detail: 'five lenses over the real shots and the files' },
    { title: 'Skeptics', detail: 'one skeptic per finding, evidence + scope' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pdp2`, THEME = `${REPO}/theme`, SIM = `${REPO}/brief/side-pages/simplify`
const SHOTS = `${P}/verify-after`
const COMMON = `Repo ${REPO}. The ElmsNest product page, round 2, is BUILT and DEPLOYED on the DEV theme (never published; the live theme is untouched; no product data changed). Two new sections sit after the buy box: sections/elmsnest-s-pdp-scene.liquid (one night photograph of THIS product in place when the store has a clean frame of it — 15 of 27 products — then the place heading, one family line and the guide link; no photograph otherwise, by design) and sections/elmsnest-s-pdp-dusk.liquid («כשמחשיך», a hairline row with two sentences, solar products only). The contract: ${P}/SPEC.md (read §1–§5 in full) and ${P}/BRIEF.md §2–§3; the concepts and judges that produced it are in ${P}/concepts/ (index of the ranking in SPEC's opening paragraph). The REAL renders (mirrored from the dev theme after the deploy, JS on, real fonts): ${SHOTS}/pdp-<name>-<v>-js-full.png and -fold.png for name in path (stainless path light: photo, solar), wall (waterproof wall light: photo, MAINS → no dusk row), flood (solar floodlight: NO photo, dusk row), rope (rope string light: photo, solar, 16 variants), deck (deck step light: NO photo, solar); v in m (390×844), s (360×640), d (1366×900). Per-section element shots: ${P}/verify/<scene|dusk>-<v>.png and -viewport.png (taken on the path light), ${P}/verify-wall/, ${P}/verify-flood/ (same names). Measured numbers: ${SHOTS}/verify.json (screens per page/viewport, the pdpSections entries with heights/images/text, pdpCopy, guideLinksInMain) and ${P}/verify-after.log. The files themselves: ${THEME}/sections/elmsnest-s-pdp-scene.liquid, ${THEME}/sections/elmsnest-s-pdp-dusk.liquid, ${THEME}/templates/product.elmsnest.json, the assets ${THEME}/assets/ens-pdp-*.jpg with their ledger ${P}/images/CHOSEN.md, and the neighbours ${THEME}/sections/elmsnest-s-pdp-facts.liquid, ${THEME}/snippets/elmsnest-s-pdp-notfor.liquid, ${THEME}/snippets/elmsnest-s-pdp-kicker.liquid, ${THEME}/snippets/elmsnest-s-place.liquid. The owner's standing verdict: ${SIM}/SPEC.md §0–§1 (simple to shop, nothing twice, the night look, the store's own product photos, no swapped product photos). Honesty rules: ${REPO}/brief/BRIEF.md §3.
Your job: find what is WRONG or WEAK in the deployed result, from your lens, with evidence (a shot name and what you see in it, a line in a file, a number from verify.json). At most 6 findings, most severe first; severity blocker / high / medium / low; each with a concrete fix. Do not report what the SPEC explicitly decided unless you can show it fails on the real render (then say which decision and why). Do not modify any file. Return the findings as JSON.`
const FIND = { type: 'object', required: ['lens', 'score', 'verdict', 'findings'], properties: { lens: { type: 'string' }, score: { type: 'number' }, verdict: { type: 'string' }, findings: { type: 'array', items: { type: 'object', required: ['id', 'severity', 'where', 'what', 'evidence', 'fix'], properties: { id: { type: 'string' }, severity: { type: 'string' }, where: { type: 'string' }, what: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }
const LENSES = [
  ['shopper', 'You are a first-time shopper in Israel on a 390×844 phone, on this product page, deciding. Look at pdp-path-m, pdp-wall-m, pdp-flood-m, pdp-rope-m and pdp-deck-m (fold + full) and the scene/dusk viewport shots. Do the new sections help you press the button or delay you? Is anything unclear in one read, poetic where it should be plain, repeated from the kicker / the not-for line / the specs? On the two pages WITHOUT a photograph (flood, deck): does the text block look like something is missing? Score the round 0–10 as a shopper.'],
  ['owner', 'You are the store owner. Your verdict (SIMPLIFY §0–§1): beautiful but simple, nothing twice, the night look, MY product photos, never another product\'s photo on a product page. You asked for 2–3 image-led sections on the product page, tuned for a sales page. Look at every shot at 390 and 1366. Is it your product in the photo? Is anything said twice on the page? Is the page still simple? Would you approve the copy (every sentence is listed in SPEC §2)? What would you change? Score 0–10.'],
  ['honesty', 'You are the brand guardian. Read the SPEC §2 copy and the two section files, then the shots. Any claim the store cannot back? Any sentence that is false for one of the 27 products it renders on (the gate: 16 solar / 11 not; the families: path, wall, spot, decor; the indoor birch)? Any photograph that shows another product than the page\'s (the map in the scene section vs images/CHOSEN.md and the product handles)? Any baked text, badge or brand sign left in an asset (open the ens-pdp-*.jpg files)? Any spec repeated? Score 10 = nothing to fix.'],
  ['designer', 'You are the design lead of a small studio known for restraint. Judge craft from the real shots at 390, 360 and 1366: the photograph\'s crop per product (is a lamp head cut? is the subject centred where it should be? object-position values in the scene section\'s map), the height and rhythm against the buy box above and the facts row below, the typography of the heading / line / link on the night ground, the dusk row against the facts\' rows, the text-only state on flood and deck (does it look designed or like a missing image?), the 1366 two-column text block. Score 0–10.'],
  ['engineer', 'You are the theme engineer. Read both section files and the template line by line, then verify.json. Check: the solar gate on every archetype (wall must have NO dusk row; flood/deck/path/rope must), the map (15 handles, crops, sizes vs CHOSEN.md), srcset candidates never wider than the file, width/height/lazy/decoding on the img, the block override path, the scene_fallback branch (all_products lookups, the credit line, the pictured product default), the guide link exactly once per page and its href, bdi isolation, logical properties, reduced motion, the section heights vs SPEC caps (verify.json pdpSections), screens ≤ 6 on every page, no Liquid error, no overflow-x, the §11 counts unchanged (forms 2, main 1, sticky 1, terms line 1, mailto 1, photo line 1). Score 0–10.'],
]
const VOTE = { type: 'object', required: ['refuted', 'confidence', 'reason'], properties: { refuted: { type: 'boolean' }, confidence: { type: 'number' }, reason: { type: 'string' } } }
phase('Critique')
const results = await pipeline(
  LENSES,
  ([lens, role]) => agent(`${COMMON}\nLENS «${lens}»: ${role}`, { label: `lens:${lens}`, phase: 'Critique', schema: FIND, effort: 'high' }),
  (r, [lens]) => r ? parallel(r.findings.map(f => () =>
    agent(`${COMMON}\nYou are the SKEPTIC. A «${lens}» critic reported this finding on the deployed product page:\n${JSON.stringify(f)}\nTry to REFUTE it on two grounds: (1) evidence — open the shot or file it cites and check whether what it describes is really there (a crop, a height, a sentence, a count; measure with the PNG and the numbers in verify.json, do not trust the description); (2) scope — is it inside this round's contract (SPEC §1–§5, BRIEF §2–§3) or a decision the SPEC records with a reason, or something out of scope (Kalles, product data, the owner's open items)? Default to refuted=true when the evidence is not there or the finding is out of scope; refuted=false only when you can confirm it on the real render or in the file and it is inside the contract. confidence 0–1. Do not modify files.`, { label: `skeptic:${lens}:${f.id}`, phase: 'Skeptics', schema: VOTE, effort: 'high' })
      .then(v => ({ lens, finding: f, vote: v })))).then(items => ({ lens, score: r.score, verdict: r.verdict, items: items.filter(Boolean) })) : null
)
const lensResults = results.filter(Boolean)
const all = lensResults.flatMap(r => r.items)
const confirmed = all.filter(x => x.vote && !x.vote.refuted)
log(`scores: ${lensResults.map(r => `${r.lens} ${r.score}`).join(' · ')} — findings: ${all.length}, confirmed by the skeptic: ${confirmed.length}`)
return { lenses: lensResults.map(r => ({ lens: r.lens, score: r.score, verdict: r.verdict })), findings: all }
