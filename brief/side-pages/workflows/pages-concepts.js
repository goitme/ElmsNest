export const meta = {
  name: 'pages-concepts',
  description: 'Content pages: five divergent concepts that re-dress the seven content pages in the SIMPLIFY vocabulary with licensed internet photographs (guide + about in full, why-solar + FAQ tops), rendered and shot; five judges; ranked result for the lead\'s synthesis',
  phases: [
    { title: 'Concepts', detail: 'five designers, five angles, guide + about in full' },
    { title: 'Judge', detail: 'five judges score every concept from the shots' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pages`, P3 = `${REPO}/brief/side-pages/pdp3`, IMG = `${P3}/images`, H2 = `${REPO}/brief/side-pages/home2/images`, P2 = `${REPO}/brief/side-pages/pdp2`, SIM = `${REPO}/brief/side-pages/simplify`, INV = `${REPO}/brief/inventory`
const PW = 'playwright is at /tmp/claude-0/-home-user-ElmsNest/*/scratchpad/node_modules/playwright (resolve the glob with fs.readdirSync), chromium executablePath /opt/pw-browsers/chromium-1194/chrome-linux/chrome with args [--no-sandbox]; fonts: the offline pack is /home/user/ElmsNest/brief/assets/fonts (FrankRuhlLibre-hebrew-*.woff2, FrankRuhlLibre-latin-*.woff2, Heebo-hebrew-*.woff2, Heebo-latin-*.woff2 — ls it) — declare @font-face from file:// paths in your index.html so the shots use the real faces'
const COMMON = `Repo ${REPO}. You design ONE concept for the seven CONTENT pages of the ElmsNest store (Hebrew RTL, solar outdoor lighting, the SIMPLIFY night look). The owner asked (in Arabic): go to the content pages too and add photographs there; the photographs may come from the internet. Read first, in full: ${P}/BRIEF.md (§1 what the pages are today, §2 the task and the screen targets, §3 the binding constraints — nothing twice, one device per idea, licensed photographs framed as place or mechanism, one section for seven handles — §4 the deliverable), ${P}/COPY-SOURCE.md (the verbatim copy of all seven pages — keep it; change only what §2 allows), ${SIM}/SPEC.md §0–§2 and §4 (the owner's verdict, P1–P7, the tokens), ${REPO}/brief/BRIEF.md §3 (honesty). Look at what the pages are today (the rejected cream editorial): ${INV}/page-guide/shot-mobile.png, ${INV}/page-about/shot-mobile.png, ${INV}/page-shipping/shot-mobile.png, ${INV}/page-guide/shot-desktop.png. Then look at the family the pages must join: ${SIM}/verify-after/home-m-js-full.png and ${P2}/verify-after/pdp-path-m-js-full.png (390 wide; the home and the product page as deployed), ${REPO}/brief/side-pages/home2/verify/home-m-js-full.png (the home with its image sections). Read the S sections for the house vocabulary: ${REPO}/theme/sections/elmsnest-s-guide-strip.liquid, elmsnest-s-terms.liquid, elmsnest-s-home-band.liquid, elmsnest-s-home-solar.liquid, elmsnest-s-pdp-scene.liquid (image treatment, credits, headings) and ${REPO}/theme/snippets/elmsnest-s-place.liquid (the four licensed place pairs and the four collection titles), ${REPO}/theme/snippets/elmsnest-v2-core.liquid (the :root block: exact colours, type sizes, --env2-* names). The photographs: ${IMG}/SHORTLIST.md (this round's licensed internet shortlist with roles — the content roles were requested — tones and credit lines; look at ${IMG}/contact-sheet.png; files ${IMG}/candidates/<id>.<ext>, records in ${IMG}/manifest.jsonl) and the home round's pool (${H2}/SHORTLIST.md, ${H2}/candidates/, ${H2}/manifest.jsonl); the store's own frames (${P2}/images/INVENTORY.md, ${H2}/own/) only where the copy speaks of the store's products.
Deliver in ${P}/concepts/<name>/ exactly per BRIEF §4: index.html (self-contained; dir="rtl" lang="he"; the GUIDE page and the ABOUT page rendered in full — header and footer as labelled grey bands of 64 px and 620 px at 390 — plus the top 1000 px of WHY-SOLAR and of FAQ; real copy from COPY-SOURCE.md; the real photograph files by relative path ../../../pdp3/images/candidates/… or ../../../home2/images/candidates/…; credits drawn where the licence needs one; give each page its own id so you can scroll to it), shots/ (390×844: guide-m-fold.png, guide-m-full.png, about-m-fold.png, about-m-full.png, why-m-fold.png, faq-m-fold.png; 1366×900: guide-d-fold.png, about-d-fold.png), COPY.md (every changed or new sentence; kept copy referenced by section), IMAGES.md (per photograph: id, author, licence, credit as drawn or 'none', crop, role, page, and how it is framed so it never reads as the store's product), NOTES.md (the device per idea and per page, the one-section template plumbing — how ONE section renders seven handles, where long copy lives, what the schema exposes — the measured heights at 390 for guide and about and the screen count with header + footer, and the honest «is any of this twice?»). ${PW}. Copy: Hebrew, claim-free, short sentences; digits/Latin in <bdi dir="ltr">; no «+», no «;», no terminal period on labels; the numbers byte-identical to the policies; the four place words only in the licensed forms; the four collection names as elmsnest-s-place prints them. Photographs: crop by CSS object-fit/object-position from the real files (no editing); ≤ 3 per page; a photograph is a place or a mechanism and is captioned or headed as such; no faces, brand marks, baked text; CC BY / BY-SA carry a drawn 11–12 px credit. No boxes, no cards, no shadows, radius 0, no JS beyond <details>. Do not touch anything outside your concept folder.`
const CONCEPT = { type: 'object', required: ['name', 'devices', 'plumbing', 'heightsAt390', 'imagesUsed', 'sentences', 'twice'], properties: { name: { type: 'string' }, devices: { type: 'string' }, plumbing: { type: 'string' }, heightsAt390: { type: 'object', properties: { guide: { type: 'number' }, about: { type: 'number' } } }, imagesUsed: { type: 'array', items: { type: 'string' } }, sentences: { type: 'array', items: { type: 'string' } }, twice: { type: 'string' } } }
const ANGLES = [
  ['atlas', 'ANGLE «an atlas of places»: the guide opens with a row of four small photographs — the four places (path · wall · garden · balcony) — each a licensed photograph of a real place captioned with the licensed place word and linking to its collection; the purposes are a numbered spine, the power sources a two-column, the checks a plain checklist. The About page opens on one wide golden-hour garden photograph under the h1 and carries the three principles as a spine. Photographs as wayfinding.'],
  ['letter', 'ANGLE «a letter from the store»: the About page is a letter — prose paragraphs in a narrow measure, one photograph beside the text on desktop and under the h1 on phone, signed with the store\'s email line. The guide is a decision spine: four decisions, each divided from the next by a hairline and ONE photograph at the break (a place, a mechanism, a check) with a caption — the text does the work, the photographs breathe. Warm, personal, quiet.'],
  ['field', 'ANGLE «a field guide»: daylight photographs with captions in the margin column — every page opens with a captioned photograph UNDER the h1 on the night ground (never a band), and the guide\'s four decisions use four different devices (photograph row · numbered spine · two-column · checklist). The FAQ is three groups of details with one photograph between groups. Captions are typography: small, muted, credited. Bright photographs on the dark page, wide gutters, editorial restraint.'],
  ['band', 'ANGLE «one band per page»: every content page opens with a top photograph band carrying the h1 on it (only where the crop measures ≥ 4.5:1 for the ink; otherwise the h1 sits under it), then ONE narrow reading column; inline photographs are full-width breaks between sections (max two), captioned. The guide\'s places are four collection links under a photograph, not a photograph each. The About page: the band, three principles as a spine, the collections as four plain links. Cinematic, few images, strong tops.'],
  ['quiet', 'ANGLE «the fewest images that make it real»: one photograph per page, placed where it means the most — the guide: after the places, a real Israeli garden at dusk with the four place words as captions pointing at what is in the picture; the About page: one photograph beside the h1; why-solar: the panel in the sun beside the mechanism; the FAQ: none, or one at the end above the contact line. Everything else is typography: a clear scale, hairlines, a numbered spine only where order is real. The template plumbing is the point: one section, seven handles, copy as literal Liquid with schema settings for the photographs and headings. Minimal, precise.'],
]
phase('Concepts')
const concepts = await parallel(ANGLES.map(([name, angle]) => () => agent(`${COMMON}\nYour concept folder: ${P}/concepts/${name}/. ${angle}\nReturn the devices per page (one line), the plumbing (one line), the measured heights at 390 for guide and about (page height without the grey header/footer bands), the image ids used, every changed or new Hebrew sentence, and your honest one-line answer to «is any of this twice?».`, { label: `concept:${name}`, phase: 'Concepts', schema: CONCEPT, effort: 'high' })))
const ok = concepts.filter(Boolean)
log(`concepts: ${ok.map(c => c.name).join(', ')}`)

phase('Judge')
const SCORE = { type: 'object', required: ['scores', 'verdict'], properties: { verdict: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['concept', 'score', 'best', 'worst'], properties: { concept: { type: 'string' }, score: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' } } } } } }
const JUDGES = [
  ['owner', `You are the store owner. Your verdict of 2026-09-05 (read ${SIM}/SPEC.md §0 and §1) still stands: beautiful but SIMPLE, nothing twice, the night look, honest, email not WhatsApp. You rejected the cream editorial these pages still wear (look at ${INV}/page-guide/shot-mobile.png). Today you asked for the content pages to get real photographs too. Score what you would approve: do the pages now read as one family with the home and the product page (${SIM}/verify-after/home-m-js-full.png, ${P2}/verify-after/pdp-path-m-js-full.png), do they have real pictures that are not pretending to be my products, is anything twice, is it simple.`],
  ['visitor', `You are a first-time visitor who arrived on the guide page from a search on a 390×844 phone; you have a garden path and a balcony and no idea what to buy. Score: does the guide help you choose and send you to a collection, or is it a wall of text? Do the photographs help you place yourself? Would you read the About page? Would you find the shipping numbers if you were on the FAQ? Is anything said twice on a page?`],
  ['honesty', `You are the brand guardian. Read ${P}/BRIEF.md §3 and ${REPO}/brief/BRIEF.md §3. For every concept read COPY.md and IMAGES.md, check each image id against ${IMG}/manifest.jsonl or ${H2}/manifest.jsonl (licence must be CC0, PDM, CC BY or CC BY-SA; author recorded; a CC BY / BY-SA photograph without a drawn credit costs 3 points), look at the shots for faces, brand marks, baked text, and for any photograph that could be read as the store's product without a caption naming what it is; check the numbers against the policies (0 ₪ pickup, 29.90 ₪, 1–3, 7–14, 8–17, 14 days, 5% or 100 ₪, 17 business days), the why-solar does-not-suit list against the four licensed pairs in elmsnest-s-place, the four collection names. Score 10 = nothing to fix; each violation costs points; a photograph implying the product is a 2; a wrong number is a 2.`],
  ['designer', `You are the design lead of a small studio known for restraint. Look at the family first (${SIM}/verify-after/home-m-js-full.png, ${P2}/verify-after/pdp-path-m-js-full.png). Score craft from the shots: family resemblance to the S pages, image treatment (how bright photographs sit on the night ground, frames, gaps, captions and credits as typography), rhythm and measure at 390 and 1366, typography, whether each page's devices vary with the content or repeat one ruled list, and whether the guide and the About page are two different pages that clearly belong to one store.`],
  ['engineer', `You are the theme engineer. Score feasibility under ${P}/BRIEF.md §3: ONE section for seven handles (read NOTES.md plumbing), the template swap (templates/page.json = [ens_page]; page.contact-us.json keeps the stock contact-form under an ens_page entry), where long copy lives, schema settings for every photograph and heading, images as theme assets ≤ 220 KB / ≤ 1800 px with width/height, lazy below the first, sizes that fit, ≤ 3 per page, the screen targets (guide ≤ 6, about ≤ 5, why ≤ 5, FAQ ≤ 6 — read the heights in NOTES.md), stock patterns only, no JS beyond details, RTL logical properties, P6 bdi, weight. Penalise anything that needs JS, a Kalles change, a page-body edit in Shopify, a metafield.`],
]
const judged = await parallel(JUDGES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role}\nThe concepts are in ${P}/concepts/<name>/ for names: ${ok.map(c => c.name).join(', ')}. For EACH concept Read shots/guide-m-fold.png, shots/guide-m-full.png, shots/about-m-full.png, shots/why-m-fold.png, shots/faq-m-fold.png, shots/guide-d-fold.png, then COPY.md, IMAGES.md, NOTES.md. Score each concept 0–10 with one sentence of best and one of worst, and a 4–6 sentence verdict naming a winner and what you would take from the others. Do not modify files.`, { label: `judge:${who}`, phase: 'Judge', schema: SCORE, effort: 'high' }).then(s => s && ({ who, ...s }))))
const jok = judged.filter(Boolean)
const weight = { owner: 1.5, visitor: 1.5 }
const totals = {}
for (const j of jok) for (const s of j.scores) { totals[s.concept] = (totals[s.concept] || 0) + s.score * (weight[j.who] || 1) }
const ranking = Object.entries(totals).sort((a, b) => b[1] - a[1])
log(`ranking: ${ranking.map(([c, t]) => `${c} ${t.toFixed(1)}`).join(' · ')}`)
return { concepts: ok, judges: jok, ranking }
