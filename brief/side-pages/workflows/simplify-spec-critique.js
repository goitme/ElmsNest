export const meta = {
  name: 'simplify-spec-critique',
  description: 'Three adversarial critics review the SIMPLIFY spec before anything is built: shopper simplicity, Kalles feasibility, copy/RTL honesty',
  phases: [{ title: 'Critique', detail: 'three lenses in parallel' }],
}
const SPEC = 'C:\\Users\\covid\\AppData\\Local\\Temp\\claude\\c--Users-covid-Desktop-claude\\bfa8f647-4fec-44c7-8dd1-7c6bb6aaaf08\\scratchpad\\repo\\brief\\side-pages\\simplify\\SPEC.md'
const AUD = 'C:\\Users\\covid\\AppData\\Local\\Temp\\claude\\c--Users-covid-Desktop-claude\\bfa8f647-4fec-44c7-8dd1-7c6bb6aaaf08\\scratchpad\\repo\\brief\\side-pages\\simplify\\audits.txt'
const REPO = 'C:\\Users\\covid\\AppData\\Local\\Temp\\claude\\c--Users-covid-Desktop-claude\\bfa8f647-4fec-44c7-8dd1-7c6bb6aaaf08\\scratchpad\\repo'
const KALLES = 'C:\\Users\\covid\\Desktop\\claude\\ElmsNest Theme'
const SLICES = 'C:\\Users\\covid\\AppData\\Local\\Temp\\claude\\c--Users-covid-Desktop-claude\\bfa8f647-4fec-44c7-8dd1-7c6bb6aaaf08\\scratchpad\\shots\\slices'
const COMMON = `Read ${SPEC} in full first. Context: ${AUD} holds four audits of the current (rejected) pages; the theme files we own are under ${REPO}\\theme; the stock Kalles 5.4.2 source (sections/, snippets/, blocks/, assets/) is a local copy at ${KALLES}. Do not modify any file. Your output is a list of concrete, verifiable defects in the SPEC with the fix for each. Do not restate the spec, do not praise it. Every finding must cite the spec section and, where relevant, the file and line in the theme source that proves it.`
const SCHEMA = { type: 'object', properties: {
  verdict: { type: 'string', description: 'one paragraph: is the spec buildable as written and will it satisfy the owner? what is the single biggest risk?' },
  findings: { type: 'array', items: { type: 'object', properties: {
    spec_section: { type: 'string' }, severity: { type: 'string', description: 'blocker | high | medium | low' },
    problem: { type: 'string' }, proof: { type: 'string', description: 'file:line or measurement' }, fix: { type: 'string' } },
    required: ['spec_section', 'severity', 'problem', 'proof', 'fix'] } },
}, required: ['verdict', 'findings'] }
phase('Critique')
const critics = [
  { key: 'shopper', prompt: `${COMMON}
You are a first-time Israeli shopper on a phone AND a conversion designer. Walk the three page structures in SPEC §6–§8 as if rendered. For each page: is there anything a Dawn/Kalles shopper would not recognise instantly? Does any buyer question get answered twice (check §3 against §6–§8 line by line)? Is the buy path on the PDP exactly one form with variant pills + quantity + one button, with nothing between the price and the button except the per-unit caption? Will the mobile screen budgets in P5 hold given the block list (estimate px per block from the current slices in ${SLICES}: e.g. main-product with thumbnails gallery on 390 wide, a 2-col Kalles grid of 27 cards at 4/5 ratio)? Is anything in the spec still 'clever' in a way the owner just rejected (novel controls, editorial devices, chapter numerals, ledgers)? Is the email CTA placement once per page really once (count header/footer too and say what we control)? Also judge the collection filter row: is 'הכל + four titles' clear enough, and is putting the place word as eyebrow above the Shopify title readable or confusing?` },
  { key: 'kalles', prompt: `${COMMON}
You are a Shopify theme engineer who knows Kalles 5.4.2. Verify EVERY claim the spec makes about stock components against the source at ${KALLES}: (1) sections/main-product.liquid settings ids (hdt_show_sticky_atc, sticky_atc, color_scheme, colors_by_section) and how the sticky bar submits — does it reuse the main form or render a second form (if second, the spec's 'exactly 1 form' test is wrong; say what the true count is and what selector to test instead); (2) blocks/_group-product.liquid accepted child block types and whether a _liquid block ('_liquid' with option_type 'liquid') can render a snippet with the product object; (3) blocks/_product-buy-button.liquid: show_quantity_selector, show_dynamic_checkout, and what happens when checkout is disabled; (4) blocks/_product-variant-picker.liquid picker_type 'block' output and how the variant change event is dispatched (name of the event / the custom element) so a per-unit caption can update; (5) snippets/elmsnest-pdp-unit-price.liquid in ${REPO}\\brief\\inventory\\theme-src\\snippets — does it exist, what does it print, does it update on variant change; (6) sections/main-collection.liquid: the settings ids the spec sets (products_count, col_mb, image_ratio values, pagination_type values, enable_sorting, enable_filtering, enable_num_cols_selector, show_description, color_scheme) and what the toolbar renders with filtering off — list the exact DOM/class names the skin must hide; the sort options it offers and their labels/locale keys; (7) snippets/card-product1.liquid: which elements render with settings minimal_products:true, hidden_badges:true, show_quick_add:false, wishlist_mode '0', compare_mode '0', color_mode 'color' — list the class names of everything that still renders besides image/title/price so the skin can hide them; which image it uses (featured_image / featured_media / secondary hover image) and whether any dimming/lazy class could reproduce the black boxes; (8) sections/related-products.liquid display_type values and whether 'grid' exists; (9) sections/brc-nav-product.liquid exists and its settings; (10) sections/elmsnest-v2-hero.liquid in ${REPO}\\theme: where the card and the secondary CTA render so two boolean settings can gate them; (11) layout/theme.liquid render order and that adding one render line after elmsnest-v2-core is safe; (12) any schema constraint that would make Shopify reject a file (label length, presets, block limits). For each verified item give the exact ids/values/classes the engineers must use.` },
  { key: 'copy', prompt: `${COMMON}
You are a Hebrew copy editor and RTL front-end reviewer with the store's honesty rules (${REPO}\\brief\\BRIEF.md §3). Check: every Hebrew string in the spec (§5.2 pairs, §5.3 terms, §5.4 contact line, §6–§8 headings and labels) against its source — BRIEF §3 pairs verbatim; the licensed terms wording in ${REPO}\\theme\\sections\\elmsnest-v2-terms.liquid schema defaults and in ${REPO}\\brief\\inventory\\theme-src\\snippets\\elmsnest-pdp-trust.liquid / elmsnest-pdp-facts.liquid — flag any word the spec changed or invented (the 1 תמונה row: is 'במייל' a change from the licensed 'בוואטסאפ' wording, and what is the safe phrasing given the owner's answer 3); nikud/typography (geresh ׳ vs apostrophe, maqaf, the em-dash usage); every place a number or Latin token needs <bdi>; the mailto subject/body strings in snippets/elmsnest-v2-photo-url.liquid; whether 'לכל המוצרים' pointing to /collections/all and the collection filter row labels are consistent with the main menu (main-menu items: דף הבית · קולקציות › the four titles · מדריך לבחירה · שאלות נפוצות · מי אנחנו · יצירת קשר); heading choices («לפי המקום», «מה שנדלק ראשון», «אנחנו נגיד לכם גם מתי לא.», «עוד לאותו מקום», «ארבעה מספרים שכדאי לדעת») — natural Israeli Hebrew or AI-ish? propose better if needed; schema label lengths ≤ 70 chars; anything that would read as a claim (P4). Also check that nothing in the spec says or implies WhatsApp.` },
]
const results = await parallel(critics.map(c => () => agent(c.prompt, { label: `critic:${c.key}`, phase: 'Critique', schema: SCHEMA }).then(r => ({ key: c.key, r }))))
return results.filter(Boolean)