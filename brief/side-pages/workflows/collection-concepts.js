export const meta = {
  name: 'collection-concepts-round-2',
  description: 'Five divergent collection-page concepts as rendered offline mockups, judged from the screenshots by five lenses, synthesized into a build-ready spec',
  phases: [{ title: 'Concepts', detail: '5 designers, 5 seeds, 3 mockups each' }, { title: 'Judges', detail: '5 lenses score from the PNGs' }, { title: 'Synthesis', detail: 'lead writes WINNING-SPEC + RULING' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/collection`
const SEEDS = [
  { key: 'howmuch', name: 'כמה אור', seed: 'HOW MUCH LIGHT. The page is organised by the quantity of light, not by SKU: the collection lays its lamps out along an axis the buyer actually thinks in — metres of string, units along a path, watts on a wall, how far the beam reaches — so choosing is moving along that axis rather than reading a grid. The axis is drawn (a rail, a scale, a horizon) and the products sit on it at their real values; price follows the axis. Narrowing 27 lamps becomes answering one question: how much light does the place need?' },
  { key: 'evening', name: 'ערב אחד', seed: 'ONE EVENING. The collection is a single night scene that the visitor scrolls through — the same garden from the gate to the terrace — and each lamp appears where it would actually stand: path lights along the walk, wall lights at the door, garlands over the table. The grid dissolves into a place. Products are pinned to the scene and their cards rise from it; the collection description becomes the caption of the scene. Browsing is walking through the evening you are buying.' },
  { key: 'index', name: 'האינדקס', seed: 'THE INDEX. A typographic catalogue, not a photo grid: the whole collection is a single editorial index — every lamp one hairline row with its name in Heebo, its option axis, its price in glow, and one photograph that enlarges as the row is reached. Dense, calm, complete, scannable in one screen on desktop; the whole of a 27-product store legible without pagination. The photographs are earned, not decorative — only the row you are on shows its light.' },
  { key: 'places', name: 'ארבעה מקומות', seed: 'THE PLACE CONTINUES. The page is the homepage section «ארבעה מקומות» opened: the visitor is still standing in the place they chose, and the page opens with that place at night, its approved suits-phrase, its count and its price span, and then answers the place\'s own question before showing any product (how long is the terrace? how dark is the path?). Products arrive already sorted by that answer. The four collections feel like four rooms of one house, and /collections/all is the house seen from outside.' },
  { key: 'switchboard', name: 'לוח המתגים', seed: 'THE SWITCHBOARD. The collection is a board of lamps you can turn on: every product renders dim, and the page gives the visitor a small number of honest switches built from the catalogue\'s own data (the place, the power source, the price band, the size axis) — flipping one lights the lamps that match and leaves the others dark, on the page, without a filter drawer. Narrowing is literally switching lights on. The state is in the URL and works server-side without JS.' },
]
const DESIGN_SCHEMA = { type: 'object', required: ['key', 'files', 'pngs', 'idea'], properties: { key: { type: 'string' }, files: { type: 'array', items: { type: 'string' } }, pngs: { type: 'array', items: { type: 'string' } }, idea: { type: 'string' }, narrowing: { type: 'string' }, weakest: { type: 'string' }, heights: { type: 'string' } } }

phase('Concepts')
const designs = await parallel(SEEDS.map((s, i) => () => agent(`You are designer ${i + 1} of 5 on the ElmsNest COLLECTION page concept panel. Repo ${ROOT}. Read, in full and in this order: ${DIR}/BRIEF.md · ${ROOT}/brief/WINNING-SPEC.md §3 (design system, binding) · ${ROOT}/brief/build-preview/CONTRACT.md · ${ROOT}/brief/side-pages/pdp/WINNING-SPEC.md §3.5, §3.6 and §4.8 (image ledger, option-axis rule, and the product card that already exists — you reuse it) · ${ROOT}/brief/inventory/AUDIT-collection.md (what you replace) · ${DIR}/data.json (your only source of products, prices, option axes and images).
Look (Read) at these renders before you design: ${ROOT}/brief/inventory/pdp-multi/http-desktop-fold.png and http-mobile-fold.png (the finished product page — the level to match and the store you belong to), ${ROOT}/brief/inventory/home/http-desktop.png (the homepage), and ${ROOT}/brief/inventory/coll-all/http-desktop.png (the Kalles catalogue you replace).

YOUR CREATIVE SEED (commit to it; the other four are radically different): ${s.seed}
Concept key: "${s.key}" ("${s.name}").

**This container is restarted every hour or two, so work in a way that survives it: FIRST run \`ls ${DIR}/concepts/${s.key}/\` — if files are already there, a previous run of you was interrupted: READ them, keep their idea and CSS, and produce only what is missing. Then work strictly in this order, writing each artefact to disk and screenshotting it BEFORE starting the next: index.html → shot → path.html → shot-path → all.html → shot-all → CRITIQUE.md. Never hold work in memory across steps.**

Build offline, real content only, in ${DIR}/concepts/${s.key}/:
- index.html — /collections/גרילנדות-ותאורה-דקורטיבית (7 products, 105 variants, 89.90–469.90): the complete page.
- path.html — /collections/תאורת-שביל-סולארית (8 products, 69.90–999.90): the same system under a 14x price span.
- all.html — /collections/all (27 products): the first two screens at minimum, and how the whole catalogue is composed and paginated (or why it is not).
Each: RTL Hebrew, responsive 1440 and 390, the night ground and the type system, lamps that light on arrival (IntersectionObserver; no-JS and reduced-motion = all lit), local assets only (images ../../../../assets/img/<handle>-<i>.jpg per the ledger — NEVER index 0 of the never-use list; fonts ../../../../assets/fonts.css), a static Kalles-like header strip (70px, transparent, centred gold logo, menu דף הבית · קולקציות · מדריך לבחירה · מי אנחנו · יצירת קשר) and a dark footer block — do not design those.
Then screenshot each: node ${ROOT}/brief/shot.js ${DIR}/concepts/${s.key}/index.html ${DIR}/concepts/${s.key}/shot ; path.html → shot-path ; all.html → shot-all. READ your PNGs (the Read tool renders images): the mobile fold must show the place, a real product and a real price, and the route to narrowing. Fix and re-shoot until the folds are right and shot.js reports no horizontal overflow and no JS errors. Then write ${DIR}/concepts/${s.key}/CRITIQUE.md (BRIEF §8).
Judged on (BRIEF §9): findability + selling 35, the one idea 20, composition 15, typography 10, honesty+system 10, feasibility 10. Rules: no toolbar, no four equal boxes in a row, no badge or strikethrough, no invented facts, no stars, no English UI, no cream/brown, product titles never in the serif, radius 0 except pills, every screen composed differently.
Return: key, the three files, the PNG paths, the idea in one sentence, how a visitor narrows 7 / 8 / 27 lamps to a choice, your weakest point, and the heights shot.js printed.`, { label: `design:${s.key}`, phase: 'Concepts', schema: DESIGN_SCHEMA, effort: 'high' })))
const ok = designs.filter(Boolean)
log(`concepts: ${ok.map(d => d.key).join(', ')}`)

phase('Judges')
const SCORE = { type: 'object', required: ['lens', 'scores', 'ranking', 'grafts', 'summary'], properties: { lens: { type: 'string' }, scores: { type: 'array', items: { type: 'object', required: ['key', 'findability', 'idea', 'composition', 'typography', 'honesty', 'feasibility', 'total'], properties: { key: { type: 'string' }, findability: { type: 'number' }, idea: { type: 'number' }, composition: { type: 'number' }, typography: { type: 'number' }, honesty: { type: 'number' }, feasibility: { type: 'number' }, total: { type: 'number' }, best: { type: 'string' }, worst: { type: 'string' }, violations: { type: 'array', items: { type: 'string' } } } } }, ranking: { type: 'array', items: { type: 'string' } }, grafts: { type: 'array', items: { type: 'object', required: ['from', 'device', 'why'], properties: { from: { type: 'string' }, device: { type: 'string' }, why: { type: 'string' } } } }, summary: { type: 'string' } } }
const LENSES = [
  ['creative-director', 'You are the creative director who rejected this store\'s first attempt as "from the nineties, everything repeated". Judge the idea, the composition and the typography from the renders first. Is the page one grid scrolled four times? Is there an idea only a lighting store could have, visible in a still? Does it belong to the same store as the finished product page and the homepage?'],
  ['shopper', 'You are an Israeli shopper on a phone who came from the homepage having chosen "מרפסת ופינת ישיבה". Judge findability and selling hardest (35 of 100): in the first screen, do you know where you are, do you see a real product with a real price, and do you understand how to narrow the set? How many taps to a lamp that suits you? Does the 14x price span on the path collection confuse you? Is /collections/all navigable or a wall?'],
  ['merchandiser', 'You are a merchandiser for a 27-product single-category store. Judge whether the page makes the catalogue feel deliberate rather than thin: does it use the real axes (metres, units, watts, Kelvin, beam) instead of generic filters; does it surface the range (69.90-999.90) honestly; does it push the single-variant products that can be bought in one tap; does it make the four places feel like four rooms of one house; and does /collections/all justify existing.'],
  ['brand-honesty', 'You are the brand guardian. Check: no badge, no strikethrough, no "-N%", no stars, no review counts, no urgency, no sales collection link, no invented facts; counts and ranges look computed, not typed; the approved suits-phrases are the only place vocabulary; the image ledger is respected (no baked-in-text photo, no LUMIÈRE mark); and the page is unmistakably the same store as the homepage and the PDP.'],
  ['liquid-feasibility', 'You are the Shopify engineer who will build the winner, replacing Kalles main-collection.liquid. Judge feasibility: can it be rendered with paginate + server-side ?sort_by= links and no JS; does the narrowing device have an honest server-side fallback (URL state, not a JS-only filter); does it reuse snippets/elmsnest-v2-pdp-card.liquid rather than forking it; how much of the concept survives when a collection has 6 products instead of 27; and what breaks on the 105-variant decor collection.'],
]
const judgeInputs = ok.map(d => `- ${d.key}: "${d.idea}" · narrowing: ${d.narrowing || '?'} · files ${d.files.join(', ')} · PNGs ${d.pngs.join(', ')} · heights ${d.heights || '?'} · designer's weakest point: ${d.weakest || '-'}`).join('\n')
const judges = await parallel(LENSES.map(([lens, role]) => () => agent(`${role}
Repo ${ROOT}. Read ${DIR}/BRIEF.md (§6 the bar, §9 criteria and weights) and ${ROOT}/brief/WINNING-SPEC.md §3.
The five concepts:
${judgeInputs}
Judge from the RENDERS first: for each concept Read shot-desktop-fold.png, shot-mobile-fold.png, shot-mobile.png, and the full desktop page sliced into readable pieces (use PIL to cut ${DIR}/concepts/<key>/shot-desktop.png into 4-6 vertical slices at 50% into /tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/cj-${'${lens}'}-<key>/ and Read them), plus shot-path-*-fold.png and shot-all-*-fold.png. Only then read CRITIQUE.md and the HTML.
Score each concept 0-10 per criterion and compute total = 0.35*findability + 0.20*idea + 0.15*composition + 0.10*typography + 0.10*honesty + 0.10*feasibility. List rule violations. Rank all five. Name up to 6 grafts worth carrying into the winner. Summary: 5-8 decisive sentences with evidence from the screenshots.`, { label: `judge:${lens}`, phase: 'Judges', schema: SCORE, effort: 'high' })))
const jok = judges.filter(Boolean)
const agg = {}
for (const j of jok) for (const s of j.scores) { const w = j.lens && j.lens.indexOf('shopper') === 0 ? 1.5 : 1; agg[s.key] = agg[s.key] || { sum: 0, w: 0 }; agg[s.key].sum += s.total * w; agg[s.key].w += w }
const table = Object.entries(agg).map(([k, v]) => ({ key: k, mean: +(v.sum / v.w).toFixed(2) })).sort((a, b) => b.mean - a.mean)
log(`judged: ${table.map(t => `${t.key} ${t.mean}`).join(' · ')}`)

phase('Synthesis')
const lead = await agent(`You are the design lead for the ElmsNest collection page. Repo ${ROOT}. Inputs: ${DIR}/BRIEF.md (binding), ${ROOT}/brief/side-pages/pdp/WINNING-SPEC.md (the MODEL of a build-ready spec — match its structure and precision; its §3.5/§3.6/§4.8 are binding here), ${ROOT}/brief/WINNING-SPEC.md §3, ${ROOT}/brief/inventory/AUDIT-collection.md, ${ROOT}/brief/side-pages/core/REPORT.md §9, ${ROOT}/brief/side-pages/OWNER-NOTES.md.
Concepts: ${JSON.stringify(ok.map(d => ({ key: d.key, idea: d.idea, narrowing: d.narrowing, files: d.files, pngs: d.pngs })))}
Judges: ${JSON.stringify(jok).slice(0, 30000)}
Weighted aggregate (shopper x1.5): ${JSON.stringify(table)}
(1) Look at every concept's renders yourself before reading the judges' text; disagree where the pixels say otherwise and say so. (2) Choose the winner — the aggregate is advice; a page that does not let a phone visitor narrow the set cannot win. (3) Write ${DIR}/WINNING-SPEC.md, build-ready, in the PDP spec's structure: §1 the idea and the ruling; §2 grafts; §3 global decisions (the ground for a page of variable length, type scale, motion, the narrowing device and its URL/no-JS contract, the card extension, the image ledger, how /collections/all differs, pagination or its replacement); §4 section-by-section build spec with exact Hebrew copy, desktop and mobile layouts, imagery, motion, schema settings/blocks and non-negotiables; §5 templates/collection.json + the file list + what is retired from Kalles; §6 the do-not list; §7 references (which concept file and lines to lift from). Be exact: px, hex, filenames, schema ids, Liquid objects. (4) Write ${DIR}/RULING.md in Hebrew for the owner: 10-15 lines — the winner and why in plain words, the runner-up and what was taken from it, two or three things that will look different once built, and the one risk.
Return: winner key, ranking, the two file paths, and one line per concept as you would say it to the owner.`, { label: 'lead:synthesis', phase: 'Synthesis', effort: 'high' })
return { designs: ok.map(d => ({ key: d.key, idea: d.idea, narrowing: d.narrowing, heights: d.heights })), table, judges: jok.map(j => ({ lens: j.lens, ranking: j.ranking, summary: j.summary })), lead }
