export const meta = {
  name: 'inventory-visual-audit',
  description: 'Audit the real renders of every side-page template on the ElmsNest dev theme, by family, then merge',
  phases: [{ title: 'Audit', detail: 'one auditor per template family, reading the contact sheets' }, { title: 'Merge', detail: 'one lead merges into INVENTORY.md' }],
}
const FAMILIES = [
  { key: 'collection', pages: ['coll-all','coll-all-sorted','coll-wall','coll-path','coll-spot','coll-decor','coll-list','coll-sale'], note: 'coll-all = /collections/all (27 products, 12 per page, pagination); coll-all-sorted = same with ?sort_by=price-ascending; coll-wall/path/spot/decor = the four real collections; coll-list = /collections (list-collections template); coll-sale = /collections/sale which currently renders the 404 template (collection not published to the online store).' },
  { key: 'product', pages: ['pdp-single','pdp-multi','pdp-wall'], note: 'pdp-single = stainless-steel-solar-path-light-ip65 (1 variant, 169.90); pdp-multi = solar-crystal-ball-string-lights (24 variants: length×colour, 89.90–179.90); pdp-wall = waterproof-led-wall-light-ip65-6w-12w (8 variants, 219.90–252.90). The PDP already had a design pass ("PDP Design v2": sections elms-pdp-trust-strip, -description, -night-gallery(showcase), -installation, -comparison, elmsnest-product-guidance, elms-pdp-faq, related products) that nobody has reviewed yet — judge it against the same bar as everything else.' },
  { key: 'cart-search-404', pages: ['cart-empty','cart-full','search-hits','search-none','search-blank','p404','page-accessibility','blog-news'], note: 'cart-full has 2× path light + 1 crystal-ball string; cart-empty is the empty state; search-hits = /search?q=שביל; search-none = zero results; search-blank = /search with no query; p404 = a missing URL; page-accessibility = an unpublished page (renders 404); blog-news = the empty default blog (placeholder posts). The cart drawer (Kalles hdt-cart-drawer) is in the system group on every page but is closed in these static renders — say so, do not guess its look.' },
  { key: 'content-pages', pages: ['page-guide','page-why-solar','page-about','page-shipping','page-faq','page-contact','page-processing'], note: 'All use templates/page.json (an "editorial" custom section elmsnest-content-page + main-heading + main-page) except contact-us (page.contact-us: main-heading + contact-form). Note the duplicate h1 (editorial section + heading section) that facts.py found on processing/why-solar.' },
  { key: 'policies-home', pages: ['policy-shipping','policy-refund','policy-terms','policy-privacy','home'], note: 'The four /policies/* pages use Shopify\'s policy layout through the theme (no template JSON to edit — only layout/theme.liquid CSS reaches them). "home" is the NEW v2 homepage on the same dev theme: it is the reference for the design system, not something to audit — describe in 6–10 lines what it looks like in these renders so the merge has a baseline, and list which of its devices (sky gradient, lamps lighting, serif display type, hairline ledger, staircase, divider stage) are visible in the static shots.' },
]
const DESIGN_SYSTEM = `Design system the side pages must join (from brief/WINNING-SPEC.md §3 and HANDOFF.md §3): one page-long dusk→night sky gradient (#4a6a9c→#1f3357→#0f1a2f→#070b15→#020306), sections transparent, no brown/beige/cream anywhere (the old theme scheme is #2b2118 on #f7f0e6 — that is exactly what must go); ink #f4eee3, ink-2 #c9c4b8, mute #8f95a3, gold #e9b96e (kickers/rules), glow #ffd394 (prices, primary button), hairline rgba(244,238,227,.12); Frank Ruhl Libre 500/700/900 display + Heebo 300/400/500 text; radius 0 except pill buttons; no boxes/cards, hairlines separate; lamps (photos) dim until they enter view then light once; no fade-in text, no parallax; honesty rules: no best-seller/review counts/customer quotes/"trusted by"/countdowns; the four approved suits/does-not-suit pairs are the only negatives allowed; consumer terms (free pickup / 29.90 door; 8–17 business days = 1–3 + 7–14, warehouses outside Israel; 14-day cancellation ≤5% or 100₪; send a photo on WhatsApp) must stay findable. The owner rejected the first homepage as "nineties, everything repeated, trivial text, no creativity" — the bar for every page is: one idea only a lighting store could have, every screen composed differently, editorial Hebrew type, motion that means something, and it must sell.`
phase('Audit')
const audits = await parallel(FAMILIES.map(f => () => agent(`You are a design auditor doing the INVENTORY step of a redesign. Repo: /home/user/ElmsNest. Family: ${f.key}. Pages: ${f.pages.join(', ')}. Context: ${f.note}

Each page has a mirror of the real dev-theme render in /home/user/ElmsNest/brief/inventory/<key>/: index.html (the real HTML), http-desktop.png / http-mobile.png (full page, 1440 and 390, 2x, rendered with the theme JS running), http-desktop-fold.png / http-mobile-fold.png (first screen), and sheet-http.jpg (contact sheet: desktop full | mobile full | both folds). IGNORE any shot-*.png / sheet-shot.jpg files in the same folders: they are stale renders without theme JS in which every product grid is invisible. Ready-made slicer: "python3 /home/user/ElmsNest/brief/inventory/crops.py <key> desktop 4 0.5 http" writes 4 readable slices to the scratchpad crops/ folder (same for mobile). Tall pages: sheet-http.jpg is downscaled, so ALSO crop the full-page PNGs into readable slices with python PIL (or use crops.py) and Read the slices. You MUST look at the images (Read the PNG/JPG files) — do not judge from HTML alone. Also read /home/user/ElmsNest/brief/inventory/INVENTORY-FACTS.md (section order per page) and, if it exists, /home/user/ElmsNest/brief/inventory/THEME-SRC.md (what each section is). Read /home/user/ElmsNest/HANDOFF.md §1–§3 for the bar and the design system.

${DESIGN_SYSTEM}

Write /home/user/ElmsNest/brief/inventory/AUDIT-${f.key}.md, in English, facts first, with for EACH page:
1. **What renders** (desktop and mobile): the visible section order with 1 line each — what it is, colours/type it uses, what it shows (e.g. "grey photo banner 1440×180 with 'קטלוג' + breadcrumb", "toolbar: sort dropdown, 6 grid-size icons, filter link", "3×4 product grid, white cards, quick-add icon, price range"). Quote real text you can read in the screenshot. Note the header/footer state (transparent? cream? dark?), the mobile first screen (what is above the 844 px fold, and whether a product/price/CTA is inside it), page height.
2. **Against the design system**: every visible contradiction (cream/brown surfaces, Assistant type, boxes/cards, radius, default e-commerce patterns, filter/sort UI kit, badges, placeholders, empty states with template copy, duplicate h1, Latin text that flips, anything that looks broken or unfinished). Be concrete: element + where + why.
3. **Honesty check**: any fabricated-claim pattern visible or in the HTML (ratings, review counts, "trusted by", countdowns, "best seller", customer quotes, guarantees not in the policies, comparison tables that assert facts about competitors). Quote the text.
4. **What is worth keeping** (device, copy, data source, Liquid mechanics) — with the file/section name from THEME-SRC.md or the HTML if you can see it.
5. **Verdict in one sentence**: what the owner would say about this page today.
Then a family summary: the 5 biggest facts a designer must know before briefing this family (e.g. 27 products / 12 per page / how many variants / what data exists / what the empty states say), and the 3 hardest technical constraints you can see (Kalles JS-driven components, filter forms, cart-drawer, sticky ATC, variant pickers).

Return (as your final text) ONLY: the path of the file you wrote, then a JSON object {"pages":[{"key":..., "verdict":..., "heightDesktop":..., "heightMobile":..., "foldHasBuy": true|false|null, "blockers":[...max 5 short strings...], "keep":[...max 4...]}]}.`, { label: `audit:${f.key}`, phase: 'Audit' })))
log(`audits done: ${audits.filter(Boolean).length}/${FAMILIES.length}`)
phase('Merge')
const merged = await agent(`You are the design lead. Read every /home/user/ElmsNest/brief/inventory/AUDIT-*.md (written by five auditors just now), /home/user/ElmsNest/brief/inventory/INVENTORY-FACTS.md, /home/user/ElmsNest/brief/inventory/THEME-SRC.md if it exists, and /home/user/ElmsNest/HANDOFF.md §3 and §5. Look yourself at these renders before writing (Read the images): /home/user/ElmsNest/brief/inventory/coll-all/sheet-http.jpg, /home/user/ElmsNest/brief/inventory/pdp-single/http-desktop-fold.png, /home/user/ElmsNest/brief/inventory/pdp-single/http-mobile-fold.png, /home/user/ElmsNest/brief/inventory/cart-full/sheet-http.jpg, /home/user/ElmsNest/brief/inventory/search-hits/sheet-http.jpg, /home/user/ElmsNest/brief/inventory/p404/sheet-http.jpg, /home/user/ElmsNest/brief/inventory/page-guide/sheet-http.jpg, /home/user/ElmsNest/brief/inventory/home/sheet-http.jpg (the new homepage = the baseline).

Write /home/user/ElmsNest/brief/inventory/INVENTORY.md (English, ≤ 350 lines): 
§0 one paragraph: the state of the side pages in one honest sentence per family, and the gap to the new homepage.
§1 a table with one row per TEMPLATE (collection, product, cart page, cart drawer, search, 404, page (editorial), page.contact-us, list-collections, policies, blog/article, password, customers/*): what it renders today (1 line), the sections/files involved (from THEME-SRC/FACTS), the visible contradictions with the design system (1 line), what to keep (1 line), and a risk/effort note (Kalles JS dependencies, filters, variant pickers, cart-drawer, sticky ATC).
§2 "Shared plumbing that must exist before any side page": what the audits show is global (header/footer state on non-index pages, back-to-top, WhatsApp float absent, cream body/wrappers, Assistant type, the index-only gradient in elmsnest-v2-base) — as a concrete list of what a page-agnostic core has to do.
§3 Honesty ledger: every fabricated-claim pattern any auditor found, with file + quote + keep/kill.
§4 Data facts for briefs: products (27; variants per product; options; metafields that exist and which products have them — from THEME-SRC and the auditors), collections (4 + all; counts; images; descriptions), pages (8, which are published), policies (4), menus, search behaviour, cart behaviour (drawer + page), what the empty states say.
§5 Open questions only the owner can answer (numbered, each one sentence, each with why it blocks design) — think: WhatsApp number, hero master photo, lockup, whether /collections/sale exists on purpose, whether the PDP v2 pass is approved or to be replaced, product photography with baked-in text, whether filters/sort matter for 27 products, whether the blog stays, whether accounts stay Shopify-hosted, the accessibility statement, the policies' English titles ("Refund policy", "Contact" with Arabic placeholders in the contact policy).
Facts only, precise, quote setting keys/file names. Then return (final text) ONLY the path and a JSON {"families":[{"key":...,"oneLine":...}], "openQuestions":[...strings...], "sharedPlumbing":[...strings...]}.`, { label: 'merge:inventory', phase: 'Merge' })
return { audits, merged }