export const meta = {
  name: 'collection-critics-remaining',
  description: 'The three adversarial lenses still owed on the deployed ElmsNest collection page — creative director, Hebrew typographer, front-end QA — each writing findings to a file',
  phases: [{ title: 'Critique', detail: 'three lenses, file-backed' }],
}
const ROOT = '/home/user/ElmsNest'
const DIR = `${ROOT}/brief/side-pages/collection`
const COMMON = `Repo ${ROOT}. Shopify DEV theme gid://shopify/OnlineStoreTheme/154726400174 — UNPUBLISHED. READ-ONLY for you: do not modify any theme file, do not deploy, do not commit, do not use gh.
The new collection page is deployed and mirrored. Five URLs, each with index.html plus http-desktop.png, http-mobile.png, http-desktop-fold.png, http-mobile-fold.png in ${ROOT}/brief/inventory/:
  coll-decor (7 garlands, 105 variants, 89.90-469.90; the metre ruler) · coll-path (8 lamps, 69.90-999.90, a 14x span; the quantity ruler) · coll-wall (6 lamps; the ruler runs in PRICE mode and the measure ledger correctly gates itself off) · coll-spot (6 lamps, six of six shown as an illustration because no clean photograph exists) · coll-all (27 products; the ruler becomes a table of contents).
Read the specs you audit against: ${DIR}/BRIEF.md (§2 owner decisions, §6 the bar), ${DIR}/WINNING-SPEC.md §4 (what each section must be) and §6 (the do-not list), ${ROOT}/brief/WINNING-SPEC.md §3 (the store's design system), ${ROOT}/brief/side-pages/core/REPORT.md §9 (known core bugs — do not report them as new), and ${DIR}/CRITIQUE-lead.md — the lead already verified the honesty rules, the bdi discipline, the tap targets, the no-JS narrowing, the counts, and executed the shopper journey; DO NOT repeat those checks, extend them. One finding is already open and being fixed (LEAD-01, the narrowing affordance was below the fold); the fix is deployed, so judge what you see.
Tooling: Chromium has no internet. Serve a mirror with (cd ${ROOT}/brief/inventory/<key> && python3 -m http.server <port>) and drive it with Playwright (module /tmp/claude-0/-home-user-ElmsNest/1c2132db-077d-58e0-b54a-35f2ebea6b2c/scratchpad/node_modules/playwright, chromium /opt/pw-browsers/chromium-1194/chrome-linux/chrome, args --no-sandbox). Route external requests: continue 127.0.0.1, fulfil fonts.googleapis.com with an empty stylesheet, abort the rest, and goto with waitUntil 'domcontentloaded' — 'load' times out on a mirror. Crop the tall full-page PNGs into readable slices with PIL before reading them (the pages are 8,700-22,300 px).`
const FIND = { type: 'object', required: ['lens', 'file', 'verdict', 'counts', 'top'], properties: { lens: { type: 'string' }, file: { type: 'string' }, verdict: { type: 'string' }, wouldOwnerSayNineties: { type: 'boolean' }, counts: { type: 'object', properties: { blocker: { type: 'integer' }, major: { type: 'integer' }, minor: { type: 'integer' }, nit: { type: 'integer' } } }, top: { type: 'array', items: { type: 'object', required: ['id', 'severity', 'where', 'what', 'fix'], properties: { id: { type: 'string' }, severity: { type: 'string' }, where: { type: 'string' }, what: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }
const CRITICS = [
  ['creative', 'creative director', `You are the creative director who rejected this store's first attempt as "from the nineties, everything repeated, trivial text, no creativity, no innovation, no creative visual design", and who has already approved the homepage and the product page. Your job is to REFUTE the claim that this collection page is world-class. Judge from the renders only, in this order: the folds of all five URLs, then each full page sliced into readable pieces. Ask: is every screen composed differently or does one band repeat down the page; is there real scale contrast; is the idea (light measured in the buyer's own units) visible in a still or only in interaction; do the five URLs read as five pages or one template with different photographs; does the page get weaker as it descends; is the illustration plate on coll-spot a considered choice or an apology; and does the whole thing read as the same store as ${ROOT}/brief/inventory/home/http-desktop.png and ${ROOT}/brief/inventory/pdp-multi/http-desktop.png. Name the weakest screen and say what you would kill.`],
  ['typographer', 'Hebrew typographer', `You are a Hebrew typographer for premium Israeli brands. Judge the type from the renders at 1440 and 390 on all five URLs: hierarchy and scale, display leading on the long collection titles (ספוטים, פרוז׳קטורים ותאורה ניידת breaks to three lines at 1440 and leaves ניידת alone in glow on the third — rule on it), line length in the description paragraphs, RTL correctness, mixed-direction strings in the rail values (5-22 מ׳, ≈17.98 ₪ למטר, 6.5 מ׳), the maqaf/geresh/gershayim, ragged edges on end-aligned paragraphs, tabular alignment of the prices down the rail, product titles never set in the serif, and any copy that reads translated rather than written. Grep the mirrored HTML to confirm each claim about markup.`],
  ['qa', 'front-end QA engineer', `You are a front-end QA engineer. TEST, do not read. On the served mirrors: (1) keyboard-only — reach the ruler, change the stop with arrow keys, reach a product and add a single-variant product to the cart; report tab order and focus visibility; (2) prefers-reduced-motion: reduce — every lamp lit, no transitions; (3) JavaScript disabled (context javaScriptEnabled:false) — the ruler, the sort links, the prices and the add-to-cart forms all work; the lead already proved the ruler narrows correctly, so test the REST; (4) 320 px and 390x664 (iPhone small viewport) — nothing clipped, no overflow, the sticky elements do not cover content; (5) screen-reader semantics — one h1 per URL, heading order, the ruler's group name, alt text on the photographs and on the illustration plates, and whether a screen-reader user can tell which stop is selected; (6) the code audit — every setting templates/collection.json writes exists in that section's schema, presets present, block.shopify_attributes on block roots, no Liquid inside stylesheet/javascript blocks, no unprefixed selectors, and no console error beyond the two known mirror ones. Report each test as pass/fail with the evidence you produced.`],
]
phase('Critique')
const out = await parallel(CRITICS.map(([key, name, role]) => () => agent(`${COMMON}

${role}

**Write your findings to ${DIR}/CRITIQUE-${key}.md AS YOU GO — in stages, before you return — because this container is restarted regularly and an unwritten finding is a lost finding.** The file must contain: a one-paragraph verdict; a table of every finding (id ${key.toUpperCase()}-01..., severity blocker/major/minor/nit, where — file or section or screen, what, the evidence YOU produced with the path of the image you read or the measurement you took, and the fix you propose); then "what is genuinely good" (three items maximum, so the fixer knows what not to break). Severity: blocker = the owner would reject the page, or a visitor cannot find or buy a lamp; major = the bar in BRIEF §6 fails, or a rule in WINNING-SPEC §6 is broken, or an accessibility failure; minor; nit. Do not pad, do not praise to be kind, and do not restate what CRITIQUE-lead.md already established.
Return: your lens, the file path, the verdict in one sentence, the counts per severity, and your top findings (up to 8, most severe first).`, { label: `critic:${key}`, phase: 'Critique', schema: FIND, effort: 'high' })))
const ok = out.filter(Boolean)
log(`critics done: ${ok.map(c => `${c.lens} ${(c.counts||{}).blocker||0}B/${(c.counts||{}).major||0}M`).join(' · ')}`)
return { critics: ok }