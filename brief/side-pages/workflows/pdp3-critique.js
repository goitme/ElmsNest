export const meta = {
  name: 'pdp3-critique',
  description: 'PDP round 3 critique: five lenses read the REAL deployed product pages (five products, three widths) and the two new section files; a skeptic tries to refute every finding with evidence and scope',
  phases: [
    { title: 'Critique', detail: 'five lenses on the real shots and the real files' },
    { title: 'Refute', detail: 'one skeptic per finding, evidence and scope' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pdp3`, V = `${P}/verify`, P2 = `${REPO}/brief/side-pages/pdp2`, SIM = `${REPO}/brief/side-pages/simplify`
const SHOTS = `The REAL deployed pages (dev theme, mirrored and shot 2026-09-08) are in ${V}/: for each of pdp-path (stainless path light, solar), pdp-rope (rope lights, solar, decor), pdp-deck (deck/step lights, solar, no clean own frame), pdp-wall (waterproof wall light, MAINS, 8 variants) and pdp-flood (floodlight, solar, text-only scene) there are <name>-m-js-full.png (390 wide, whole page), <name>-m-js-fold.png (390x844 first screen) and <name>-d-js-full.png (1366). The measured numbers are ${V}/verify.json (screens, section heights, pdpCopy with the drawn credits and the print caption). The round-2 page for comparison: ${P2}/verify-after/pdp-path-m-js-full.png.`
const FILES = `The two new sections are ${REPO}/theme/sections/elmsnest-s-pdp-weather.liquid and elmsnest-s-pdp-print.liquid; the contract is ${P}/SPEC.md (§1 the rulings, §2 and §3 the two sections, §5 the budgets); the photographs and their licences are ${P}/images/CHOSEN.md and ${P}/images/SHORTLIST.md; the honesty rules are ${REPO}/brief/BRIEF.md §3 and ${P}/BRIEF.md §4; the store's principles are ${SIM}/SPEC.md §0-§2.`
phase('Critique')
const FIND = { type: 'object', required: ['score', 'findings'], properties: { score: { type: 'number' }, findings: { type: 'array', items: { type: 'object', required: ['title', 'severity', 'evidence', 'fix'], properties: { title: { type: 'string' }, severity: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }
const LENSES = [
  ['owner', `You are the store owner, reading your own product pages on the dev theme after asking for «more content, two or three image sections, creative, not the same look/colour». Your SIMPLIFY verdict binds: simple, nothing twice, my night store, honest, no invented product photos. Look at all five products at 390 and at 1366. Is the new look really different from the night band you already had? Is anything said twice? Does any photograph look like it is pretending to be my lamp? Is the page still simple to buy from? Score 0-10 and list what you would send back.`],
  ['shopper', `You are a first-time shopper in Israel on a 390x844 phone, deciding whether to buy. Read the five full-page shots as a customer scrolls them. Does the new content help you decide or delay you? Is anything confusing, repeated, or unclear in one read? Do the two new sections earn their place between the buy box, the specs and the related grid? Would you reach the footer? Score 0-10.`],
  ['honesty', `You are the brand guardian. ${FILES} On the REAL renders check: every CC BY / CC BY-SA photograph carries its drawn credit (the verify.json pdpCopy.credits arrays record what rendered per page); the public-domain print carries none; every caption describes the frame it sits under (open the asset files in ${REPO}/theme/assets/ and look); no photograph reads as the store's product; no sentence claims what the listings do not state; the mains page (pdp-wall) has no solar word and no panel photograph. Score 0-10; a false caption or a missing credit is a blocker.`],
  ['designer', `You are the design lead. Compare the round-2 page (${P2}/verify-after/pdp-path-m-js-full.png) with today's five. Judge: is the new look one clear idea, does the cream print sit right on the night ground, do the two square tiles read at 390, crops, type sizes, the rhythm between the buy box, the scene, the dusk row, the pair, the specs, the print and the related grid; the 1366 renders; whether the mains and the text-only-scene products still look designed. Score 0-10.`],
  ['engineer', `You are the theme engineer. ${FILES} Read ${V}/verify.json in full: screens per page and width, section heights, images sized/lazy/loaded, srcset candidates, liquidErr, ovX (horizontal overflow), bdi. Check the budgets of SPEC §5 (each section <= 430 / 420 px at 390, both <= 850, page <= 7.5 screens at 390) and note any page over target at 360 as well. Read both section files for correctness (gates, escaping, srcset caps, logical properties, no JS). Score 0-10.`],
]
const lensed = await parallel(LENSES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role}\n${SHOTS}\nOpen the shots you need with Read (they are large: read the -m-js-full ones at least for all five products). Return your score and every finding with a short title, a severity (blocker | should | nit), the EVIDENCE you saw (name the shot or file and what is at which position), and the exact fix. Do not modify any file.`, { label: `lens:${who}`, phase: 'Critique', schema: FIND, effort: 'high' }).then(r => r && ({ lens: who, ...r }))))
const ok = lensed.filter(Boolean)
const all = ok.flatMap(l => l.findings.map(f => ({ lens: l.lens, ...f })))
log(`${all.length} findings from ${ok.length} lenses`)

phase('Refute')
const VOTE = { type: 'object', required: ['refuted', 'confidence', 'why'], properties: { refuted: { type: 'boolean' }, confidence: { type: 'number' }, why: { type: 'string' }, scope: { type: 'string' } } }
const voted = await parallel(all.map((f, i) => () => agent(`Repo ${REPO}. You are a skeptic. A critique lens («${f.lens}») claims about the deployed ElmsNest product page:\nTITLE: ${f.title}\nSEVERITY: ${f.severity}\nEVIDENCE: ${f.evidence}\nPROPOSED FIX: ${f.fix}\n\nTry to REFUTE it. ${SHOTS} ${FILES} Open the exact shot or file the evidence names and measure (crop the PNG with PIL and Read the crop; grep the Liquid; read ${V}/verify.json). Refute if the evidence is wrong, if it misreads the render, if the page already does what it asks, or if it is outside this round's contract (${P}/SPEC.md §1 records what was deliberately rejected and why — a finding that re-opens a recorded ruling is out of scope unless it shows the ruling's own reasoning is factually wrong). Default to refuted=true when uncertain. Return refuted, confidence 0-1, why (with what you measured), and scope.`, { label: `skeptic:${i}:${f.lens}`, phase: 'Refute', schema: VOTE, effort: 'high' }).then(v => v && ({ ...f, vote: v }))))
const vok = voted.filter(Boolean)
const confirmed = vok.filter(f => !f.vote.refuted)
log(`confirmed ${confirmed.length} of ${vok.length}`)
return { lenses: ok.map(l => ({ lens: l.lens, score: l.score })), findings: vok }
