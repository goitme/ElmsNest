export const meta = {
  name: 'pages-critique',
  description: 'Content pages critique: five lenses read the REAL deployed content pages (seven pages, three widths) and the section file; a skeptic tries to refute every finding with evidence and scope',
  phases: [
    { title: 'Critique', detail: 'five lenses on the real shots and the real files' },
    { title: 'Refute', detail: 'one skeptic per finding' },
  ],
}
const REPO = '/home/user/ElmsNest', P = `${REPO}/brief/side-pages/pages`, V = `${P}/verify`, SIM = `${REPO}/brief/side-pages/simplify`, P2 = `${REPO}/brief/side-pages/pdp2`, INV = `${REPO}/brief/inventory`
const SHOTS = `The REAL deployed pages (dev theme, mirrored and shot 2026-09-08) are in ${V}/: for each of page-guide, page-why, page-about, page-faq, page-processing, page-shipping and page-contact there are <name>-m-js-full.png (390 wide, whole page), <name>-m-js-fold.png (390x844) and <name>-d-js-full.png (1366). The measured numbers are ${V}/verify.json (screens, pageCopy with the h1, the photographs found with kicker/caption/credit, the policy numbers, the place labels, the signature). What these pages looked like BEFORE (the rejected cream editorial) is ${INV}/page-guide/shot-mobile.png and ${INV}/page-shipping/shot-mobile.png. The family they must join: ${SIM}/verify-after/home-m-js-full.png and ${P2}/verify-after/pdp-path-m-js-full.png.`
const FILES = `The build is ${REPO}/theme/sections/elmsnest-s-page.liquid (one section, seven handles) and ${REPO}/theme/snippets/elmsnest-s-page-photo.liquid, with ${REPO}/theme/templates/page.json and page.contact-us.json. The contract is ${P}/SPEC.md (§1 the rulings, §2 the plumbing, §3 the ten photographs, §4 the copy changes, §5 the budgets); the source copy is ${P}/COPY-SOURCE.md; the photographs and licences are ${P}/IMAGES.md; the honesty rules are ${REPO}/brief/BRIEF.md §3.`
phase('Critique')
const FIND = { type: 'object', required: ['score', 'findings'], properties: { score: { type: 'number' }, findings: { type: 'array', items: { type: 'object', required: ['title', 'severity', 'evidence', 'fix'], properties: { title: { type: 'string' }, severity: { type: 'string' }, evidence: { type: 'string' }, fix: { type: 'string' } } } } } }
const LENSES = [
  ['owner', `You are the store owner. You rejected the cream editorial these pages wore and asked for photographs on the content pages. Your SIMPLIFY verdict binds: simple, nothing twice, my night store, honest, email not WhatsApp. Read all seven pages at 390 and the guide and About at 1366. Do they open the way my home and my product page open? Is anything twice? Does any photograph pretend to be my product or sit where it does not belong? Is the guide still the page that helps someone choose? Score 0-10.`],
  ['visitor', `You are a first-time visitor who arrived on the guide from a search on a 390x844 phone; you have a path and a balcony and no idea what to buy. Then you check the FAQ for shipping and open the contact page. Score: does the guide help you choose and send you to a collection? Can you find the numbers? Is any page a wall of text, or repeated? Would you write to the store? Score 0-10.`],
  ['honesty', `You are the brand guardian. ${FILES} On the REAL renders check: every CC BY / CC BY-SA photograph carries its drawn credit exactly as ${P}/IMAGES.md records it; every caption names what the photograph is before anything else and describes the frame truthfully (open the assets in ${REPO}/theme/assets/ens-page-*.jpg and look); no photograph reads as the store's product; the seven policy numbers are byte-identical to the policies; the four does-not-suit lines are the licensed pairs; the four collection titles are the live Shopify ones; no count except products_count, no urgency, no review, no invented claim. Score 0-10; a false caption, a missing credit or a wrong number is a blocker.`],
  ['designer', `You are the design lead. Compare the seven pages with the home and the product page (paths above) and with what they replaced. Judge: the family resemblance, the band grammar (short band, h1 on it only where contrast allows), how bright photographs sit on the night ground, whether the devices really vary with the content or repeat one ruled list, the reading measure and rhythm at 390 and at 1366, typography, the captions and credits as type. Score 0-10.`],
  ['engineer', `You are the theme engineer. ${FILES} Read ${V}/verify.json in full (screens per page and width, h1 counts, images sized/lazy/loaded/files, badLinks, liquidErr, ovX). Check SPEC §5's targets, the one-section-for-seven-handles shape, the template swap and the untouched stock contact form, image weights and srcset caps, the fallback branch for an unknown handle, RTL logical properties, no JS beyond details. Read the section and the snippet for correctness. Score 0-10.`],
]
const lensed = await parallel(LENSES.map(([who, role]) => () => agent(`Repo ${REPO}. ${role}\n${SHOTS}\nOpen the shots you need with Read. Return your score and every finding with a short title, a severity (blocker | should | nit), the EVIDENCE you saw (name the shot or file and what is at which position) and the exact fix. Do not modify any file.`, { label: `lens:${who}`, phase: 'Critique', schema: FIND, effort: 'high' }).then(r => r && ({ lens: who, ...r }))))
const ok = lensed.filter(Boolean)
const all = ok.flatMap(l => l.findings.map(f => ({ lens: l.lens, ...f })))
log(`${all.length} findings from ${ok.length} lenses`)

phase('Refute')
const VOTE = { type: 'object', required: ['refuted', 'confidence', 'why'], properties: { refuted: { type: 'boolean' }, confidence: { type: 'number' }, why: { type: 'string' }, scope: { type: 'string' } } }
const voted = await parallel(all.map((f, i) => () => agent(`Repo ${REPO}. You are a skeptic. A critique lens («${f.lens}») claims about the deployed ElmsNest content pages:\nTITLE: ${f.title}\nSEVERITY: ${f.severity}\nEVIDENCE: ${f.evidence}\nPROPOSED FIX: ${f.fix}\n\nTry to REFUTE it. ${SHOTS} ${FILES} Open the exact shot or file the evidence names and measure (crop the PNG with PIL and Read the crop; grep the Liquid; read ${V}/verify.json; open the asset). Refute if the evidence is wrong, if it misreads the render, if the page already does what it asks, or if it is outside this round's contract (${P}/SPEC.md §1 records what was deliberately ruled and why — a finding that re-opens a recorded ruling is out of scope unless it shows the ruling's own reasoning is factually wrong). Default to refuted=true when uncertain. Return refuted, confidence 0-1, why (with what you measured), and scope.`, { label: `skeptic:${i}:${f.lens}`, phase: 'Refute', schema: VOTE, effort: 'high' }).then(v => v && ({ ...f, vote: v }))))
const vok = voted.filter(Boolean)
log(`confirmed ${vok.filter(f => !f.vote.refuted).length} of ${vok.length}`)
return { lenses: ok.map(l => ({ lens: l.lens, score: l.score })), findings: vok }
