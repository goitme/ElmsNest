{"lens": "engineer", "finding": {"id": "E1", "severity": "high", "where": "/home/user/ElmsNest/theme/assets/ens-pdp-rope.jpg (map entry /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid lines 71–74; SPEC §3 row solar-rope-string-lights «pre-crop 0,210,1254,1044 (the IP65 badge is above y 200)»)", "what": "The rope frame still contains the baked badge «עמידות IP65 / מתאימה לשימוש חוץ» and it is fully visible on the phone render. The pre-crop removed y 0–210 of the source, but the badge sits at source y 210–350 (file rows 0–140). At 390×440 object-fit:cover the scale is .421 → 529×440, so the whole file height is shown and no object-position can hide it; at 1366×560 the 50% 50% crop happens to cut it out, which is why the desktop shot passes. This is the state SPEC §3 rejects outright («rejected if any glyph, badge or icon survives»), BRIEF §3 image rule (4) forbids, and it prints «IP65» on the photograph while the facts print it again (nothing twice).", "evidence": "verify-after/pdp-rope-m-js-full.png, scene at y≈1326: the cream pill with «עמידות IP65» top-left of the photograph; verify-after/pdp-rope-s-js-fold.png same; pixel scan of ens-pdp-rope.jpg: last row with badge pixels in x<600 = 140 (source y 350); verify-after/pdp-rope-d-js-full.png shows no badge (cropped by the desktop position).", "fix": "Re-cut with images/prepare-assets.py using crop 0,370,1254,884 (badge bottom is source y 350; 884 px tall is still ≥ 600 so no vertical upscaling at 440/560), set ens_h = 884 in the map, re-upload via themeFilesUpsert, update CHOSEN.md and DEPLOY-LOG, re-mirror pdp-rope at 390/360. Until the re-cut lands, remove the rope entry from the map (text-only is the honest state) or let the owner add a `frame` block."}, "vote": {"refuted": false, "confidence": 0.95, "reason": "Confirmed on the file and the real render. ens-pdp-rope.jpg (1254×1044) carries the cream «עמידות IP65 / מתאימה לשימוש חוץ» pill in file rows 0–139 (pixel scan: last cream row 139 = source y 349), so SPEC §3 line 126's recorded reason for crop 0,210 (\"the IP65 badge is above y 200\") is false. Scene CSS (scene.liquid line 361: block-size 440px, object-fit cover) gives cover scale .421 at 390 wide, showing the full file height — no object-position can hide it; at 1366×560 (line 377) the 50% crop shows rows ≈265–779, which is why the desktop shot is clean. Cropping verify-after/pdp-rope-m-js-full.png at y 1326 (scene start per verify.json: header 60 + main 1266) shows the badge fully legible top-left of the photograph. Minor citation error: pdp-rope-s-js-fold.png only shows the gallery (first 640 px), not the scene, but the m-full shot and the identical cover geometry at 360 make the point regardless. In scope: SPEC §3 line 139 ('rejected if any glyph, badge or icon survives') and pdp2 BRIEF §3 rule (4) (line 79) both forbid this; the SPEC decision exception does not apply because its stated reason fails on the real file. The proposed re-cut 0,370,1254,884 (band ≥ 600 px, no vertical upscaling) matches the SPEC's own band rule."}}
{"lens": "engineer", "finding": {"id": "E2", "severity": "medium", "where": "/home/user/ElmsNest/theme/assets/ens-pdp-stainless.jpg (304455 B), ens-pdp-powerful.jpg (293076 B), ens-pdp-scene-path.jpg (294799 B); ledger /home/user/ElmsNest/brief/side-pages/pdp2/images/CHOSEN.md rows 1, 12, 15", "what": "Three assets exceed the SPEC §3 / BRIEF §3 budget of ≤ 220 KB per file (297, 286, 287 KB; CHOSEN.md records the overage without flagging it). It matters on the render: the path light is the store's flagship page, and the CDN derivatives it serves are 600w 148 KB, 900w 281 KB (what a 390 px @2x phone picks with sizes=100vw), 1254w 297 KB (@3x, the original untouched) — versus 56/109/150 KB for the wall frame of the same pixel size. The 900w candidate saves 16 KB over the original, i.e. the E7 concern (heavy re-encode) is real here.", "evidence": "verify-after/mirrors/pdp-path/index.html srcset files: a/861c0e24… 600×600 148 KB, a/e2197091… 900×900 281 KB, a/a7462d21… 1254×1254 297 KB; mirrors/pdp-wall: 56 / 109 / 150 KB; ls -la theme/assets/ens-pdp-*.jpg.", "fix": "Re-encode the three files under the budget (q≈68–70 or a light chroma/noise reduction on the grainy stainless frame; the scene-path file can also go to 1100 px since it is an off-by-default option), confirm ≤ 220 KB and progressive, re-upload, update CHOSEN.md; re-mirror pdp-path and record the 900w derivative size."}, "vote": {"refuted": false, "confidence": 0.92, "reason": "Evidence holds and the finding is inside the contract. (1) Files: `ls -la theme/assets/ens-pdp-*.jpg` gives ens-pdp-stainless.jpg 304455 B (297 KiB), ens-pdp-powerful.jpg 293076 B (286 KiB), ens-pdp-scene-path.jpg 294799 B (288 KiB); all three are 1254 px progressive JPEGs (header parsed), so the overage is not a dimension error. CHOSEN.md rows 1, 12, 15 record «297 KB q78», «287 KB q74», «286 KB q82» with no flag; the ledger's own tool images/prepare-assets.py (lines 34–38) steps quality down from 82 to 55 until ≤ 220 KB, so a q78/q82 file over 220 KB means the budget loop was bypassed or run with a different --kb. (2) Render: sections/elmsnest-s-pdp-scene.liquid line 330 has sizes=\"100vw\" with a 600/900/1254w srcset (verify.json pdp-path-m-js pdpSections srcset \"600w/900w/1254w\", imgs 1); the mirrored CDN derivatives on pdp-path are a/861c0e24… 600×600 152400 B (149 KiB), a/e2197091… 900×900 288747 B (282 KiB — what a 390 px @2x phone selects), a/a7462d21… 1254×1254 304501 B (297 KiB); pdp-wall's derivatives of identical pixel sizes are 58142 / 112445 / 154250 B (57/110/151 KiB). The 900w derivative saves only 15.7 KB over the 1254 original, matching the engineer's E7 point. (3) Scope: SPEC §3 heading states «assets/ens-pdp-*.jpg, ≤ 220 KB, progressive JPEG» and line 157 «JPEG ≤ 220 KB»; BRIEF §2 rule 6 (weight) and §3 («≤ 220 KB, ≤ 1800 px, JPEG, progressive») bind every concept. No SPEC decision records an exception for these three files. Minor caveats that do not refute: only the stainless file is served on one of the five verified pages (powerful is not among them; scene-path is the off-by-default fallback and never rendered), so the rendered impact is confirmed on pdp-path only; the per-file budget breach itself is a file rule and stands for all three. Severity «medium» is fair."}}
{"lens": "engineer", "finding": {"id": "E3", "severity": "medium", "where": "/home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid lines 169–218 (the scene_fallback branch), especially 177–179, 210–217", "what": "When `scene_fallback` is on and the pictured product resolves to blank — the owner leaves `pictured_*` empty and the default `all_products['…']` handle is unpublished from the storefront, renamed or deleted — `ens_kind` is still set to 'asset'/'picker' (lines 210–214) while `ens_credit` stays false (line 215 requires `ens_pictured != blank`). The collection scene then renders with no «בתמונה:» line: a sibling product's photograph on a product page with nothing saying so — exactly what BRIEF §3 image rule (3) and the owner's «swapped product photos» verdict forbid. Dormant today (option off, credit=false on every render), but the option exists for the owner to switch on.", "evidence": "Code path only: line 215 `if ens_pictured != blank and ens_pictured.handle != product.handle` gates the credit, nothing gates the photograph on the same condition; verify.json pdpCopy.credit=false on all 30 renders confirms the branch was never exercised.", "fix": "In the fallback branch, after resolving `ens_pictured`, add `if ens_pictured == blank → assign ens_kind = ''` (no photograph rather than an uncredited one), and make the schema info text say the photo only shows when the pictured product is live."}, "vote": {"refuted": false, "confidence": 0.7, "reason": "Evidence holds in the file: /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid lines 210–214 commit the collection photograph (ens_kind = 'picker'/'asset') unconditionally inside the scene_fallback branch, while the credit at line 215 is gated by `ens_pictured != blank`; ens_pictured comes only from the empty-by-default pictured_* settings (177/186/196/205) or `all_products['…']` (179/188/198/207), which is an EmptyDrop (== blank) for an unpublished or deleted handle. Nothing at line 325 (`if ens_kind != ''`) or 336 (`if ens_credit`) stops a sibling product's frame (ens-pdp-scene-path.jpg = the stainless light, per SPEC §3) rendering with no «בתמונה:» line in that state. verify.json confirms the branch is dormant (pdpCopy.credit=false and pdpSections[*].credits=0 on all 30 renders; product.elmsnest.json sets no scene_fallback/pictured_*), which the engineer already conceded — no render shows it, but the instruction allows confirmation in the file. Scope holds: SPEC §1 A rule 4 defines the option as photograph plus credit with a single stated exception (pictured IS the page product) and never addresses a blank pictured product, so this is a gap, not a recorded decision; BRIEF §3 image rule (3), binding via pdp2 BRIEF §3, says a photograph of another store product must carry the credit line \"or must not use it. Never imply.\" — the code path violates the second half. Not Kalles, product data or an owner open item. Caveat on severity only: it needs the option switched on AND the default product retired AND pictured_* left empty, so it is a low-probability latent defect — \"low\" fits better than \"medium\" — but the defect and the one-line guard fix (ens_kind = '' when ens_pictured is blank) are both correct."}}
{"lens": "engineer", "finding": {"id": "E4", "severity": "low", "where": "/home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid lines 256–262 (the «pre-cropped file narrower than 900» rule) with the dual-head entry lines 113–117 (930×1254)", "what": "The own-width candidate is added only when `ens_w < 900`, so the 930 px dual-head file offers 600w and 900w and never its own 930w; at 1366 the browser takes the 900 candidate and upscales ×1.52, beyond the ×1.1–1.5 the SPEC §3 row accepted for that file. The 854 (warm-step) and 1000 (wall-6w, SPEC chose 900 as largest) cases are correct.", "evidence": "Liquid: for ens_w = 930 the loop yields 600, 900 (930 ≤ 1254 fails for the 1254 candidate), then `if ens_w < 900` is false → no 930w; not visible in verify.json since the dual-head page was not mirrored.", "fix": "Add the own width whenever it is below 1254 and not already a candidate, exempting 1000 if the SPEC's «900 only for the 1000 px source» choice stands: e.g. `if ens_w < 1254 and ens_w != 900 and ens_w != 1000` (or drop the 1000 exemption and let the 6w file offer 1000w too)."}, "vote": {"refuted": false, "confidence": 0.6, "reason": "Evidence holds in the file. /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid lines 113–117 set ens_w = 930 for the dual-head; the candidate loop (lines 244–255, `if wn <= ens_w` over 600/900/1254) yields 600w and 900w; the own-width branch (line 257) is `if ens_w < 900`, false for 930, so no 930w is emitted. The asset really is 930×1254 (header of theme/assets/ens-pdp-dual-head.jpg; CHOSEN.md line 11). With `sizes=\"100vw\"` (line 330) at 1366 the browser takes the 900 candidate: 1366/900 = ×1.52, whereas the 930w the SPEC's row assumed would be ×1.47. The SPEC §3 dual-head row (line 135) states «renders … at 1366 like every other 1254 source, ×1.1–1.5», so the deployed srcset misses that promise by a hair; the 854 (warm-step gets 854w via the `< 900` branch) and 1000 (SPEC line 134: «the 900 candidate is the largest») cases are handled as the critic says. The deployed code is this code path — the mirrored pages report exactly the loop's «600w/900w/1254w» for 1254 sources (verify.json pdp-path-d-js pdpSections[0]). Caveats that keep confidence moderate: (a) the dual-head page was not mirrored (verify.json only lists it as a related-grid card, line 786), so the ×1.52 is computed, not seen; (b) SPEC line 160 «a pre-cropped file's candidates stop at its own width» can be read literally as 600/900/1254 capped at the file width, which the code satisfies — the only explicit contradiction is the row's «×1.1–1.5» figure and the section's own comment (line 256); (c) 900 vs 930 px is a 3 % width difference, not visible in practice. Inside the contract (SPEC §1 A srcset rule, §3 map row), correctly rated low; the proposed fix (`ens_w < 1254 and ens_w != 900 and ens_w != 1000`) is sound."}}
{"lens": "engineer", "finding": {"id": "E5", "severity": "low", "where": "/home/user/ElmsNest/brief/side-pages/pdp2/verify-after/verify.json entries pdp-path-s-js (screens 6.54, overTarget true), pdp-rope-s-js (6.30), pdp-wall-s-js (6.31); verify-before: pdp-wall-s-js 5.38", "what": "At 360×640 the verify tool's own target (6) is exceeded on three of five pages, and the wall page crossed it in this round (+596 px = the scene section). SPEC P5 defines the cap at 390×844 only, where every page is ≤ 5.01, so this is not a SPEC failure — but it is the first time an ens round leaves `overTarget: true` in a verify.json, and the DEPLOY-LOG does not mention it.", "evidence": "verify-after.log lines «pdp-path-s-js screens=6.54/6», «pdp-rope-s-js screens=6.3/6», «pdp-wall-s-js screens=6.31/6»; before/after delta on pdp-wall-s-js 3440 → 4036 px.", "fix": "Record the 360×640 numbers and the P5 scope explicitly in DEPLOY-LOG/the report; if the lead wants the small viewport under 6, the only lever inside the SPEC is the photograph height (e.g. 400 px below 380 px width) — otherwise leave as is."}, "vote": {"refuted": true, "confidence": 0.82, "reason": "The raw numbers check out but the finding fails on scope and on its one novel factual claim. (1) Evidence: verify-after/verify.json does hold pdp-path-s-js screens 6.54, pdp-rope-s-js 6.30, pdp-wall-s-js 6.31 with overTarget:true, and verify-before pdp-wall-s-js is 5.38 (delta ≈ 0.93 × 640 ≈ 595 px = the 596 px scene section) — that part is real. But the claim that this is «the first time an ens round leaves overTarget:true in a verify.json» is false: /home/user/ElmsNest/brief/side-pages/simplify/verify-after/verify.json already carries four overTarget:true entries at 360×640 (home-s-js/nojs 8.43 vs target 7.5, collection-all-s-js/nojs 9.49 vs 8), so the pdp2 round introduces nothing new in kind. (2) Scope: the contract binds the screen cap at 390×844 only — pdp2 SPEC §5 line 181 «PDP ≤ 6 screens at 390×844», and the owner's standing rule simplify/SPEC.md line 357: «The screen targets bind at 390×844 (P5); 360×640 and 1366×900 are recorded for information.» The overTarget flag is simply computed by verify-mirror.js (line 148) for every viewport, so a true value at 360 is the recorded-for-information case the SPEC describes, not a failure. At 390 every page is ≤ 5.01 (DEPLOY-LOG lines 45–46), as the critic concedes. What remains is a documentation preference (mention the 360 numbers in DEPLOY-LOG), which SPEC §5 does not require and which is not a defect in the deployed page; the proposed CSS lever (400 px photo below 380 px) would alter a SPEC-fixed dimension for a viewport the SPEC explicitly does not cap."}}
{"lens": "engineer", "finding": {"id": "E6", "severity": "low", "where": "/home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid line 324 and /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-dusk.liquid line 96: `style=\"scroll-margin-top:90px\"`", "what": "Both section roots use the physical `scroll-margin-top` while SPEC §4 says «logical properties only»; every other CSS rule in both files is logical. It is the same inline attribute every existing ens section carries (home-band, fit, products, terms…), so it is a house pattern rather than a regression, and it has no visible effect on the renders.", "evidence": "grep scroll-margin theme/sections/elmsnest-s-*.liquid → 9 sections, all `scroll-margin-top:90px`; mirrors show the attribute verbatim on both new roots.", "fix": "Use `scroll-margin-block-start:90px` in the two new files (harmless either way for a vertical-writing-mode-free theme), or amend SPEC §4 to name this attribute as the accepted house exception."}, "vote": {"refuted": true, "confidence": 0.9, "reason": "Evidence check: the attribute is really there — /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-scene.liquid:324 and /home/user/ElmsNest/theme/sections/elmsnest-s-pdp-dusk.liquid:96 both carry `style=\"scroll-margin-top:90px\"`, the five mirrors under verify-after/mirrors/*/index.html show it verbatim, and grep finds it on all 9 ens sections. But it is not a contract violation; it is the contract. (a) The master brief mandates this exact property: /home/user/ElmsNest/brief/BRIEF.md line 111 «Header is sticky, ~90px. Use `scroll-margin-top: 90px` on anchored sections.» (b) SPEC §4 names `elmsnest-s-fit` as the model for the section root and its `<style>`, and elmsnest-s-fit.liquid:8 carries the same inline attribute; the «logical properties only» clause sits in the sentence about the `<style>` inside the section, i.e. the CSS rules, which in both new files are indeed all logical (grep for physical margin/padding/inset in the style blocks returns nothing). (c) SPEC §4's own acceptance is «Lint clean (`python3 brief/lint.py theme`)»: /home/user/ElmsNest/brief/lint.py line 63 REQUIRES the literal `scroll-margin-top` on every section («no scroll-margin-top for the sticky header» error otherwise), and its physical-property check (line 86) deliberately scans only `{% stylesheet %}`/`<style>` blocks, not the inline attribute; the run I did prints «LINT OK (0 issues)». The critic's proposed fix (`scroll-margin-block-start`) would therefore make lint FAIL and break SPEC §4's acceptance, while having no visible effect (the critic concedes this). No render is affected. Out of scope as a defect; at most it is a wording nit for the SPEC author, not a flaw in the deployed result."}}
