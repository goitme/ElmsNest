

===== CRITIC ? =====
VERDICT: The page structures in §6–§8 are the right shape (one grid, one buy box, one strip) and the mobile budgets hold for home (≈4.7 screens) and the PDP (≈4.9–5.6), with the collection sitting at ≈7.9–8.2 screens once the 849 px footer the spec forgot is counted. It is not buildable or verifiable as written, though, because §8 and §11 assert things about stock Kalles 5.4.2 that its source contradicts: main-product renders a SECOND /cart/add form for the sticky bar (with its own quantity stepper), the variant picker is JS-only with the form's id input disabled (no-JS add-to-cart fails for 20 of 27 products), the sort control is a JS popover of buttons listing all eight Shopify sort options (including «הנמכרים ביותר»), the add-to-cart label in he.json is «הוסיפו להזמנה» not «הוספה לסל», the breadcrumb never shows the collection because remove_collection_url is true, related-products only renders as a carousel, and the "existing" per-unit snippet never prints a per-metre figure. The single biggest risk to the owner's verdict is §8.3: rendering the .elms-sales description OPEN drops a cream, rounded, Rubik-font sales card with its own marketing CTA into the night PDP and repeats the spec bullets and the variant list a second and third time (a 16–30-row read-only variant table for the string-light products) — exactly the "twice, and complicated" he rejected. Fix the eight Kalles-fact defects, cut the variant table and the open description, and the spec is one honest round from shippable.

[blocker] §8 (end) + §11 — exactly 1 form[action*="/cart/add"]; sticky bar reuses it
  PROBLEM: Stock main-product renders a second, independent product form for the sticky bar (its own hidden id, its own quantity stepper via product-qty-selector, its own submit). With sticky_atc true the page has 2 /cart/add forms and 2 quantity steppers, so the §11 check fails on every PDP and the 'quantity once' promise of P2 is broken. Also `hdt_show_sticky_atc: true` is not a valid value — it is a select with values hdt_show_always / hdt_show_scrolls_outside_the_scope_of_the_form.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\sections\main-product.liquid:37-149 (line 77 `{%- form 'product', product, id: product_form_id_sticky …-%}`, line 132 `render 'product-qty-selector'`), schema lines 181-222; the in-flow form is C:\Users\covid\Desktop\claude\ElmsNest Theme\blocks\_product-buy-button.liquid:68
  FIX: Either (a) keep the sticky bar and rewrite the criterion as `form.hdt-main-product-form` = 1 AND `hdt-sticky-btn-atc form` = 1, hide `.hdt-sticky-atc__qty-selector` in the skin (§5.1) and set `hdt_show_sticky_atc: "hdt_show_scrolls_outside_the_scope_of_the_form"` (the pre-env2 value, stock product.elmsnest.json line 251); or (b) `sticky_atc: false` — the buy button is on screen 1–2 anyway — and keep the literal 'exactly 1 form' test. Pick one and say which in §8 and §11.

[blocker] §7 (sort select) + P1 'Sort = a native select' + P6 'Sorting works with ?sort_by='
  PROBLEM: Stock main-collection's sort is `sort-by-popover`: a <button> opening a <dialog> with <hdt-richlist> of <button type=button value=…> — JS-only, no <select>, no ?sort_by links, and it lists every entry of collection.sort_options (8: incl. «הנמכרים ביותר» best-selling, a ranking claim P4 forbids for a store with zero sales, and two date sorts). The spec's 'must offer exactly מומלץ · מחיר ↑ · מחיר ↓ · א–ב' has no file in §10 that could deliver it.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\sections\main-collection.liquid:192-196 renders 'sort-by-popover'; C:\Users\covid\Desktop\claude\ElmsNest Theme\snippets\sort-by-popover.liquid:18-42 (line 33-35 buttons, no <select>, no href)
  FIX: Add `snippets/elmsnest-s-sort.liquid` to §10: a native `<form method=get><select name="sort_by" onchange="this.form.submit()">` built from `collection.sort_options` filtered to values manual, price-ascending, price-descending, title-ascending, with a `<noscript><button>` submit; render it from `elmsnest-s-coll-header` (or a `_liquid`-style spot) and set `enable_sorting: false` on main-collection so the popover never renders. Then P1/P6/§7 are true.

[high] P6 + §11 'the form still posts the right id (JS off)'
  PROBLEM: With a variant-picker block present, Kalles DISABLES the form's hidden `id` input (`disabled` when product.has_only_default_variant == false), the picker element is `no-js-hidden`, and the <noscript> variant <select> is commented out. Without JS the form posts option values but no `id`, so add-to-cart fails for all 20 multi-variant products; with JS off the shopper also cannot choose a variant at all. The §11 JS-off assertion will fail on rope/path/net.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\blocks\_product-buy-button.liquid:69 (`disabled` on name=id); C:\Users\covid\Desktop\claude\ElmsNest Theme\blocks\_product-variant-picker.liquid:65 (`no-js-hidden`), 145-185 (noscript select inside {% comment %})
  FIX: Add a ≤15-line `_liquid` block after the picker: `<noscript><select name="id" form="product-form-{{ section.id }}{{ product.id }}">…variant.title – price…</select></noscript>` (the commented-out Kalles markup, lines 149-182, is copy-paste ready). Or drop the JS-off variant claim from P6/§11 and test only 'form exists and posts the default id'. State which.

[high] §8.3 facts — 'the product description OPEN (the .elms-sales HTML) with a «קרא עוד» clamp at 8 lines'
  PROBLEM: The stored description is a self-styled cream sales page: `<div class="elms-sales">` with an inline `<style>` (paper #FFFDF7→#F7F0E6 gradient, border-radius 22px, Rubik, gold CTA footer «אל תתנו לשקע לקבוע איפה תיגמר האווירה», 2-column benefit cards) and it re-lists the «פרטים שכדאי לדעת» bullets that the <dl> above it is built from. Rendering it open on the night PDP puts a bright rounded cream card with a marketing CTA in the middle of the page (violates §4 tokens: radius 0, Heebo/Frank Ruhl, #020306 ground; and P4 'no baked-in text of ours' in spirit) and answers 'specs' and 'which lengths' a second and third time — the owner's exact complaint. A -webkit-line-clamp cannot clamp a multi-block container anyway.
  PROOF: Live descriptionHtml of solar-rope-string-lights (Admin API, 2026-09-05): header/hero/promise/benefits/choice/specs/cta sections + `<style>.elms-sales{…border-radius:22px;background:linear-gradient(180deg,var(--paper),var(--cream))…}`; the specs <ul> lists 7 מ׳/50, 12/100, 22/200, 32/300 — the same values as the variant pills and the <dl>
  FIX: Keep the description behind a native <details> «כל מה שכתוב על המוצר» (as env2 did) OR render it open but strip `.elms-sales__specs` and `.elms-sales__cta` and neutralise the card in the skin (`.elms-sales{background:transparent;border:0;border-radius:0;color:inherit;font-family:inherit}`, hide `.elms-sales__kicker`). Replace the 8-line clamp with `max-height + fade + button`. Say which in §8.3.

[high] §8.3 'read-only variant table when the product has > 1 variant: variant title · price · ≈ per unit' vs P2 and P5
  PROBLEM: Variants are already chosen in the pills (§8.2 #5), priced in the price block and in the sticky bar. A table of every variant is the PDP ledger the owner rejected, minus buttons. For the decor products it is huge: decorative-led-net-lights 30 variants, led-globe-string-lights 30, solar-crystal-ball 24, solar-rope 16 → 16–30 rows × ~40 px = 0.8–1.4 screens on its own, pushing 'facts ~1.6 screens' to ~2.5 and the net-lights PDP toward 6 screens.
  PROOF: Admin API variantsCount 2026-09-05: net 30, globe 30, crystal 24, rope 16, waterproof-wall 8, step-deck 8; SPEC §8.3 lines 224-226; §3 row 'How do I choose length / colour' → 'variant picker' (one answer)
  FIX: Delete the table. If a per-length price overview is wanted, it belongs in the per-unit caption (§8.2 #4) which updates with the pills. Recompute §8.3 to ~1.0 screen (numeral + dl + collapsed description).

[high] §8.2 #4 per-unit caption 'the EXISTING snippet elmsnest-pdp-unit-price … verify it prints ≈ X ₪ למטר / ליחידה' + §3 row 'cost per metre / per unit'
  PROBLEM: Verified: the snippet renders ONLY for products with an option literally named «כמות», prints `<price> · <price/qty> ליחידה` (repeating the full variant price the price block just showed), never «למטר», and parses qty with `split ' ' | first | plus 0`, so «יחידה אחת»/«זוג» (modern-led-wall-light-6w-up-down) yield 0 and no line. The 5 length products (rope 7–32 m, crystal, globe, edison, net) — the ones where per-metre matters — get no caption. The §3 question is unanswered where it counts and half-duplicated where it renders.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\snippets\elmsnest-pdp-unit-price.liquid:17-37 (option name == 'כמות', line 30 split, line 35 output); Admin API options 2026-09-05: rope/crystal/globe/edison option «אורך ומספר נורות» values «7 מ׳ / 50 נורות»…, net «גודל» «1.5×1.5 מ׳ / 96 LED»
  FIX: Write `snippets/elmsnest-s-pdp-unit.liquid` (add to §10): pre-render one line per variant — parse the first number before «מ׳» → «≈ X ₪ למטר», else the number before «יח»/«יחידות» or «זוג»=2 → «≈ X ₪ ליחידה», nothing for qty 1 — and reuse the existing `variant:change` listener on `form.hdt-main-product-form` (lines 40-64 of the old snippet) to swap lines. Never repeat the full price in the caption.

[high] P4 'CTA vocabulary, site-wide, exactly: «הוספה לסל» (PDP form)' + §10 'all Kalles files untouched'
  PROBLEM: The stock buy button and the sticky bar print `products.product.add_to_cart` from locales/he.json, which is «הוסיפו להזמנה» (and the drawer's key is «הוספה לעגלה»). No skin, snippet or template setting changes that string; P4 cannot be met without editing a Kalles file, which §10 forbids.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\locales\he.json:462 (`"add_to_cart": "הוסיפו להזמנה"`), :4501 («הוספה לעגלה»); C:\Users\covid\Desktop\claude\ElmsNest Theme\blocks\_product-buy-button.liquid:94; sections\main-product.liquid:137
  FIX: Add `locales/he.json` to §10 'Edited' with exactly the two keys `products.product.add_to_cart` → «הוספה לסל» (and line 4501 for consistency), or relax P4 to the Kalles label. Note that `themeFilesUpsert` must send the whole file — read it first.

[medium] P2 'The four places appear once (+ header menu + footer)' vs §6.1/§6.2/§6.4
  PROBLEM: On the home the four places are named three times in the body: the kept hero lead («מנורות שביל, קיר, מרפסת וגינה» — §6.1 says keep the current copy), the collection tiles (place word in gold above each title), and the fit block (place word in gold per row). Same failure mode the audits counted as 'five times' before.
  PROOF: SPEC §6.1 line 149 'keep the current copy'; C:\…\scratchpad\repo\theme\templates\index.json env2_hero.lead; SPEC §6.2 line 152, §6.4 line 159
  FIX: Tiles = image + Shopify title + count only (no place word); the fit block is the single place where the four place words appear with their yes/no lines; either keep the hero lead as prose (it is a sentence, not a list) or shorten it to one clause without the enumeration. Add the tile/fit rule to §3's 'Which four places' row.

[medium] P2 'The photo/email CTA appears once in the body (+ footer)' vs §5.3 row 4, §6.4, §7.3, footer-group.json
  PROBLEM: On home and collection the photo promise appears three times: the contact line (fit block / guide strip, mailto), the terms strip row 4 «1 תמונה — שולחים תמונה של המקום במייל…» (same promise, unlinked), and the footer's «לשליחת תמונה של המקום» which links to /pages/contact-us, not the mailto. verify.js's `a[href^=mailto]` in <main> = 1 passes while the shopper still reads the promise three times. P2 also claims the footer is 'which we do not control' — `sections/footer-group.json` is our file (night scheme, our copy).
  PROOF: SPEC §5.3 line 130; §6.4 line 160; §7.3 line 182; C:\…\scratchpad\repo\theme\sections\footer-group.json footer_contact.links text (`<a href="/pages/contact-us">לשליחת תמונה של המקום</a>`) and footer_about («כתבו לנו: info@elmsnest.com» plain text)
  FIX: Make the strip three numbers (0 / 8–17 / 14) and let the contact line (5.4) be the only photo promise in the body; in footer-group.json turn the contact column into ONE mailto line built from the same subject/body as elmsnest-v2-photo-url (or drop it), and count it in §11 as 'footer mailto = 1'. Correct P2: header/footer groups are ours.

[medium] §1 #4 (binding: 'the place word is a one-word subtitle') vs §6.2 / §7.1 ('place word small in gold ABOVE the Shopify title', 'gold eyebrow above it')
  PROBLEM: The spec contradicts the owner's binding answer, and the eyebrow reads wrong on two of four: «גינה» sits above «ספוטים, פרוז׳קטורים ותאורה ניידת» while the first collection's title «תאורת שביל, עמוד וגינה» itself contains גינה; «שביל» above a title that already says שביל is noise. A gold word above an h1 reads as a category/breadcrumb, which is how a Kalles shopper will parse it — and it points at the wrong collection for spotlights.
  PROOF: SPEC §1 line 29; §6.2 line 152; §7.1 line 170; Admin API titles 2026-09-05: 'תאורת שביל, עמוד וגינה', 'ספוטים, פרוז׳קטורים ותאורה ניידת'
  FIX: Title first (h1 / tile title), place word as a muted one-word subtitle under it, and omit it where the title already contains the word (path, and arguably wall). On the collection header the deck line already opens with the place ('מתאים כדי…'), so the subtitle can be dropped there entirely.

[medium] §7.1 FILTER ROW — 'hairline pills «הכל» + the four collection titles'
  PROBLEM: Five pills with the full titles («ספוטים, פרוז׳קטורים ותאורה ניידת» = 32 chars ≈ 260 px at 15 px Heebo) cannot sit in one row at 390 px; the spec does not say wrap or scroll. Wrapped, it is 3–4 rows ≈ 150–190 px (fine, but not the 'row' the copy promises); as a horizontal scroller it is a novel-ish control on a page that must be boring. 'הכל + four titles' is otherwise clear — it is the Dawn/Kalles 'collection chips' pattern.
  PROOF: SPEC §7.1 lines 173-174; Admin API collection titles 2026-09-05; card/pill width from search-m-s01/s02 slices (390 wide, 15 px gutters)
  FIX: Specify: wrap, right-aligned, 44 px tall pills, current one gold-filled, no scroller; budget it at 0.2 screen. If one row is wanted, allow the short forms the footer already uses («שביל, עמוד וגינה» · «תאורת קיר» · «ספוטים ופרוז׳קטורים» · «גרילנדות ודקורטיבי») — but then align the footer and menu to the same short forms so there is one vocabulary.

[medium] §8.1 breadcrumb «דף הבית › {collection title} › {product}»
  PROBLEM: settings_data has `remove_collection_url: true`, so brc-nav-product never assigns the fallback collection and renders «בית › {product}» (label from he.json is «בית», not «דף הבית»); the prev/next arrows also never appear. The crumb will not name the collection on any PDP.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\sections\brc-nav-product.liquid:24-33 (`if settings.remove_collection_url == false`), :45 (`products.breadcrumb.home`); he.json:681 («בית»); C:\…\scratchpad\repo\theme\config\settings_data.json:95 (`remove_collection_url: true`)
  FIX: Either drop brc-nav-product (the kicker + header menu already answer 'where am I'; saves ~60 px and one section) or write the real output «בית › {product}» into §8.1/§3. Do not flip remove_collection_url — it changes product URLs site-wide.

[medium] §8.4 related-products: display_type 'grid' (no carousel on mobile), collection = auto (same collection)
  PROBLEM: `display_type` only accepts '1'..'4' ('grid' is not a value) and every branch renders inside `<hdt-slider>` — related-products has no grid mode, so on mobile 4 cards are a 2-slide carousel with dots. With collection blank, type '1' uses `product.collections.first`, which for waterproof-solar-deck-step-lights is the `sale` collection (1 product = itself) → empty related.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\sections\related-products.liquid:58-72 (case '1'..'4'), :77 and :99 (`<hdt-slider …>` in both branches), :235-258 (option values); Admin API 2026-09-05: waterproof-solar-deck-step-lights collections = [תאורת-שביל-סולארית, sale]
  FIX: Reuse the new `elmsnest-s-products` section (§6.3) on the PDP with a `source: same_place` mode: resolve the place collection via elmsnest-s-place (ignores `sale`), skip the current product, take 4, render card-product1 in a plain 2/4-col grid. One card grid everywhere, no slider JS to fight. Remove related-products from §8.4.

[medium] P5 collection ≤ 8 screens + §7 'Expected ≈ 7 screens'
  PROBLEM: The ≈7 omits the footer (797 + 52 = 849 px = 1.0 screen on mobile, measured). Recount at 390×844: header ≈650 (42 vh photo + h1 + lines + 3 rows of pills), sort ≈60, grid 14 rows × ~320 px (search-m slices show a 317 px row pitch for the stock card at 4/5) ≈ 4500, guide strip ≈200, terms ≈420, footer 849 → ≈6.7 k px ≈ 7.9–8.2 screens. The target holds only if nothing grows (hover image, pill wrap, card padding).
  PROOF: C:\…\scratchpad\shots\report.json collection-all-m secs footer_KwXhWr h 797 + footer_bottom h 52; shots\slices\search-m-s02.png (image tops 48→365); base.css:1899 `.hdt-ratio--portrait{--ratio-percent:125%}`
  FIX: Count the footer in §7's estimate. Cheapest 0.5-screen lever: drop the terms strip from the collection (BRIEF §3 only requires the terms findable on the HOMEPAGE; the PDP has its one-liner) and fold «למדריך המלא» into the header deck line so the guide strip is one row. Then ≈7.3 screens with margin.

[medium] §11 verification checks (img src, svg plates, PDP sample)
  PROBLEM: (1) 'every .hdt-card-product img src contains the featured filename' fails: `show_secondary_image` defaults true, so each card also carries a hover <img> of media[1]. (2) '0 svg plates' is undefined — every Kalles card/breadcrumb/sort control contains inline <svg> icons. (3) The PDP sample is unspecified; the 30-variant products are where the buy box and facts blow up.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\snippets\card-product-media.liquid:27-47 (hover media); config\settings_schema.json:1352-1354 (`show_secondary_image` default true; not overridden in settings_data); report.json already carries a `bigSvgs` metric
  FIX: Check `.hdt-card-product__media--main[src]` only (or hide `.hdt-card-product__media--hover` in the skin and note the lazy request still fires); define plates as `svg` ≥ 100×100 px inside `.hdt-card-product__media` (the audit's bigSvgs rule); run the PDP checks on solar-rope-string-lights (16), decorative-led-net-lights (30) and stainless-steel-solar-path-light-ip65 (1).

[medium] footer-group.json (owned, listed as untouched) — dead anchor and third naming vocabulary
  PROBLEM: footer_bottom links «תנאי משלוח וביטול» to `/#env2-terms`, an id that leaves templates/index.json in this round → dead anchor on every page. The footer's «קולקציות» column uses a third set of labels («שביל, עמוד וגינה», «ספוטים ופרוז׳קטורים», «גרילנדות ודקורטיבי») against §1 #4 'Label = the Shopify collection title' and the menu. P2 calls the footer 'which we do not control' — it is `repo/theme/sections/footer-group.json`, ours.
  PROOF: C:\…\scratchpad\repo\theme\sections\footer-group.json footer_bottom.text (`<a href="/#env2-terms">`), footer_collections.links.text; SPEC P2 line 44, §10 lines 257-260
  FIX: Add footer-group.json to §10 'Edited': give the new terms section `id="env2-terms"` (keeps the anchor and the rollback trivial) or repoint the link to /policies/shipping-policy; align the four footer labels to the collection titles (same order as the menu, which the API confirms is path → wall → spots → decor).

[low] §5.1 skin — '.hdt-price: the מ־ prefix for price_varies in muted ink'
  PROBLEM: settings_data has `price_varies_style: "1"` → cards print the range «₪69.90 - ₪159.90» (price_range_html), no prefix element exists to style. Style "2" would print «החל מ-₪69.90» (from_price_html), not «מ־», and settings_data is 'untouched' per §10.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\snippets\card-price.liquid:55-62; locales\he.json:557 (price_range_html), :572 (`"from_price_html": "החל מ-{{ price }}"`); settings_data.json:66
  FIX: Either accept the min–max range on cards (honest for a 4-length rope) and delete the מ־ line from §5.1, or add `price_varies_style: "2"` to a one-key settings_data edit and write «החל מ-» into P4's vocabulary.

[low] §8.2 `_product-medias` `show_first_media: false` vs P3 'featured first'
  PROBLEM: `show_first_media: false` makes the gallery start at the selected variant's featured media (`startIndex = selected_variant_featured_media.position - 1`); with `hide_sold_out: true` + `pick_mode: first_available` that can be a variant image that is not position 1, so the first slide is not `product.featured_image` on products with per-variant images.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\blocks\_product-medias.liquid:54; settings_data.json:100-101
  FIX: Set `show_first_media: true` (start on media 1 = featured) and keep `use_select_varaint_change_media: true` so a pill tap still jumps to its image.

[low] §5.3 'Licensed wording, verbatim from sections/elmsnest-v2-terms.liquid schema defaults'
  PROBLEM: Not verbatim, and the source is unsafe: the preset's row 4 reads «שלחו תמונה של המקום בוואטסאפ.» (WhatsApp — banned by §1 #3), and rows 1–3 in the spec are new compressions («משלוח חינם לנקודת איסוף · משלוח עד הבית 29.90 ₪» vs preset «משלוח לנקודת איסוף — חינם. משלוח עד הבית: 29.90 ₪.»). P4 forbids 'baked-in text of ours' while §5.3 introduces some.
  PROOF: C:\…\scratchpad\repo\theme\sections\elmsnest-v2-terms.liquid:201-208 (preset, line 204 «בוואטסאפ»); index.json env2_terms blocks (the owner-seen copy); SPEC §5.3 lines 126-130
  FIX: Cite templates/index.json env2_terms line_* as the source, copy `title` verbatim for each row and drop the `text` sentences (they are the 'small print'), and mark row 4 (if kept) as a rewritten email line pending owner OK.

[low] §6.1 hero 'keep the current copy' — the note «השעה הכחולה … הדף הזה מחשיך איתן, וכל מנורה נדלקת כשמגיעים אליה»
  PROBLEM: That sentence describes the page-wide lights-on-scroll device, which §4 now confines to the hero and P3 bans on product images. Keeping it makes the first screen promise an effect that no longer exists, and it is an editorial device of the kind the owner called 'معقد'.
  PROOF: C:\…\scratchpad\repo\theme\templates\index.json env2_hero.note_title / note_text; SPEC §4 line 91, P3 line 48
  FIX: Set note_title/note_text to blank in index.json (the section already hides an empty note, hero line 101) — no section edit needed. Same for `cta_secondary_label: ""` (hero line 115 hides an empty label), which makes the new `show_secondary_cta` setting redundant; only `show_card` is needed.

[low] §8.3 'the big outlined numeral (IP65 / the LED count / the wattage — derived exactly as elmsnest-v2-pdp-facts derives it)'
  PROBLEM: env2-facts derives the IP code first, else an hours figure parsed from «שעות עבודה…» sentences — never LED count or wattage. Copying it 'exactly' cannot produce the numerals the spec names, and for the 5 products with no IP code and no hours line the numeral is blank (fine) or a bare hours number with a quoted sub-line (puzzling for a first-time shopper).
  PROOF: C:\…\scratchpad\repo\theme\sections\elmsnest-v2-pdp-facts.liquid:273-302 (IP from `latin` list, else hours from «שעות»), :122 (latin list)
  FIX: Rewrite §8.3 as 'IP code if present, else nothing' and always print a 4-word caption under it («עמידות למים ואבק» is licensed by the IP65 bullet); drop the hours branch so the section never shows a giant number without a noun.

[low] P4 'no counts' vs §6.2 «N מוצרים» and §7.1 «{count} מוצרים · {price_min}–{price_max} ₪»
  PROBLEM: P4 forbids counts while two sections render counts; the acceptance rule contradicts the build spec.
  PROOF: SPEC P4 line 52; §6.2 line 153; §7.1 line 171
  FIX: Change P4 to 'no review, sales or popularity counts'; product counts and the price range are catalogue facts (BRIEF §2).

[low] §5.1 skin — controls the spec does not name
  PROBLEM: Card-product1 also renders the `btn-ultra` 'בחירת אפשרויות' overlay (show_ultra_btn defaults true) — a buy control on the card against P4 'cards carry NO button'; core only hides it ≤900 px / no-hover. Kalles pills are ~36–40 px tall (P6 wants 44). The env2 ground-product still reserves `padding-block-end:78px` on mobile for the removed env2 buybar.
  PROOF: C:\Users\covid\Desktop\claude\ElmsNest Theme\config\settings_schema.json:1436-1439; C:\…\scratchpad\repo\theme\snippets\elmsnest-v2-core.liquid:110-111; snippets\elmsnest-v2-ground-product.liquid:44-46
  FIX: Add to §5.1: `.hdt-card-product__btn-ultra,.hdt-card-product__media--hover{display:none}`, `.hdt-product-form__values label{min-height:44px}`, and override `html body.hdt-page-type-product{padding-block-end:0}` (or let the Kalles sticky bar's own observer reserve space).


===== CRITIC ? =====
VERDICT: Buildable in structure, but not as written in three places the spec treats as settled: (1) the stock Kalles sort is a JS popover of eight buttons (not a native select, no ?sort_by= path, and it prints «הנמכרים ביותר»), (2) the "existing" per-unit snippet never prints «≈ … למטר» and is silent for every length-option product, and (3) the fit copy in §5.2 is glued from fragments («מתאים כדי הארה ממוקדת…», «כש־המקום…») and drops env2's solar gate, so the PDP will print a false «לא מתאים כש…» on the five non-solar wall lights. The owner's "repeated twice" verdict is also not fully met: the photo promise still appears twice in the body of home and collection (terms row 4 + the 5.4 contact line), and the home repeats «לפי המקום» and «לפני שתזמינו/שמזמינים» across consecutive sections. Nothing in the spec says or implies WhatsApp on the storefront; the only latent risk is the photo-url builder's wa.me branch versus the invented word «במייל» in the terms row. Single biggest risk: a P4 violation on the PDP (an untrue "does not suit" sentence on 220V/rechargeable products) shipped under a heading that promises honesty. Paths below are relative to the scratchpad repo (theme/, brief/) and to the stock Kalles copy (ElmsNest Theme/).

[high] §5.2, §6.4, §7.1, §8.2.1 — garden 'yes' phrase after «מתאים כדי»
  PROBLEM: «מתאים כדי {yes}» with the garden pair's «הארה ממוקדת של עץ או ערוגה» is ungrammatical Hebrew (כדי requires an infinitive, not a noun phrase). It renders on the home fit row, the spot collection header deck line and every spot-collection PDP kicker («גינה · מתאים כדי הארה ממוקדת של עץ או ערוגה»).
  PROOF: SPEC.md:118 (the pair); env2 already licensed the infinitive form: theme/sections/elmsnest-v2-coll-scene.liquid:107 «מתאים כדי להאיר עץ או ערוגה», theme/sections/elmsnest-v2-pdp-stage.liquid:136, theme/sections/elmsnest-v2-pdp-fit.liquid:597 (allowed list: לראות את הדרך · להאיר נקודה מסוימת · ליצור אווירה · להאיר עץ או ערוגה)
  FIX: In 5.2 set the spot collection's emit:'yes' to «להאיר עץ או ערוגה» (BRIEF's noun form stays the source; the infinitive is the env2-licensed compression). Add a line to P4: the yes-phrase is always an infinitive so «מתאים כדי …» parses.

[high] §5.2, §8.2.1, §8.2.8 — solar gate dropped from the 'no' phrase
  PROBLEM: The spec maps the 'does not suit' phrase by collection handle only. env2-pdp-fit gates three of the four pairs on the product being solar (the path/wall/spot caveats are solar caveats). Five of six wall products have no «סולארי» in their title (6W Up & Down, indoor/outdoor, IP65 6W/12W, magnetic rechargeable, bidirectional), plus the mains bollard, the camping lantern and the 10W dual-head; printing «לא מתאים כשנדרש אור חזק וקבוע לאורך כל הלילה» under a 220V wall light is a false claim (P4) under a heading that promises honesty.
  PROOF: theme/sections/elmsnest-v2-pdp-fit.liquid:98-112 (is_solar test), 121-128 (gate: `elsif is_solar and ptype == 'תאורת קיר'`), 595 (schema info: «שלושה מהארבעה נכונים רק במוצר סולארי»); brief/catalog.json titles for wall/path/spot handles without סולארי (11 products); SPEC.md:115-121, 200-201, 215-217
  FIX: Copy the is_solar test (custom.power_source == 'סולארי', else title+description contains 'סולארי') into elmsnest-s-place; emit:'no' returns blank for non-solar products in the path/wall/spot collections (decor stays ungated). elmsnest-s-pdp-notfor prints only the contact line when blank. The home/collection rows speak about the collection (whose SEO titles are 'סולארית'), so they may keep the pair, but §11 must add: 0 «לא מתאים» lines on the 11 non-solar PDPs.

[medium] §5.2, §6.4, §7.1, §8.2.8 — «לא מתאים כש־{no}» template
  PROBLEM: Gluing the label «כש־» (maqaf) to a full clause produces «כש־המקום כמעט אינו מקבל אור יום», «כש־נדרש…», «כש־נדרשת…», «כש־צריך אור חזק — זו אינה מטרתה» inside running text. The maqaf form was an env2 LABEL with the phrase on a separate visual side; inline it reads as a typo, and the decor row ends up with three separators (· , כש־, —) in one line.
  PROOF: SPEC.md:159, 173, 216 (U+05BE maqaf confirmed); joined licensed sentences already exist: theme/sections/elmsnest-v2-coll-scene.liquid:98 «לא מתאים אם צריך אור חזק — זו אינה מטרתה.», :101 «לא מתאים כשהמקום כמעט אינו מקבל אור יום.», :104 «לא מתאים כשנדרש אור חזק וקבוע לאורך כל הלילה.», :107 «לא מתאים כשנדרשת התקנה מיוחדת או חיבור קבוע.»
  FIX: Make emit:'no' return the four coll-scene sentences verbatim (joined כשה־/כשנ־, and «אם» for the decor pair) and drop the «כש־» template from §6.4/§7.1/§8.2.8; the switch/fit label style stays only where the phrase sits in its own cell.

[medium] §5.3 — 'Licensed wording, verbatim from sections/elmsnest-v2-terms.liquid schema defaults'
  PROBLEM: None of the four rows is verbatim. Row 2 introduces a «+» and semicolons («1–3 ימי טיפול + 7–14 ימי משלוח; ייתכן…») that appear in no licensed string and read as spec notation; row 3 reorders the clause; row 4 is new copy. The claim of verbatim wording is what P4 relies on.
  PROOF: SPEC.md:126-130 vs theme/sections/elmsnest-v2-terms.liquid:203-206 and the already-compact licensed set theme/sections/elmsnest-v2-coll-terms.liquid:355-358 («משלוח לנקודת איסוף — חינם. עד הבית 29.90 ₪.» / «לאספקה: 1–3 ימי טיפול ו־7–14 ימי משלוח. ייתכן משלוח ממחסן מחוץ לישראל.» / «לביטול מקבלת המוצר, לפי חוק הגנת הצרכן. דמי ביטול עד 5% או 100 ₪ — הנמוך מביניהם.» / «שולחים תמונה של המקום ואנחנו בודקים התאמה לפני ההזמנה.»); coll-terms:347 info «זהו נוסח משפטי — אין לקצר אותו»
  FIX: Replace the four row texts with coll-terms.liquid:355-358 verbatim (numeral + unit + headline + sub as there) and change the sentence in §5.3 to 'verbatim from elmsnest-v2-coll-terms presets'. No «+», no «;» anywhere in storefront Hebrew.

[medium] §5.3 row «1 תמונה» — «במייל»
  PROBLEM: «במייל» is a word no licensed string contains: the original says «בוואטסאפ», the compact variants are channel-neutral. Owner answer 3 licenses the mailto LINK, not new copy. Two concrete risks: elmsnest.com has no MX (§1), so the sentence promises a channel that cannot receive; and if settings.whatsapp_number is ever filled, the builder switches every href to wa.me while the row still says «במייל».
  PROOF: SPEC.md:130; theme/sections/elmsnest-v2-terms.liquid:206 («בוואטסאפ»), theme/sections/elmsnest-v2-coll-terms.liquid:358 and elmsnest-v2-coll-goodnight.liquid:432 (channel-neutral), theme/sections/elmsnest-v2-pdp-terms.liquid:287; theme/snippets/elmsnest-v2-photo-url.liquid:19-25 (wa.me branch)
  FIX: Use the channel-neutral coll-terms:358 sentence and let the link carry the channel. If the owner insists on naming it, write «במייל» only in the link's title attribute, never in the sentence.

[high] P2 vs §5.3 + §5.4 + §6.4 + §7.3 — the photo promise twice per page
  PROBLEM: Home and collection each print the promise twice in the body: terms row 4 («שולחים תמונה של המקום…») and the 5.4 contact line in the fit block / guide strip. P2 says once in the body; §11 counts only a[href^=mailto] so the duplicate passes the test. This is the exact 'terms item 1 + goodnight' duplication the audits flagged and the owner's verdict 2.
  PROOF: SPEC.md:41-43 (P2), 130 (row 4), 136-138 (5.4), 160 (fit foot), 181-182 (guide strip); brief/side-pages/simplify/audits.txt lines 23-24 and 181-183 (photo promise counted 4× on home incl. terms row + goodnight)
  FIX: Make terms row 4 BE the contact line: numeral «1 תמונה» + the coll-terms:358 sentence + the single mailto link «לשלוח תמונה של המקום ←»; remove 5.4 from elmsnest-s-fit and elmsnest-s-guide-strip (guide strip keeps only the question + «למדריך…»). §11: count occurrences of the string «תמונה של המקום» in <main> = 1 per page.

[medium] §5.4 — contact line copy
  PROBLEM: «לא בטוחים? שלחו תמונה של המקום ואנחנו נבדוק התאמה לפני שמזמינים.» shifts person mid-sentence (imperative 2nd person «שלחו» → impersonal «שמזמינים»), and the same phrase «תמונה של המקום» is then repeated in the link label ten words later — the repetition pattern the owner objected to.
  PROOF: SPEC.md:136-137; licensed consistent form: theme/sections/elmsnest-v2-pdp-terms.liquid:287 «שלחו תמונה של המקום — נבדוק התאמה לפני שתזמינו.»; theme/sections/elmsnest-v2-pdp-ask.liquid:481 «עוד לא בטוחים?»
  FIX: Sentence: «לא בטוחים? נבדוק התאמה לפני שתזמינו.» + link «לשלוח תמונה של המקום ←» (both halves are licensed fragments; the phrase appears once). On the PDP, where it follows «לא מתאים כש…», keep the same two parts.

[medium] §5.4 — 'prefill the subject with the product title' / mailto body
  PROBLEM: Contradicts the existing builder, which puts the product title in the BODY and keeps a fixed subject; and on home/collection (no product) the builder's default body ends with a dangling «המוצר ששוקלים: ». Subject with a 60-char product title («בדיקת התאמה — מנורת קיר LED עמידה למים IP65 – 6W/12W») is also poor mail UX.
  PROOF: theme/snippets/elmsnest-v2-photo-url.liquid:16-17 (default subject «בדיקת התאמה — תמונה של המקום», body ends «המוצר ששוקלים: »); theme/snippets/elmsnest-v2-pdp-photo-cta.liquid:29-30, 42-45 (title appended to body); theme/sections/elmsnest-v2-coll-terms.liquid:105 and elmsnest-v2-coll-goodnight.liquid:198 override the body to «שלום, מצרף/ת תמונה של המקום שרוצים להאיר.» when there is no product
  FIX: 5.4 passes subject = default; body = «שלום, מצרף/ת תמונה של המקום שרוצים להאיר.» and, only when product is given, appends « המוצר ששוקלים: {{ product.title }}». Reuse elmsnest-v2-pdp-photo-cta's logic rather than a new label path.

[medium] §8.2.7 — PDP terms line
  PROBLEM: «אספקה 8–17 ימי עסקים» drops «משוערת» (an estimate becomes a promise); «ביטול עד 14 יום» drops «מקבלת המוצר» (14 days from receipt, not from order); «לכל התנאים ←» points to /policies/shipping-policy although the line's third item is cancellation (refund-policy). §3's row says '→ links to /pages' while §8.2.7 says /policies.
  PROOF: SPEC.md:78, 212-214; licensed: brief/inventory/theme-src/snippets/elmsnest-pdp-facts.liquid:13 «אספקה משוערת: 8–17 ימי עסקים», elmsnest-pdp-trust.liquid:9 «ניתן לבטל עסקה עד 14 יום מקבלת המוצר»; page «משלוחים והחזרות» = /pages/shipping-delivery (brief/inventory/INVENTORY.md:81, theme/sections/footer-group.json:152)
  FIX: Line: «משלוח חינם לנקודת איסוף · אספקה משוערת <bdi>8–17</bdi> ימי עסקים · ביטול עד <bdi>14</bdi> יום מקבלת המוצר» + «לכל התנאים ←» → /pages/shipping-delivery (covers shipping and returns and matches the footer). Align §3.

[high] P1, P6, §5.1, §7 (sort select)
  PROBLEM: Stock Kalles sorting is not a native select and has no no-JS path: it is a <hdt-popover-sorting> button opening a <dialog> of <button role=option> for EVERY collection.sort_options entry (8), labels from the locale, mobile button reads «מיין». It will show «הנמכרים ביותר» (a best-seller surface, banned by BRIEF §3 with one test order) and its labels differ from the four the spec mandates; §10 forbids editing Kalles files/locales, so the spec's 'must offer' list is unreachable as written.
  PROOF: ElmsNest Theme/snippets/sort-by-popover.liquid:18-42; ElmsNest Theme/sections/main-collection.liquid:193; ElmsNest Theme/locales/he.json:434-441 («מומלץ», «הנמכרים ביותר», «לפי א'-ב' - סדר עולה», «מחיר: נמוך לגבוה»…); brief/inventory/AUDIT-collection.md:45 (mobile control never reflects the active sort); SPEC.md:34-37, 59-60, 103-104, 185
  FIX: Set enable_sorting: false on main-collection and render the sort in elmsnest-s-coll-header as <form method="get"><select name="sort_by" onchange="this.form.submit()"> with exactly four options (manual «מומלץ», price-ascending «מחיר: מהנמוך לגבוה», price-descending «מחיר: מהגבוה לנמוך», title-ascending «א–ב»), selected from collection.sort_by, plus a <noscript> submit. Add to §11: 0 occurrences of «הנמכרים ביותר».

[high] §8.2.4 — 'the EXISTING snippet elmsnest-pdp-unit-price'
  PROBLEM: The existing snippet prints «{price} · {per_unit} ליחידה» ONLY for products with an option literally named «כמות»; it never prints «≈», never «למטר», and renders nothing for the length-option products (rope, edison, net, crystal, globe). The verification step 'verify it prints ≈ X ₪ למטר / ליחידה' cannot pass; the per-metre derivation lives in env2 pdp-ledger, which the spec removes.
  PROOF: brief/inventory/theme-src/snippets/elmsnest-pdp-unit-price.liquid:17-24 (option name == 'כמות' gate), 33-35 (output has no ≈/למטר); SPEC.md:204-207
  FIX: Specify a new snippets/elmsnest-s-pdp-unit.liquid: metres parsed from the length option value («7 מ׳», «5 מטר»), units from «כמות»; output «≈ <bdi dir="ltr">12.84 ₪</bdi> למטר» or «<bdi dir="ltr">84.95 ₪</bdi> ליחידה» (no ≈ when exact); keep the same variant:change wiring on form.hdt-main-product-form (unit-price.liquid:46-48, valid for stock main-product).

[low] §5.1 — «מ־» prefix for price_varies in muted ink
  PROBLEM: With the theme's own settings the card prints «החל מ-89.90 ₪» (ASCII hyphen, «החל מ» not «מ־») as ONE text node, so the prefix cannot be coloured separately by CSS; changing it needs card-price.liquid or the locale (Kalles files) or settings_data — all frozen by §10.
  PROOF: theme/config/settings_data.json:66 price_varies_style "1"; ElmsNest Theme/snippets/card-price.liquid:55-60, 89 (no span around the prefix in the varies branch); ElmsNest Theme/locales/he.json:572 «החל מ-{{ price }}»
  FIX: State the actual string («החל מ-X ₪», all gold) in 5.1 and drop the muted-prefix requirement; if the maqaf matters, the only in-scope route is a locale override, which §10 must then permit explicitly.

[medium] §8.1 — breadcrumb «דף הבית › {collection title} › {product}»
  PROBLEM: brc-nav-product prints «בית» (locale products.breadcrumb.home), and the collection crumb only when the URL is /collections/x/products/y or settings.remove_collection_url is false; the theme has remove_collection_url: true and card-product1 links product.url without `within:`, so every PDP reached from a card shows «בית › {product}» — neither word of the spec's crumb holds. §3 relies on this crumb to answer 'where am I'.
  PROOF: ElmsNest Theme/sections/brc-nav-product.liquid:24-33, 45, 49-55; ElmsNest Theme/locales/he.json:680-681 («בית»); theme/config/settings_data.json:95 remove_collection_url true; ElmsNest Theme/snippets/card-product1.liquid:28 (product_url = card_product.url); brief/inventory/AUDIT-collection.md:12 («בית › …» on collections)
  FIX: Write §8.1 as «בית › {product}» and let the kicker _liquid block print the place + collection link (product.collections.first) — or, if the collection crumb is wanted, render it in the kicker block rather than expecting it from Kalles.

[medium] §8.4 — related-products settings and heading «עוד לאותו מקום»
  PROBLEM: display_type: "grid" is not a valid value; the options are "1" By Collection / "2" Vendor / "3" Type / "4" Dynamic recommendations (default). With the default, products come from Shopify's Recommendations API from any collection, so the heading «עוד לאותו מקום» would be untrue. The `collection` setting is commented out in the schema, so 'collection = auto' is meaningless as a setting.
  PROOF: ElmsNest Theme/sections/related-products.liquid:236-253 (options), 258-262 (`when '1'` uses product.collections.first), 263-268 (collection setting commented out), 388-434 (carousel settings are separate); SPEC.md:228-230
  FIX: display_type: "1"; specify the carousel/grid toggle from lines 388-434 separately; heading stays «עוד לאותו מקום» (natural) — or «עוד מנורות לאותו מקום» if the count is < 4.

[medium] §6.1, §7.1, §1 answer 4 — names for /collections/all and the header's «קולקציות»
  PROBLEM: Four labels for the same destination: hero «לכל המוצרים» (/collections/all), filter pill «הכל», the /all page's own h1 «קטלוג», and the header menu parent «קולקציות» → /collections (the stock cream «קטלוגים» list page, not touched by this round). A shopper who taps the header's own «קולקציות» leaves the night design entirely; answer 4 asked for one naming set and one entry.
  PROOF: SPEC.md:149, 173; brief/inventory/INVENTORY.md:21 (list-collections template, «קטלוגים», broken breadcrumb), :85 (main-menu קולקציות → /collections); brief/inventory/AUDIT-collection.md:27 (/all banner and crumb «קטלוג»)
  FIX: Pill label «כל המוצרים»; on /all override the h1 text with the same words (setting `all_title`, default «כל המוצרים»); add to §9 the owner action 'main-menu: קולקציות → /collections/all' (or bring templates/list-collections.json into scope with a one-line redirect section).

[medium] P2 ('footer, which we do not control') vs §10 and §11
  PROBLEM: The footer is ours (theme/sections/footer-group.json) and contradicts two decisions: its four collection labels are not the Shopify titles («שביל, עמוד וגינה», «ספוטים ופרוז׳קטורים», «גרילנדות ודקורטיבי»), and its © line links «תנאי משלוח וביטול» to /#env2-terms — an anchor that dies when env2-terms leaves index.json. Its photo link reads «לשליחת תמונה של המקום» (→ /pages/contact-us), a different verb form from P4's «לשלוח תמונה של המקום». §11's same-order check excludes the footer.
  PROOF: theme/sections/footer-group.json:101 (names), :203 (photo link), :276 (/#env2-terms); brief/inventory/INVENTORY.md:35; SPEC.md:43-44, 162, 259, 279
  FIX: Add footer-group.json to 'Edited' in §10: names = the four Shopify titles in menu order; © link → /pages/shipping-delivery (or give elmsnest-s-terms id="env2-terms"); photo link label «לשלוח תמונה של המקום». Extend §11's order check to the footer.

[medium] §6.1 'keep the current copy' vs §1 answer 4 'ONE order everywhere'
  PROBLEM: The hero lead lists the places as «מנורות שביל, קיר, מרפסת וגינה» — מרפסת before גינה — while the binding order is שביל → קיר → גינה → מרפסת (tiles, filter row, menu). The audits flagged 'two different orders' on the home as a duplication defect; §11's order check would pass because it ignores the lead.
  PROOF: theme/sections/elmsnest-v2-hero.liquid:238 (lead default); SPEC.md:29, 149, 279; brief/side-pages/simplify/audits.txt line 178-180
  FIX: Set `lead` in index.json to «מנורות שביל, קיר, גינה ומרפסת. קטגוריה אחת בלבד — ואם מוצר לא מתאים למקום שלכם, נגיד את זה לפני שתזמינו.» and include the lead in the §11 order check.

[medium] §6.2, §6.4 headings vs the hero copy — repetition within one screen
  PROBLEM: The hero eyebrow already reads «תאורת חוץ, לפי המקום»; the very next section is headed «לפי המקום». The hero lead already promises «…נגיד את זה לפני שתזמינו», §6.4 restates it as «אנחנו נגיד לכם גם מתי לא.», then the contact line and terms row 4 both end «…לפני שמזמינים» — the 'before you order' promise appears three times on the home.
  PROOF: theme/sections/elmsnest-v2-hero.liquid:235, 238; SPEC.md:150, 158, 136, 130
  FIX: Tiles heading → «איפה צריך אור?» (the store's own question) or «בחרו לפי המקום»; keep «אנחנו נגיד לכם גם מתי לא.» as the fit heading and drop the second sentence of the hero lead (or keep the lead and drop the period-heading); after the 5.4/terms merge (finding above) «לפני ש…» appears once below the hero.

[low] §6–§8 headings — natural Hebrew and punctuation
  PROBLEM: «אנחנו נגיד לכם גם מתי לא.» carries a full stop while «לפי המקום», «מה שנדלק ראשון», «עוד לאותו מקום», «ארבעה מספרים שכדאי לדעת» do not; env2 used the period style consistently. «למדריך המלא ←» implies a shorter guide precedes it — on the collection strip nothing does. Otherwise the headings read as natural Israeli Hebrew, not AI-ish; «מה שנדלק ראשון» is poetic but fine as the store's line.
  PROOF: SPEC.md:150, 154, 158, 160, 181, 230; theme/sections/elmsnest-v2-coll-terms.liquid:327 («ארבעה מספרים, לפני שמשלמים.»)
  FIX: Pick one convention (recommend no terminal period on headings; «אנחנו נגיד לכם גם מתי לא»). Guide link label «למדריך לבחירת תאורה ←» (matches the page title and menu item «מדריך לבחירה»).

[low] §8.3 «קרא עוד» and P4's 'exactly' CTA list
  PROBLEM: «קרא עוד» is masculine-singular imperative in a site written in the plural («שלחו», «בחרו», «לכם», «תזמינו»). P4 says the CTA vocabulary is 'exactly' four phrases, yet the spec itself adds «לכל התנאים ←», «קרא עוד», «הכל», so the rule is self-contradictory and §11 cannot test it.
  PROOF: SPEC.md:53-54, 173, 213, 226
  FIX: «להמשך קריאה» (infinitive, matches «לשלוח…», «למדריך…»). Restate P4 as: buttons use only the four phrases; text links use the infinitive/ל־ form and are listed («לכל התנאים ←», «להמשך קריאה», «למדריך…»).

[medium] P6 <bdi> rule — not carried into §5.3, §7.1, §8.3
  PROBLEM: En-dash ranges flip in RTL («8–17» renders «17–8», «89.90–999.90» renders reversed) — the reason v2-terms auto-wraps digit-leading tokens. §5.3 (three ranges + the numeral «8–17»), §7.1 («{price_min}–{price_max} ₪»), §8.3 (numeral IP65/LED/W, dl values, variant-table titles like «2/4/6 יחידות», the «≈ X ₪» cell) give no wrapping instruction; §8.2.7 does. Single numbers («N מוצרים», «14 יום», «29.90 ₪», «5%») are safe.
  PROOF: theme/sections/elmsnest-v2-terms.liquid:8-10 (comment: ranges flipped in the dusk mockup), 39-55 (auto-bdi loop); theme/snippets/elmsnest-v2-bdi-range.liquid exists; SPEC.md:59-61, 124-131, 169-170, 220-227
  FIX: Add one sentence to §5.3/§7.1/§8.3: every en-dash range and every Latin token is `<bdi dir="ltr">…</bdi>` (reuse elmsnest-v2-bdi-range or copy the v2-terms loop); the collection header line is «<bdi>27</bdi> מוצרים · <bdi dir="ltr">89.90–999.90</bdi> ₪». Add to §11: 0 bare en-dash ranges outside <bdi> in <main>.

[medium] §7.1 — filter row on 390 px
  PROBLEM: Five pills, the longest «ספוטים, פרוז׳קטורים ותאורה ניידת» (32 chars), cannot fit one row on mobile; the stock strip already clips this exact label («ספוטים, פרוז׳קטורינ»). The spec gives no wrap/scroll rule, and a horizontal scroller would hide the current-gold pill.
  PROOF: brief/inventory/INVENTORY.md:17, brief/inventory/AUDIT-collection.md:11 and :129 (ellipsised labels on mobile); SPEC.md:172-174
  FIX: `display:flex; flex-wrap:wrap; gap:8px; min-height:44px` per pill, no overflow-x; budget 2–3 rows inside the 0.9-screen header. Never truncate the titles.

[low] §6.2 — collection tiles at 4/5
  PROBLEM: The wall collection image is 1456×816 landscape; a 4/5 object-fit cover keeps ~35 % of its width and no focal point is specified.
  PROOF: brief/inventory/INVENTORY.md:79 (solar-wall-lights image 1456×816, 'focal point needed'); SPEC.md:151-152
  FIX: Add a per-block `object_position` text setting (default «50% 50%», wall tile «0% 50%» as env2-places used) or use 1/1 tiles.

[low] §5.3 foot line — policy links without labels; page vs policy
  PROBLEM: The foot line lists four hrefs and no Hebrew labels; it swaps /pages/shipping-delivery (the page every other terms block and the footer link, titled «משלוחים והחזרות») for /policies/shipping-policy, so the store would link two different 'shipping' destinations.
  PROOF: SPEC.md:131; theme/sections/elmsnest-v2-coll-terms.liquid:335 and elmsnest-v2-pdp-terms.liquid:262 (labels «משלוחים ואספקה · זמני טיפול · מדיניות ביטולים · שאלות נפוצות» with /pages/shipping-delivery); theme/sections/footer-group.json:152
  FIX: Labels verbatim from coll-terms:335; keep /pages/shipping-delivery for the first link so the footer and the strip agree.

[low] §10 'Hebrew labels ≤ 70 chars' and §6.1/§7.1 new settings
  PROBLEM: The spec adds settings (show_card, show_secondary_cta, all_image, product_list, the terms heading) without giving their Hebrew labels, so the ≤ 70-char rule and the wording cannot be checked before build; also guillemets «» in the spec are notation and must not be typed into defaults.
  PROOF: SPEC.md:145-149, 155-157, 168, 262
  FIX: Provide labels: «להציג את כרטיס המוצר בהירו» (show_card), «להציג כפתור משני» (show_secondary_cta), «תמונת הכותרת בעמוד כל המוצרים» (all_image), «ארבעת המוצרים בעמוד הבית» (product_list), «כותרת» (heading). State once: defaults are written without «».

[low] §11 — WhatsApp checks
  PROBLEM: Nothing in the spec says or implies WhatsApp on the storefront (verified: §1 answer 3 quotes the owner, §5.4 forbids it, the floating snippet renders nothing without a number). But the §11 check greps only «וואטסאפ»; the builder emits wa.me if settings.whatsapp_number is ever set, and the hero's editor labels still say «וואטסאפ» (editor-only).
  PROOF: theme/snippets/elmsnest-v2-photo-url.liquid:19-21; brief/inventory/theme-src/snippets/elmsnest-whatsapp.liquid:15-16; theme/sections/elmsnest-v2-hero.liquid:242-243; SPEC.md:278
  FIX: §11: also assert 0 «wa.me», 0 «WhatsApp», 0 «ווטסאפ» in the rendered HTML, and assert settings.whatsapp_number is blank on 154726400174 before the run.


===== CRITIC ? =====
VERDICT: The spec is buildable in shape (custom sections + stock Kalles main-product/main-collection skinned), and the direction matches all four audits, but roughly a third of its claims about the stock components are wrong, and two of its acceptance tests cannot pass as written. Every stock file the spec relies on is present on dev theme 154726400174 with the same content as the local Kalles copy (MD5-verified via Admin API; main-collection differs only by one `render 'sizes'` argument), so the source citations below hold for the deployed theme. The single biggest risk is the buy path: the sticky bar is a SECOND `/cart/add` form whose hidden variant id is not visibly synced with the pills, the picker is hidden and the main form's `id` input is `disabled` without JS, and single-variant cards render their own add-to-cart forms — so "exactly 1 form", "sticky submits the same form" and P6 "posts the right id with JS off" are all false on stock Kalles until the fixes below are applied. Second risk: the owner's "simple" verdict will be undercut by stock clutter the spec does not know about (icon buttons on every card, a hover image, a desktop sale badge, a carousel for related, a JS-only sort popover with 8 options) unless `config/settings_data.json` gets a 2–3 key patch that §10 currently forbids.

[blocker] §8 (end) + §11 — 'exactly 1 form[action*="/cart/add"]'
  PROBLEM: The sticky bar does not reuse the main form; it renders its own product form with its own hidden id and quantity input, so the page always has 2 cart/add forms when sticky_atc is on. Single-variant product cards (related section) add a third kind of form each.
  PROOF: ElmsNest Theme/sections/main-product.liquid:77-78 `{%- form 'product', product, id: product_form_id_sticky, class: 'hdt-sticky-atc__form' -%}<input type="hidden" name="id" value="{{ selected_variant.id }}">` (id `form-product-sticky{{product.id}}`, line 54) vs blocks/_product-buy-button.liquid:68 main form `id="product-form-{{section.id}}{{product.id}}" class="hdt-main-product-form"`; snippets/btn-ultra.liquid:68-97 renders `form.hdt-product-form` inside every single-variant card when settings.show_ultra_btn is true (schema default true, config/settings_schema.json:1436-1439; key absent from theme/config/settings_data.json). powerful-solar-garden-light and solar-firefly-garden-lights are single-variant (Admin API).
  FIX: Rewrite the test: `form.hdt-main-product-form` count == 1; `form.hdt-sticky-atc__form` count == 1 (0 if sticky_atc is turned off); `.hdt-card-product form` count == 0 (requires show_ultra_btn:false, see settings_data finding). Change the spec sentence 'the sticky bar reuses it' to 'the sticky bar is a second form kept in sync'.

[high] §8.2 settings / P1 'the sticky bar that submits the same form'
  PROBLEM: Kalles' sticky component syncs the chosen variant into a `<button tag-select>` and re-dispatches variant:change on its own form, but nothing observed writes the new variant id into the sticky form's `input[name=id]`; its submit posts `new FormData(this.form)`. After changing a pill, the sticky button may add the initially selected variant. (PLAUSIBLE — read from minified JS; must be confirmed on the preview.)
  PROOF: ElmsNest Theme/assets/global.min.js `class StickyATC extends VariantChangeBase … onVariantChanged(t){const e=t.detail.variant; … getterGet(_sticky_select,this).value=e.id … $4("wrapp-hdt-pr-frm > form",this).dispatchEvent(new CustomEvent(variantChange…))` where `_sticky_select = $4("[tag-select]",this)` = the `<button type="button" … tag-select>` at sections/main-product.liquid:113; submit handler `const s=new FormData(this.form); fetch(`${Shopify.routes.root}cart/add.js`…)`; grep for 'product-sticky' in global.min.js finds no other sync.
  FIX: Add to verify.js: change a pill, click the STICKY button, assert the drawer line item variant id == the pill's variant. If it fails, either set `sticky_atc:false` (P1 already treats the bar as optional) or put a ≤10-line script in the terms-line `_liquid` block: `document.forms['product-form-main-product{{ product.id }}'].addEventListener('variant:change',e=>{var f=document.forms['form-product-sticky{{ product.id }}'];if(f&&f.id)f.id.value=e.detail.variant.id});`

[high] P6 / §11 'the form still posts the right id (JS off)'
  PROBLEM: With JS off the stock variant picker is hidden and the main form's hidden id input is disabled, so the main form posts no variant id at all; the noscript select fallback is commented out in Kalles.
  PROOF: blocks/_product-variant-picker.liquid:65 `class="hdt-variant-picker no-js-hidden …"` + assets/base.css:272-274 `:where(.no-js:not(html),.no-js-inline,html.no-js .no-js-hidden){display:none !important}`; blocks/_product-buy-button.liquid:69 `<input type="hidden" name="id" … {% if variant_picker_block != blank and product.has_only_default_variant == false %} disabled{% endif %}>`; the `<noscript>…<select name="id">` block is inside `{% comment %}` at _product-variant-picker.liquid:145-185. Only the sticky form (main-product.liquid:78) keeps an enabled id, fixed to the first variant.
  FIX: Add one more `_liquid` block right after `_product-variant-picker`: `<noscript><select name="id" form="product-form-main-product{{ product.id }}">{% for v in product.variants %}<option value="{{ v.id }}"{% if v == product.selected_or_first_available_variant %} selected{% endif %}>{{ v.title }} – {{ v.price | money }}</option>{% endfor %}</select></noscript>`. The section key in product.elmsnest.json MUST be `main-product` so the id equals `'product-form-' | append: section.id | append: product.id` (main-product.liquid:20). verify.js JS-off: assert `noscript select[name=id]` renders and a submit reaches /cart with that id.

[high] §7 (sort select) / P1 'Sort = a native select' / P6 '?sort_by='
  PROBLEM: Kalles' sort is a JS popover of role=option buttons over ALL of collection.sort_options (8 Shopify options with Shopify's own Hebrew names), not a native select, not links; with JS off it cannot open. The spec's 4-option list and labels cannot be produced by the stock control.
  PROOF: snippets/sort-by-popover.liquid:18-42 `<hdt-popover-sorting>` … `<dialog …>` … `{%- for option in results.sort_options -%}<button type="button" role="option" value="{{ option.value }}">{{ option.name | escape }}</button>`; it is the only sort output (sections/main-collection.liquid:191-195).
  FIX: Set `enable_sorting:false`, hide `.hdt-shop-control` in the skin, and render in `elmsnest-s-coll-header` under the filter row: `<form method="get" action="{{ collection.url }}"><select name="sort_by" onchange="this.form.submit()">` with options `manual`=מומלץ, `price-ascending`=מחיר: מהנמוך לגבוה, `price-descending`=מחיר: מהגבוה לנמוך, `title-ascending`=א–ב (selected = `collection.sort_by | default: collection.default_sort_by`) + `<noscript><button>מיון</button></noscript>`. verify.js: `select[name=sort_by]` exists; `/collections/all?sort_by=price-ascending` reorders with JS off.

[medium] §5.1 'main-collection toolbar: hide the filter button, the column switcher, the list/grid toggle and the showing N of M line'
  PROBLEM: None of those four exist once enable_filtering and enable_num_cols_selector are false; the list is imaginary, while the element that does remain is not named.
  PROOF: sections/main-collection.liquid:171-177 filter button gated by enable_filtering; 182-189 layout-switch (grid/list buttons, snippets/layout-switch.liquid) gated by enable_num_cols_selector; 'showing N of M' (`products.facets.product_count`, he.json:657) is referenced by no section or snippet (grep). What renders: `div.hdt-shop-control.hdt-row-flex.hdt-flex-cols-3` (line 167) with `.hdt-col.hdt-control-filter` (empty), `.hdt-col.hdt-control-layout` (empty), `.hdt-col.hdt-control-sorting` › `hdt-popover-sorting.hdt-popover__sorting`; `active-filters` (line 198) prints nothing without active filters.
  FIX: Replace the list with: `.hdt-shop-control{display:none}` (when the native select replaces sorting) — or, if the stock popover is kept, `hdt-popover-sorting [value="best-selling"],hdt-popover-sorting [value="title-descending"],hdt-popover-sorting [value="created-ascending"],hdt-popover-sorting [value="created-descending"]{display:none}`.

[high] §5.1 card rules + §11 'every .hdt-card-product img src contains the featured filename'
  PROBLEM: The compare-at selector matches nothing, and card-product1 still renders (with the store's settings) an icon button per card, a form per single-variant card, a second non-featured hover image, and a sale badge on desktop — none listed in the skin, and the hover image fails the §11 image test on every card.
  PROOF: Compare price element is `<hdt-compare-at-price class="hdt-compare-at-price">` (snippets/card-price.liquid:70,82; snippets/price.liquid:82,94,107), never `.hdt-price__compare`/`<s>`/`<del>`. Still rendered with settings_data (wishlist_mode "0", compare_mode "0", show_quick_view false, show_quick_add false, hidden_badges true; `show_ultra_btn` and `show_secondary_image` absent → schema defaults true, settings_schema.json:1436-1439, 1352-1355): `.hdt-product-btns.hdt-pr-btns-group1 > hdt-tooltip.hdt-ultra_btn_parent > a.hdt-card-product__btn-ultra` or `wrapp-hdt-pr-frm > form.hdt-product-form > button.hdt-card-product__btn-ultra` (card-product1.liquid:83-86, btn-ultra.liquid:56-97); `img.hdt-card-product__media--hover` = `card_product.media[featured_media.position]` (card-product-media.liquid:27-28,47); `.hdt-badge__wrapp` with «מבצע» for waterproof-solar-deck-step-lights — the hidden_badges rule `.hdt-hidden-pr-badges .hdt-card-product .hdt-badge__wrapp{display:none!important}` (collection-products.css:662-665) is inside `@media(max-width:767px)`; `hdt-variant-option.hdt-variant-option--default[hidden]` per option (color-list.liquid:122). Main image class: `img.hdt-card-product__media--main` (card-product-media.liquid:25); title `h3 > a.hdt-card-product__title.hdt-line-clamp` (card-product1.liquid:101); price `hdt-price.hdt-price` inside `.hdt-price-wrapp > .hdt-price__list`.
  FIX: Skin: `.hdt-card-product .hdt-product-btns,.hdt-card-product .hdt-badge__wrapp,.hdt-card-product .hdt-badge,.hdt-card-product__media--hover,.hdt-card-product hdt-variant-option,.hdt-scrolling-badge,hdt-compare-at-price,.hdt-price__sale hdt-compare-at-price{display:none}`. verify.js: test `img.hdt-card-product__media--main` only. Preferably also patch settings_data `show_ultra_btn:false`, `show_secondary_image:false` so the forms and second images leave the DOM (see settings_data finding).

[medium] §5.1 '.hdt-price: the מ־ prefix for price_varies in muted ink'
  PROBLEM: With the store's price_varies_style "1" the card prints a min–max range, not a from-price; style "2" prints «החל מ-», never «מ־».
  PROOF: theme/config/settings_data.json:66 `"price_varies_style": "1"`; snippets/card-price.liquid:55-62 `if settings.price_varies_style == "1" → price_range_html else from_price_html`; locales/he.json:557 `price_range_html: "<span class=hdt-money>{{ minimum }}</span> - <span class=hdt-money>{{ maximum }}</span>"`, he.json:572 `from_price_html: "החל מ-{{ price }}"`; schema options "1"/"2" (settings_schema.json:1145-1158).
  FIX: Decide explicitly: keep the range (no change, spec text updated to 'range'), or set `price_varies_style:"2"` in settings_data and accept «החל מ-» (changing it to «מ־» means editing locales/he.json `products.product.price.from_price_html`). The skin can then style `hdt-price.hdt-price-item--range`.

[medium] P4 'CTA vocabulary exactly «הוספה לסל» (PDP form)' + §11
  PROBLEM: The stock add-to-cart button (main and sticky) prints the theme's translation «הוסיפו להזמנה», and JS re-applies the same string after every variant change; the exact-vocabulary rule cannot be met without a locale edit the spec does not list.
  PROOF: blocks/_product-buy-button.liquid:94 and sections/main-product.liquid:137 `{{ 'products.product.add_to_cart' | t }}`; locales/he.json:462 `"add_to_cart": "הוסיפו להזמנה"`; global.min.js StickyATC/buy-buttons set `textContent=themeHDN.extras[..."addToCart"...]` from the same key.
  FIX: Add `locales/he.json` (key `products.product.add_to_cart` → «הוספה לסל») to §10 Edited, or do it in Admin → Edit default theme content (same file); otherwise relax P4 to accept «הוסיפו להזמנה».

[medium] §8.1 brc-nav-product «דף הבית › {collection title} › {product}»
  PROBLEM: With the store's remove_collection_url:true and plain product.url card links, the stock breadcrumb has no collection object and prints «בית › {product}» (home label is «בית», not «דף הבית»).
  PROOF: sections/brc-nav-product.liquid:24-33 `unless collection … if settings.remove_collection_url == false assign collection = product.collections.first`; theme/config/settings_data.json:95 `"remove_collection_url": true`; line 45 `{{ 'products.breadcrumb.home' | t }}` = locales/he.json:681 `"home": "בית"` (the «דף הבית» string at he.json:234 is a different key).
  FIX: Drop brc-nav-product and let `elmsnest-s-pdp-kicker` print a `<nav aria-label>` «דף הבית › {collection.title} › {product.title}» from the menu-order match it already computes (it also avoids `product.collections.first` returning `sale` for waterproof-solar-deck-step-lights, which is in both `תאורת-שביל-סולארית` and `sale` per Admin API). Alternative: settings_data `remove_collection_url:false` (changes every card URL to /collections/x/products/y).

[medium] §8.2.4 per-unit caption — 'the EXISTING snippet … prints «≈ X ₪ למטר / ליחידה» … if it does not update, add the data attribute the Kalles variant script dispatches on'
  PROBLEM: The snippet exists on the dev theme but only handles products whose option is literally named «כמות», prints «{price} · {per-unit} ליחידה» with no «≈» and no per-metre case, so rope/edison (option «אורך ומספר נורות») get nothing; it already updates correctly, and the 'data attribute' fallback is a misdescription — the hook is an event on the main form.
  PROOF: Dev theme file snippets/elmsnest-pdp-unit-price.liquid md5 7f000c35… == local; lines 17-24 `if en_opt.name == 'כמות'`, line 35 `<bdi dir="ltr">{{ en_variant.price | money }}</bdi> · <bdi dir="ltr">{{ en_per_unit | money }}</bdi> ליחידה`; lines 45-56 listen to `variant:change` on `form.hdt-main-product-form`; global.min.js dispatches `this.masterInput.form.dispatchEvent(new CustomEvent(variantChange /*"variant:change"*/,{bubbles:!0,detail:{variant:this.currentVariant}}))`. Product options (Admin API): solar-rope-string-lights «אורך ומספר נורות» = «7 מ׳ / 50 נורות»…; waterproof-solar-deck-step-lights «כמות» = «2 'יח»…; powerful-solar-garden-light «כמות» = «יחידה אחת» (prints an empty span).
  FIX: Add `snippets/elmsnest-s-pdp-unit.liquid` to §10: pre-render one `<span data-en-variant="{{ v.id }}" hidden>` per variant; per-unit when option name == «כמות» and leading integer > 1 (₪ ליחידה); per-metre when option name contains «אורך» (leading number before «מ׳», ₪ למטר); «≈» + `<bdi>`; copy lines 40-64 of the existing snippet for the toggle. Delete the 'data attribute' sentence from the spec.

[medium] §8.2 `_product-medias` static block settings
  PROBLEM: `show_first_media:false` makes the gallery open on the selected variant's media rather than the featured image, and `use_select_varaint_change_media:true` makes swiping the gallery change the selected variant — both contradict P3 'featured first' and P1 'Choose = pills'.
  PROOF: locales/en.default.schema.json:3497-3500 show_first_media = "Show first media until customers hand-pick a variant"; 3501-3503 use_select_varaint_change_media = "select a product variant by change a variant image. Only working when slider active"; blocks/_product-medias.liquid schema ids at lines 382-406 (values `fraction|thumbnails`, defaults thumbnails/false/true).
  FIX: Set `"show_first_media": true, "use_select_varaint_change_media": false` (other ids as spec: `mobile_media_layout:"thumbnails"`, `desktop_media_layout:"thumbnail_left"`, `image_ratio:"adapt_image"`, `image_zoom:"zoom_lightbox"`). Keep theme.liquid:123-131 preload as is (it preloads media.first, which is now always the start slide).

[medium] §8.4 related-products: `display_type: "grid"`, `collection = auto`, heading via section-heading
  PROBLEM: 'grid' is not a display_type value, the section is always a carousel, the `collection` setting is commented out, and the heading lives in a child block the spec does not name.
  PROOF: sections/related-products.liquid:235-258 options "1" By Collection, "2" Vendor, "3" Type, "4" Dynamic; markup is `<hdt-slider … hdt-slides-{{ col_mb }}>` in both branches (lines 77, 99); `collection` setting commented out at 259-264, type "1" uses `product.collections.first` (60-61); heading = static block `{%- content_for "block", type: "section-heading", id: "pr-heading" -%}` (73) whose text is a child `_heading_section` block setting `heading` (blocks/_heading_section.liquid:17). Cards: snippets/related_option.liquid:18 card-product1 with `limit`.
  FIX: Preferred: give `elmsnest-s-products` a `source` setting (`list` | `related`): related = product.collections matched in menu order, exclude current, limit 4 — one real 2-col grid, one card implementation. If stock is kept: `{"type":"related-products","blocks":{"pr-heading":{"type":"section-heading","static":true,"blocks":{"h":{"type":"_heading_section","settings":{"heading":"עוד לאותו מקום"}}},"block_order":["h"]}},"settings":{"display_type":"1","limit":4,"product_des":"1","col_mb":"2","col_tb":"3","col_dk":"4","image_ratio":"portrait","navUI":false,"dotUI":false,"colors_by_section":true,"color_scheme":"scheme-env2-night"}}` and accept a 2-up swipe carousel on mobile.

[high] §6.3 `elmsnest-s-products` renders card-product1 on the home
  PROBLEM: The card CSS is never loaded on the index template, so the homepage grid would render unstyled (no ratio box, overlay buttons visible, no clamp).
  PROOF: theme/layout/theme.liquid:162-164 `unless request.page_type == 'index' render 'product-styles'`; ElmsNest Theme/snippets/product-styles.liquid is the only loader of `product-card.css` and `collection-products.css` (grep of layout/snippets/sections). Card ratio needs the wrapper class: sections/main-collection.liquid:218 `hdt-collection-products … hdt-row-grid hdt-grid-cols-{{col}} md:hdt-grid-cols-… lg:hdt-grid-cols-… hdt-ratio--{{ image_ratio }}`; assets/base.css:1899 `.hdt-ratio--portrait{--ratio-percent:125%}`.
  FIX: In `elmsnest-s-products` (and any custom section rendering card-product1): `{{ 'product-card.css' | asset_url | stylesheet_tag }}{{ 'collection-products.css' | asset_url | stylesheet_tag }}`, wrapper `<div class="hdt-collection-products hdt-row-grid hdt-grid-cols-2 md:hdt-grid-cols-2 lg:hdt-grid-cols-4 hdt-ratio--portrait">`, and call `render 'card-product1', card_product: p, section_id: section.id, sizes: '(min-width:1150px) 25vw, 50vw', image_ratio: 'portrait', class: 'hdt-pr-style1', btn_wishlist_code: '', show_vendor: false`.

[medium] §4 'Nothing else animates on scroll' / §11 screenshots / audits' black boxes
  PROBLEM: The stock collection grid is itself a scroll-reveal: cards start at opacity 0 and are animated in on intersection, independent of the theme's reveal setting — a full-page capture taken before intersection shows empty card boxes.
  PROOF: sections/main-collection.liquid:218 `<hdt-reval-items reveal-on-scroll="true" …>`; assets/base.css:1782-1785 `.js hdt-reval-items[reveal-on-scroll=true] :where(.hdt-card-product,.hdt-reval-item){opacity:0}`; global.min.js `get enabledReveal(){return"true"===this.getAttribute("reveal-on-scroll")} reveal(){animate(…,{opacity:[0,1],transform:["translateY(30px)","translateY(0)"]}…)}`; `animations_reveal_on_scroll:false` (settings_data:107) only disables the `[hdt-reveal]` family (base.css:1774-1808).
  FIX: Skin: `hdt-reval-items[reveal-on-scroll=true] .hdt-card-product{opacity:1!important;transform:none!important}` (comment: forced, base.css:1782); verify.js scrolls to the bottom and waits for images before capturing.

[medium] §10 'Untouched: config/settings_data.json'
  PROBLEM: Three of the spec's own outcomes need theme settings the skin cannot fully deliver (card forms and hover images stay in the DOM; range vs from-price; breadcrumb collection).
  PROOF: See findings on card rules (show_ultra_btn / show_secondary_image absent from settings_data → schema defaults true), price_varies_style "1" (settings_data:66), remove_collection_url true (settings_data:95).
  FIX: Add a minimal settings_data patch (same procedure as theme/config/settings_data.patch.md): `"show_ultra_btn": false, "show_secondary_image": false` and, if a from-price is wanted, `"price_varies_style": "2"`; list it under §10 Edited and upsert it before the templates. If the owner insists on an untouched file, state explicitly that the DOM keeps hidden forms/images and the verify tests are scoped accordingly.

[medium] §5.3 'Licensed wording, verbatim from sections/elmsnest-v2-terms.liquid schema defaults'
  PROBLEM: The schema defaults contain only one row (14 יום); the four rows live in three templates with three different wordings, and the spec's lines match none of them («במייל» and «ייתכן משלוח ממחסנים…» are new copy) — a P4 violation by the spec itself.
  PROOF: theme/sections/elmsnest-v2-terms.liquid:181-184 (single default block: 14/יום/«ביטול עסקה לפי חוק הגנת הצרכן.»/«עד 14 יום מקבלת המוצר. דמי ביטול עד 5% ממחיר העסקה או 100 ₪ — הנמוך מביניהם.»); templates/index.json env2_terms blocks (~294-327: «משלוח עד הבית: 29.90 ₪.», «1–3 ימי טיפול ועוד 7–14 ימי משלוח. חלק מהמוצרים נשלחים ממחסנים מחוץ לישראל — …», «שלחו תמונה של המקום.»); templates/collection.json:126-129 coll_terms («ייתכן משלוח ממחסן מחוץ לישראל.», «שולחים תמונה של המקום ואנחנו בודקים התאמה לפני ההזמנה.»); templates/product.elmsnest.json:430-466 pdp_terms. Link: theme uses `/pages/shipping-delivery` for «משלוחים ואספקה» (index.json:337, pdp_terms foot_links); `/policies/shipping-policy` also resolves (SHIPPING_POLICY exists, Admin API).
  FIX: Name ONE source and copy byte-for-byte (recommend templates/collection.json coll_terms rows: numeral/unit/headline/sub); drop «במייל»; pick one shipping link (recommend /pages/shipping-delivery to match the footer) and use it in §5.3 and §8.2.7 alike.

[medium] §6.1 hero: 'add show_card / show_secondary_cta … and the lamp motion'
  PROBLEM: The only [data-lamp] in the hero is the product card, so with show_card:false there is no lamp motion left; the spec also edits the owned copy while the dev theme holds a different version of the file.
  PROOF: theme/sections/elmsnest-v2-hero.liquid:122 `<article class="env2-hero__card" data-lamp>` is the sole data-lamp (grep); remaining motion is the scroll-driven dusk filter `--env2-p` (line 150) and the sun rail (91-97). Card block = lines 121-141 (`{%- if product != blank -%} … {%- elsif request.design_mode -%} … {%- endif -%}`); secondary CTA = lines 115-117. Owned copy LF-md5 ede5d763… ≠ dev theme 287b8d90… (also elmsnest-v2-core 7088c988… ≠ 420c9994…, product.elmsnest.json 13a43667… ≠ 94c3ee50…); theme.liquid and photo-url match.
  FIX: Gate: `{%- if section.settings.show_card and product != blank -%}` (keep the design_mode branch under the same flag) and `{%- if section.settings.show_secondary_cta and section.settings.cta_secondary_label != blank -%}`; keep lines 10-29 (product still feeds the background). Reword §6.1: 'the dusk darkening + sun rail; no lamp'. Add to §11: fetch hero/core/product.elmsnest.json from the dev theme first and apply the edits to those bodies.

[low] §8.2 `_liquid` blocks (kicker, unit caption, terms line, not-for) and ground-product via `_liquid`
  PROBLEM: Liquid-type settings cannot see `section` or `settings`, and the env2 ground snippet reserves 78px for a buy bar that no longer exists.
  PROOF: Shopify docs, Input settings → liquid → Limitations: no `layout`, `content_for_*`, `section`, `javascript`, `stylesheet`, `schema`, `settings`; `product` and standard tags incl. `render` allowed (and the live pre-env2 template used exactly this: brief/inventory/theme-src/templates/product.elmsnest.json `liquid_en_unit_price` = `{% render 'elmsnest-pdp-unit-price', product: product %}`). theme/snippets/elmsnest-v2-ground-product.liquid:44-46 `@media(max-width:900px){html body.hdt-page-type-product{--env2-pdp-bar-h:78px;padding-block-end:var(--env2-pdp-bar-h)}}`; Kalles sets its own `--sticky-atc-height` (global.min.js form_atc_visible_fn). blocks/_liquid.liquid:2,20 wraps output in `<div hdt-reveal="slide-in" timeline class="hdt-text-warry hdt-rte …">` (inert while animations_reveal_on_scroll is false).
  FIX: Hard-code the form id from the section key (`product-form-main-product{{ product.id }}`); never read `settings.`/`section.` in the s-pdp-* snippets (elmsnest-v2-photo-url degrades to mailto, fine); add to the skin `@media(max-width:900px){html body.hdt-page-type-product{padding-block-end:0}}` or fork the ground snippet without lines 42-46.

[medium] §8.2 section settings for main-product
  PROBLEM: `hdt_show_sticky_atc: true` is not a valid value (it is a select), the checkbox is `sticky_atc`; and with the default background_opacity the sticky bar is transparent.
  PROOF: sections/main-product.liquid:180-185 `checkbox id sticky_atc default true`; 206-221 `select id hdt_show_sticky_atc` values `hdt_show_always` / `hdt_show_scrolls_outside_the_scope_of_the_form`; line 60 `style="--background-opacity: {{ section.settings.background_opacity }};backdrop-filter: blur(8px)"` with schema default 0 (420-429). Static blocks needed in the template: `main-product-sidebar` (`_product_sidebar`, static:true) and `main-product-medias` (`_product-medias`, static:true) — see brief/inventory/theme-src/templates/product.elmsnest.json; `_product-title` default tag is h2 (blocks/_product-title.liquid:58-90).
  FIX: `"sticky_atc": true, "hdt_show_sticky_atc": "hdt_show_scrolls_outside_the_scope_of_the_form", "hdt_layout_atc_mob": "hdt_layout_atc_mob_default", "colors_by_section": true, "color_scheme": "scheme-env2-night", "background_opacity": 1`; skin `.hdt-sticky-btn-atc{background:#05070c;border-top:1px solid #1f1e1d}`; `_product-title` settings `"tag":"h1"`; copy the two static blocks from the inventory template.

[low] §10 schema constraints
  PROBLEM: The spec caps labels at 70 chars but omits the 25-char limit on section/block `name` and setting `id`, which Shopify rejects.
  PROOF: Shopify theme-check ValidSchemaName: schema `name` ≤ 25 characters (docs); section-schema docs list name/tag/class/limit/settings/blocks/max_blocks/presets; JSON templates ≤ 25 sections / 50 blocks per section.
  FIX: Add: section and block `name` ≤ 25 chars, setting `id` ≤ 25 chars (a–z0–9_), `presets[].name` ≤ 25; keep Hebrew names short (e.g. «ElmsNest S — מוצרים»).

[low] §1.3 / §11 «וואטסאפ» = 0 and mailto = 1
  PROBLEM: Both counts silently flip if the owner ever fills `whatsapp_number`: the layout renders a floating WhatsApp button with the word «וואטסאפ», and every photo link becomes wa.me.
  PROOF: theme/layout/theme.liquid:210 `{%- render 'elmsnest-whatsapp' -%}`; ElmsNest Theme/snippets/elmsnest-whatsapp.liquid:16-29 renders when settings.whatsapp_number is set, label «שאלות? וואטסאפ»; theme/snippets/elmsnest-v2-photo-url.liquid:19-25 emits wa.me when set. Key is absent from settings_data today.
  FIX: State in §9 that `whatsapp_number` must stay empty for this round, and have verify.js assert `settings.whatsapp_number` is blank (or `.en-wa` count == 0).

[low] §7.2 main-collection settings list
  PROBLEM: Two settings that decide the card style and grid mode are omitted, and a stale cart attribute can override the column counts.
  PROOF: sections/main-collection.liquid:233-244 `case section.settings.product_des when '1' render 'card-product1'` (default "1", schema 451-497); 19-26 `enable_list_default` → list layout (checkbox without default, 953-957); 11-18 `cart.attributes.products_items_per_row` overrides col_mb/col_tb/col_dk when present.
  FIX: Add `"product_des": "1", "enable_list_default": false, "image_size": true, "section_layout": "container", "pr_border": "none"` to the template; note in §11 that a tester who used the old column switcher must clear the cart attribute (new session).
