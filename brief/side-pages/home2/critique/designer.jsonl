{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-band.liquid", "line": 74, "severity": "blocker", "where": "band at 1366 — `.ens-home-band__fig img{object-position:var(--ens-op-lg,50% 60%)}` with the caption at `inset-block-end:56px` (line 75)", "defect": "In band-d.png the lower string of crystal bulbs runs through the sentence box (x 872–1303, y 394–504): a bulb sits behind the right end of «חושך הוא לא» at the frame edge (≈1300,385) and another between the two lines under «סוף» (≈1180,450), and contrast.txt measures the glow serif at 3.93:1 on p90 over them — under the 4.5:1 floor of SIMPLIFY P6 and of SPEC §1 C, whose 30 % fade is already on (show_fade default true) and cannot darken a light source. At 360/390 the same sentence sits under the string on dark leaves and measures 6.74–7.03:1, so this is a desktop composition problem, not a type problem.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/band-d.png; /home/user/ElmsNest/brief/side-pages/home2/verify/contrast.txt line 39", "fix": "Move the sentence off the string at ≥901 while keeping the photograph clean — raise the caption (inset-block-end ≈ 170 px, onto the dark gap between the two strings) or change object_position_desktop so the lower string leaves the sentence box (try «50% 42%») — then re-run contrast.py; a local backing is the fallback only if neither reaches 4.5:1.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.85, "reason": "The defect was real on the render the critic looked at, but the file already does what the fix asks. Evidence: (1) /home/user/ElmsNest/brief/side-pages/home2/verify/band-d.png and contrast.txt line 39 (band-d p90 3.93:1) were shot at 08:27 against the committed section, whose desktop default was `50% 60%` — the finding even quotes `var(--ens-op-lg,50% 60%)`. (2) The working copy of /home/user/ElmsNest/theme/sections/elmsnest-s-home-band.liquid (modified 08:46, uncommitted; `git diff` shows the change) now reads `assign ens_op_lg = ... | default: '50% 40%'` (line 16), `object-position:var(--ens-op-lg,50% 40%)` (line 74), the schema default `50% 40%` (line 91), and the comment on line 67 records the reason; SPEC.md line 5 also states «the band's desktop crop is 50% 40%». That is exactly the finding's own alternative fix («try 50% 42%»). (3) The number holds: verify/exp/c2 is the same shot with `object-position:50% 40%` at ≥901 (exp/candidate-2.css), and `python3 contrast.py exp/c2` on the hide-text background gives band-d p90 6.62:1 (p50 9.89:1) — above the 4.5:1 floor, with no extra fade; critique/LEAD-DECISIONS.md #2 records the same decision and measurement. I also re-derived it from the asset (theme/assets/ens-home-fence.jpg cover-cropped to 1366×560, the 30 % .55 fade applied, the 431×110 box at 872,394): 60 % → p90 3.42:1 (matches the 3.5–3.9 measured), 40 % → p90 6.54:1. Caveat: the verify/ folder itself has not been re-shot at 40 %, so contrast.txt line 39 still shows the stale 3.93; the fixer's re-run of shoot-sections.js + contrast.py should refresh it, but no code change is needed."}, {"refuted": true, "confidence": 0.82, "reason": "The finding is stale: it reads line 74 as `50% 60%`, but the current file (08:46) already has `object-position:var(--ens-op-lg,50% 40%)` with the schema default «50% 40%», because the amended SPEC header and §1 C, and LEAD-DECISIONS #2, deliberately settled exactly this defect («band sentence over the lower string of bulbs at 1366 — 3.50:1 … object_position_desktop default 50% 60% → 50% 40%. No extra fade», measured 6.62:1 with the photograph untouched). Its evidence (band-d.png 08:27, contrast.txt 08:27) predates that amendment, so the 3.93:1 number describes the superseded render. Acting on it now — raising the caption to ≈170 px or changing the crop to «50% 42%» — would override a contract line the lead chose after measuring three candidates, for no shopper gain; the only legitimate residue is re-running contrast.py on the re-mirrored home to confirm the 40 % crop, which is the SPEC §5 acceptance step, not a design change."}], "survives": false}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid", "line": 108, "severity": "major", "where": "`.ens-home-solar__tag` — 12 px ink-2 at the top inline-end corner with only a blurred text-shadow", "defect": "The day tag «יום» is a ghost at every width: in the zoomed corner of solar-s.png / solar-m.png / solar-d.png the three letters are barely separable from the orange photinia leaves behind them, and contrast.txt gives 1.81:1 (360), 1.75:1 (390) and 1.45:1 (1366) on p90 — a label a shopper cannot read. The night tag «לילה» survives only because its corner happens to be dark (4.92–6.23:1). The 8 px blurred shadow does nothing on a mid-tone leaf field, and because inline-end is the LEFT corner in RTL, the day tag hugs the gutter while the night tag sits at the far left of the page, so the pair is not even symmetric. Not a blocker only because the headline word («ביום» / «בלילה») already carries the meaning.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png; /home/user/ElmsNest/brief/side-pages/home2/verify/contrast.txt lines 2, 15, 28", "fix": "Take the tags off the open photograph and seat them on the scrim just above their headline (inset-block-start → inset-block-end ≈ 52 px at 390 / 80 px at ≥901, inset-inline-start to align with the word), where the background already measures ≈10:1 — or, since the headline already says ביום/בלילה, drop the tags (empty defaults) and keep the frames clean.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.9, "reason": "The defect was real in the evidence render (contrast.txt lines 2/15/28 do read 1.81/1.75/1.45:1 p90 for «יום», and a zoom of solar-d.png shows the faint tag on the photinia leaves), but the finding cites HEAD (`git show HEAD:` line 108: ink-2 + 8px text-shadow, no backing, defaults \"יום\"/\"לילה\"). The working-tree /home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid (mtime 08:46, after the 08:27 evidence) already does what the fix asks: label_day/label_night have no defaults (info: \"ריק = בלי תווית (הכותרת על המסגרת כבר אומרת «יום»)\"), index.json sets no label_day/label_night, so the tags do not render at all; and the `.ens-home-solar__tag` rule now has `padding:4px 8px;background:rgba(5,8,14,.85);color:var(--env2-ink)` with a comment noting 14:1 measured on the day frame, so an owner-typed tag would sit on a night backing rather than the open photo. The evidence PNGs/contrast.txt simply predate this change; the ghost-tag defect does not exist in the file as it stands."}, {"refuted": true, "confidence": 0.95, "reason": "The finding is stale: it measures the pre-amendment render (contrast.txt still lists `__eyebrow` and the three `__txt` steps that no longer exist) and the old CSS (text-shadow only). The amended SPEC §1 A and §2 already decided exactly this — «corner labels «יום»/«לילה» exist as settings label_day/label_night but are EMPTY by default … a typed label renders on a .85 night backing», and critique/LEAD-DECISIONS.md #1 records «Labels «יום»/«לילה» are gone from the page» for the P2 «nothing twice» reason. The current section file matches: schema line 148/155 has no default, lines 69/82 render the tag only when non-blank, and line 117 gives it `background:rgba(5,8,14,.85)` with no text-shadow (14:1 measured). Both halves of the proposed fix (drop the tags; back a typed one) are therefore already done, so acting on it would change nothing a shopper or the owner would see."}], "survives": false}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid", "line": 106, "severity": "major", "where": "`.ens-home-solar__frame::after` night scrim — `linear-gradient(180deg,rgba(5,8,14,0) 40%,rgba(5,8,14,.72) 100%)`", "defect": "The glow headline «בלילה נדלק.» sits on the lit pavement rays at the foot of the night frame (night-s foot zoom: the upper halves of the glyphs cross the light pool at the base of the nearest bollard), and glow (#ffd394) over warm lamplight is a same-hue pairing, so the brightest tenth behind the glyphs measures 4.43:1 at 360 and only 4.53:1 at 390 (5.77:1 at 1366) — under or at the P6 floor on the two phone widths. At 4:5 the night image (1254×1090, landscape) is cropped horizontally, so the object_position setting cannot move the light pool away; only the scrim can.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/solar-s.png; /home/user/ElmsNest/brief/side-pages/home2/verify/contrast.txt lines 5, 18", "fix": "Deepen the night scrim's foot — e.g. `rgba(5,8,14,0) 35%, rgba(5,8,14,.84) 100%` — which is invisible on the already-dark frame but lifts the headline past 4.5:1 at 360; re-measure.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.9, "reason": "The finding is stale: it quotes a scrim (`rgba(5,8,14,0) 40%, .72 100%`) and numbers (contrast.txt lines 5/18: 4.43:1 at 360, 4.53:1 at 390) that were measured at 08:27 against an earlier version of the file. /home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid (mtime 08:46, uncommitted diff vs HEAD) already has the fix the finding asks for: line 106 reads `.ens-home-solar__frame::after{...background:linear-gradient(180deg,rgba(5,8,14,0) 36%,rgba(5,8,14,.84) 100%)}` — identical to verify/exp/candidate-1.css. That candidate was re-shot and re-measured (exp/c1 with an exact hidden-text -bg pass): running verify/contrast.py on exp/c1 gives «בלילה נדלק.» p90 6.38:1 at 360, 6.69:1 at 390, 7.85:1 at 1366 — all above the 4.5:1 P6 floor, matching the comment in the liquid (\"measured 6.4:1 at 360\"). So the defect as described (4.43/4.53 under the stale 40%/.72 scrim) is no longer present in the file; only the top-level verify/contrast.txt and solar-s.png need regenerating to reflect the current CSS."}, {"refuted": false, "confidence": 0.8, "reason": "The contract agrees with the finding rather than deciding otherwise: the amended home2 SPEC §1 A fixes the night scrim at «rgba(5,8,14,0) 36% → rgba(5,8,14,.84) (measured 6.4:1 behind the glow headline at 360)» and LEAD-DECISIONS #6 ruled the same, so the committed line 106 (40% → .72, measured 4.43:1 at 360 / 4.53:1 at 390 in verify/contrast.txt) is a P6 floor miss a phone shopper would see on the glow headline. The fix is one CSS gradient value, no JS, no Kalles change, invisible on an already-dark frame. Note the finding is partly stale: the uncommitted working tree already carries 36%/.84 (git diff on the section), and contrast.txt predates the amendment (it still lists the cut eyebrow, tags and three steps) — acting on it means committing that change and re-measuring, not a new design decision."}], "survives": true}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid", "line": 14, "severity": "minor", "where": "day frame default `object_position_day` «50% 50%» under the 16:10 desktop frame (line 118)", "defect": "At 1366 the 16:10 frame shows source rows ≈235–1018 of the 1254 px day file, and the nearest lamp's panel rim starts at ≈245 in the source (day-src-top crop) — so in solar-d.png the crown of the nearest head touches the frame's top edge (day-d-top zoom: the panel's dark rim is clipped at y 107). The panel is still visible on the second and third heads, so the frame does say «panel», but the hero object of the frame is cramped against the edge. At 4:5 (360/390) the crop is horizontal and the crown has ≈20 % headroom — fine.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png (day frame top edge, x 850–1010)", "fix": "Give the day frame a desktop crop of «50% 40%» (a `--ens-op-lg` like the band already has, or a media-query override) so the nearest panel gets ≈90 px of headroom; the pavement at the foot is unaffected.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.9, "reason": "The screenshot does show what the critic describes (solar-d.png, 08:27: the nearest lamp's panel rim sits on the day frame's top edge), but the file already does what the fix asks. /home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid (working tree, mtime 08:46 — later than the screenshot) now has line 17 `assign ens_op_day_lg = section.settings.object_position_day_desktop | default: '50% 40%'`, line 63 writes it as `--ens-op-lg` on the day figure, and line 126 inside `@media (min-width:901px)` sets `.ens-home-solar__frame img{object-position:var(--ens-op-lg,50% 40%)}` — i.e. a per-breakpoint desktop crop of 50% 40% for the day frame (plus a schema setting `object_position_day_desktop`, default 50% 40%, at line 146; index.json passes no override). The 50% 50% at line 14/16 is only the mobile (4:5) default, where the critic agrees headroom is fine. The evidence PNG predates this change and should be re-rendered, but no further code change is needed."}, {"refuted": true, "confidence": 0.9, "reason": "The amended SPEC §1A already decides exactly this: «Desktop crops per frame: object_position_day_desktop «50% 40%» and object_position_night_desktop «50% 30%» (≥ 901, 16:10 — the nearest panel crown and the far lamp heads were cut at 50%/40%)», and LEAD-DECISIONS.md row 8 is the same ruling. The working-tree section (/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid lines 17, 63, 126, 146) already implements it with --ens-op-lg defaulting to 50% 40% under @media (min-width:901px). The critic's evidence, /home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png (08:27), predates the section edit (08:46) and shows the pre-amendment 50% 50% crop, so there is nothing left to act on except re-shooting the verify screenshot; the requested fix is a no-op against the current file."}], "survives": false}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid", "line": 15, "severity": "minor", "where": "night frame default `object_position_night` «50% 40%» under the 16:10 desktop frame", "defect": "In solar-d.png the two farthest bollard heads at the top-left of the night frame (x ≈ 85–250, y 107–160) are cut by the top edge — the lit heads lose their crowns exactly where the «לילה» tag sits (night-d-top zoom). The nearest head is whole, so the frame still works, but a lamp head sliced by the edge is the one crop a lighting store's diptych should avoid.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png (night frame, top-left)", "fix": "Change the night default to «50% 30%» (shows ≈50 px more of the top at 1366; no effect at 4:5 where the crop is horizontal).", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.92, "reason": "Checked the render and the file; the defect does not hold on either count. (1) The file already does what the fix asks: the desktop frame (≥901 px, 16:10) does not use `object_position_night` at all — line 19 defines `object_position_night_desktop` with default «50% 30%», and line 126 applies `--ens-op-lg` in the ≥901 media query (SPEC line 47 records the same: desktop night crop «50% 30%»). index.json `ens_home_solar` has empty settings, so solar-d.png (1366 px) was rendered at 50% 30% already; the 50% 40% default on line 18 only affects the mobile 4:5 crop. (2) The crop the critic describes is not visible in /home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png: pixel-scanning the night frame, its top edge is at y=147 (not 107 — the cited region y 107–160 is mostly page background above the frame). The farthest lamp head's dark cap rim starts around y≈179–200 and its lit bowl at y=209 (x≈162), i.e. ~30–50 px of foliage/dark garden between the frame edge and the crown; a 4x zoom of the top-left corner confirms both far heads sit whole below the edge with the hedge above them. Cross-check against the source asset ens-home-night.jpg (1254×1090): at 50% 30% in a 620×388 frame the crop starts at source y≈92, while the far head caps begin at source y≈200, so no crown is sliced."}, {"refuted": true, "confidence": 0.9, "reason": "The contract already decided this exact defect the finding's way, but through a separate desktop setting rather than the mobile default: SPEC §1 A (amended) reads «Desktop crops per frame: `object_position_day_desktop` «50% 40%» and `object_position_night_desktop` «50% 30%» (≥ 901, 16:10 — the nearest panel crown and the far lamp heads were cut at 50%/40%)», per LEAD-DECISIONS #8, and the working-tree file implements it (line 19 default '50% 30%', `--ens-op-lg` on the night figure at line 76, applied at ≥ 901 by line 126). The finding targets `object_position_night` («50% 40%», the 4:5 mobile crop, which SPEC keeps at «50% 40%») and asks for a change it admits has no effect at 4:5 — so acting on it as written is either a no-op duplicate of what is built or a deviation from the mobile default the SPEC fixed. If solar-d.png still shows sliced heads, the correct action is re-shooting the verify render against the amended file, not editing the default."}], "survives": false}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid", "line": 121, "severity": "minor", "where": "`.ens-home-solar__steps{…margin-block-start:8px…}` at ≥901 with each step's `border-block-start` (line 112)", "defect": "At 1366 the three step columns start 8 px under the diptych, so their three hairline tops (broken by the 40 px column gaps) sit at y ≈ 498 directly beneath the frames ending at 492 — in solar-d.png and the steps-d-rule zoom they read as three short underlines drawn under the photographs rather than as rules above the steps; the eye pairs them with the frames, not the numerals. At 390 the 22 px margin keeps the rule attached to the steps.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/solar-d.png (y 492–500)", "fix": "At ≥901 set `.ens-home-solar__steps{margin-block-start:28px}` so the rules sit with the numerals, not under the frames.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.97, "reason": "The finding describes markup and CSS that do not exist. /home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid has no `.ens-home-solar__steps` selector, no three-column grid, no 8px margin and no numerals (grep for \"steps\"/\"numeral\" returns nothing); SPEC.md line 88 records the three numbered steps as cut this round. What exists is `.ens-home-solar__lines{margin:22px 0 0}` (line 120) with `@media (min-width:901px){ .ens-home-solar__lines{margin-block-start:28px} }` (line 129) — already the exact value the proposed fix requests — and each `.ens-home-solar__line` carries a full-width `border-block-start` (line 121). Measured on solar-d.png (1366×676): the frames end at y≈533, the only hairline rows are y=561 and y=619, both continuous across the full 1240px content width (310 of 310 sampled columns match #1f1e1d), with the single text row \"בלי חיבור לחשמל, בלי חשמלאי.\" between them. There are no short, gap-broken rules at y 492–500 under the photographs; the rule sits 28px below the diptych, attached to the text row. sizes.json for ens-home-solar-d likewise lists only the two frame words, no step numerals."}, {"refuted": true, "confidence": 0.92, "reason": "The finding is against the superseded first build: SPEC §1 A (amended) says «The three numbered steps of the first build were cut by the critique» and «Under the frames ONE line on hairlines», and LEAD-DECISIONS #3 cut the three-column steps for the owner's «nothing twice». There is no `.ens-home-solar__steps` in the current /home/user/ElmsNest/theme/sections/elmsnest-s-home-solar.liquid (0 matches); line 121 is now `.ens-home-solar__line`, a single full-width hairline row, so the «three short underlines under the frames» geometry no longer exists. The only surviving half of the ask, `margin-block-start:28px` at ≥901, is already applied on `.ens-home-solar__lines` in the ≥901 block (LEAD-DECISIONS #9), so acting on this finding changes nothing; the cited solar-d.png is a shot of the pre-amendment render."}], "survives": false}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-winter.liquid", "line": 86, "severity": "minor", "where": "`.ens-hw__h2.env2-h{font-size:34px}` at ≥901", "defect": "The winter heading «ובחורף?» renders at 34 px (sizes.json: box h 37) while the section directly below it, «מתי כן, ומתי לא», and the two above use `clamp(25px,3.4vw,44px)` = 44 px at 1366 — in home-d-js-full.png the two serif headings sit ≈250 px apart at visibly different sizes, the only break in the page's heading scale. At 390 the pair is 26 vs 25 px and reads as one scale. The spec asked for 34, so this is the spec's number, but it does not match the neighbours it was placed between.", "evidence": "/home/user/ElmsNest/brief/side-pages/simplify/verify-after/home-d-js-full.png (winter vs fit headings); /home/user/ElmsNest/brief/side-pages/home2/verify/sizes.json ens-home-winter-d", "fix": "Use the neighbours' `font-size:clamp(25px,3.4vw,44px)` on `.ens-hw__h2.env2-h` (or, if the note is meant to sit lower, 38 px) so consecutive h2s share one scale.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.92, "reason": "The file already does what the fix asks. /home/user/ElmsNest/theme/sections/elmsnest-s-home-winter.liquid line 81 is `.ens-hw__h2.env2-h{font-size:clamp(25px,3.4vw,44px);line-height:1.1;margin:0 0 12px}` and the ≥901 block (line 89) only sets `margin-block-end:16px` — there is no `font-size:34px` rule anywhere in the file (grep for 34px returns nothing). `git diff HEAD` confirms the uncommitted working tree removed `-.ens-hw__h2.env2-h{font-size:34px;...}` and replaced the base `26px` with the neighbours' clamp. The current verify/sizes.json also contradicts the finding's numbers: ens-home-winter-d heading box is h 48 (= 44px × 1.1), not 37; at 360/390 it is h 28 (≈25px), matching the clamp minimum. SPEC.md line 61 explicitly specifies «ובחורף?» at \"the neighbours' clamp(25px,3.4vw,44px)\", so the finding's claim that \"the spec asked for 34\" is also wrong — 34 px in the spec refers to the band heading (line 75). The finding was made against the older committed state (or the simplify/verify-after screenshot), not the current file/evidence."}, {"refuted": false, "confidence": 0.85, "reason": "The contract agrees with the finding rather than deciding otherwise: the amended home2 SPEC (header note and §1.B) says the winter heading uses «the neighbours' clamp(25px,3.4vw,44px)», and critique/LEAD-DECISIONS.md #10 explicitly orders «drop the section's font-size overrides on .ens-hw__h2 … one scale for consecutive h2s». The 34 px value the critic cites was the committed round-2 build (6e2c01e, line 101 of the diff), so a shopper at 1366 did see consecutive serif headings at 34 vs 44 px. Note the working tree already applies the fix (line 82 now reads clamp(25px,3.4vw,44px), uncommitted), so acting on it means keeping/committing that change and re-verifying, not a new edit; it is a one-line CSS change with no Kalles, JS or section cost."}], "survives": true}
{"file": "/home/user/ElmsNest/theme/sections/elmsnest-s-home-winter.liquid", "line": 75, "severity": "minor", "where": "`.ens-hw__fig{…border:1px solid #1f1e1d…}`", "defect": "The winter photograph is the only photograph on the home page with a hairline border: the solar frames (line 102 of the solar file) and the band figure have none, the hero and collection tiles have none — only product cards carry the #1f1e1d hairline (the skin's card rule). In winter-m.png / winter-d.png the 1 px frame around a night photo on a night ground is faint, but it quietly puts the winter image in the product-card class instead of the photograph class its two siblings belong to.", "evidence": "/home/user/ElmsNest/brief/side-pages/home2/verify/winter-d.png; /home/user/ElmsNest/brief/side-pages/home2/verify/winter-m.png", "fix": "Drop `border:1px solid #1f1e1d` from `.ens-hw__fig` so the three new photographs share one treatment.", "lens": "designer", "votes": [{"refuted": true, "confidence": 0.95, "reason": "The file already does what the fix asks. In the working tree, /home/user/ElmsNest/theme/sections/elmsnest-s-home-winter.liquid line 78 reads `.ens-hw__fig{...background:#05070c;border:0;border-radius:0}` — no `1px solid #1f1e1d`. The only `#1f1e1d` hairlines in the three new sections are the solar section's fit-line block (`.ens-home-solar__lines` / `__line`, lines 120–121), which the SPEC explicitly calls for; the band figure (line 66) and winter figure both carry `border:0`, and the solar frames have no border rule at all, so the three photographs already share one treatment. The critic was reading the committed version (HEAD 6e2c01e has `border:1px solid #1f1e1d` on that rule); the uncommitted diff replaces it with `border:0` and adds the header comment \"No link, no button, no card chrome, no border\", matching SPEC.md line 61 (\"no border on the figure — photographs on this page carry no frame\"). The verify screenshots (winter-d.png 08:54:21, winter-m.png 08:54:14) were rendered after the source edit (08:46:41), and winter-d.png shows the photo sitting on the night ground with no visible hairline. Note the line number in the finding (75) is also off — the rule is at line 78."}, {"refuted": true, "confidence": 0.95, "reason": "The contract agrees with the finding's principle, but the defect no longer exists: SPEC §1 B says \"no border on the figure — photographs on this page carry no frame\" and the amendment header says \"the winter figure has no border\", and the current /home/user/ElmsNest/theme/sections/elmsnest-s-home-winter.liquid line 78 already reads `.ens-hw__fig{…border:0;border-radius:0}` — no `border:1px solid #1f1e1d` anywhere in the file (the band figure at band.liquid:66 is `border:0` too). The finding was made against the pre-amendment build (b27711c) and the verify shots winter-d/m.png predate the fix, so acting on it now would be a no-op; the only remaining action is to re-shoot the winter verify images, not to edit CSS."}], "survives": false}
